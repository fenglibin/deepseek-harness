# Policy-inheritance overlay: the root session is switched to read-only at
# creation (the UI Access switch equivalent), so a continuable background
# child must inherit that override instead of the deployment default.
- insert:
    - id: parent-sandbox-override
      name: '../../../packages/test-support/session-snapshot/tests/fixtures/parent-sandbox-override.ts'

# `auto` follows the host's own locale, so a replay's prompt would change with
# the machine that produced it. Pin English — which emits no directive — and
# every recorded system prompt stays machine-independent.
- id: response-language
  name: '@deepseek-ai/dsh-response-language'
  config:
    language: en
