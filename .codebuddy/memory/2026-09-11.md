# 2026-09-11 变更记录

## 轮次用时面板新增「输出峰值速度（TPS）」

**时间**：2026-09-11 11:40
**类型**：新功能
**语言/技术栈**：TypeScript / React / Vitest / Playwright

### 变更内容

- `deriveTurnMetrics` 新增 `peakTokensPerSecond`：对该 turn 内每个同时具备 decode 墙钟与 provider 上报输出 token 的步骤，算 `outputTokens ÷ (decodeMs / 1000)` 并取最大值。
- 该值经 `TurnTailChatData` 与 `TurnTimePanelProps` 透传到轮次用时弹窗，渲染为「输出峰值速度（TPS）」一行，排在输出速度之后、首 token 用时之前。
- decode 墙钟为 0 的步骤不产生比值，被排除；所有步骤 decode 墙钟均为 0 时不输出该行。
- 需求中的另外两项（首 Tok 耗时、每秒平均 Tok）在改动前已由同一弹窗的 TTFT 行与「输出速度（TPS）」行提供，本次未重复实现。

### 涉及文件

- `packages/client/ui-chat/src/client/contract/turn-metrics.ts`：`TurnMetrics` 与 `TurnFold` 新增字段，折叠逻辑新增单步比值取最大。
- `packages/client/ui-chat/src/client/contract/chat-nodes.ts`：`TurnTailChatData` 新增可选字段。
- `packages/client/ui-chat/src/client/conversation-nodes/turn-tail.ts`：透传峰值。
- `packages/client/ui-chat/src/client/chat/TurnUsagePanel.tsx`：`TurnTimePanelProps` 与弹窗新增一行。
- `packages/client/ui-chat/src/client/chat/TurnTailNodeView.tsx`：传入峰值。
- `packages/client/ui-chat/src/client/locale.ts`：新增 `message.turnTime.peakSpeed`。
- `packages/client/ui-chat/tests/chat-snapshot-fixture.client.ts`：fixture 同步透传。
- `packages/client/ui-chat/tests/turn-metrics.client.spec.ts`：新增 3 条用例，3 条既有全等断言补字段。
- `packages/client/ui-chat/tests/turn-usage-panel.client.spec.tsx`：新增渲染与省略断言。
- `apps/web/tests/turn-tail-actions.e2e.ts`：用时弹窗 tok/s 行计数由 1 改为 2。
- `.agents/notes/implemented/feature/2026-09-11-turn-peak-decode-throughput.zh.md`：新增 Agent Note（项目强制）。

### 四向追溯矩阵

| 需求 # | todo 任务 | 代码位置 | 测试用例 |
|--------|-----------|----------|----------|
| 需求-1 每秒最高 Tok（口径 A） | 折叠单步峰值 | `turn-metrics.ts:11-18, 86-96, 116` | `turn-metrics.client.spec.ts::takes the peak from the fastest step rather than the last one` 等 3 条 |
| 需求-2 面板展示峰值 | 用时弹窗新增行 | `TurnUsagePanel.tsx:26-28, 275-281` | `turn-usage-panel.client.spec.tsx::shows a clock-and-duration pill and opens the time dialog on click` |
| 需求-3 未记录时不展示 | 条件渲染 | `TurnUsagePanel.tsx:275` | `turn-usage-panel.client.spec.tsx::omits unrecorded speed and TTFT rows` |
| 需求-4 首 Tok / 平均 Tok 可见 | 既有实现 | `TurnUsagePanel.tsx:268-274, 282-287` | 同上两条 |

### 测试验证

| 检查 | 结果 |
|------|------|
| `vitest run packages/client/ui-chat/tests` | 26 文件 / 329 条通过 |
| `npm run typecheck` | 通过（`tsc -b tsconfig.client.json` 无错误） |
| `npm run verify-client-ui-i18n` | 通过（555 文件） |
| `npm run verify-agent-note-format` | 本次文件无违规 |
| `npm run verify-md-wrap` | 本次文件无违规 |
| `npm run verify-export-jsdoc` | 本次文件无违规 |
| `npm run test:e2e` | 26 条失败，全部为 `SAFE_DELETE_BULK_CONFIRM_REQUIRED`（IDE 注入的 safe-delete shim 拦截测试临时目录清理），与本次改动无关 |

### 漏拦复盘

- **本次 DoD 门禁是否发现并当场修复了任何偷懒/漏实现？** 是。单元测试层全绿后，跨层检查发现 `apps/web/tests/turn-tail-actions.e2e.ts:200` 断言用时弹窗内 `/tok\/s/` 计数为 1；新增一行后该计数变为 2，单测层无法拦住。已同步该断言。
- **阶段 5 需求完整性校验是否发现任务级 DoD 漏拦的需求点？** 是，属于测试面漏拦而非功能漏拦。原因：DoD 只列了「本包单测」，没有列「展示同一数据的跨包 e2e 断言」。改进：凡新增/删除 UI 可见行，DoD 必须追加一项「grep 该文案或计数断言的全部消费点」。
- **本次交付是否有任何"口头说完成但实际是占位"的情况？** 否。

#### 双向修复 3 问

1. **这个 bug 是哪种通用模式？** 新增「模式 M.7：UI 行数变化击穿按文本计数断言的跨包 e2e」。既有的 M.1（helper 不存在）等都不覆盖这种"断言按数量而非语义"的失效方式。
2. **其他语言 / 模块是否同样脆弱？已扫了吗？** 已扫：全仓库 grep `tok/s`、`TTFT`、`Turn time`，确认除本文件外，其余命中都在 StatsLine 与会话统计 golden（未改动），另有一处 trajectory tooltip 断言与本次无关。
3. **有没有写自检脚本 + 单测机器拦截这种模式？** 未落地脚本。建议后续在 `verify-client-ui-i18n` 或新增检查中，对「同一 locale key 的渲染行数」与「e2e 中的按数量断言」做交叉核对；本次先以 DoD 清单条目形式约束。

### 注意事项

- 峰值与平均共用同一口径（provider 上报 token ÷ decode 墙钟），单步骤 turn 两者必然相等。
- 峰值随客户端窗口口径伸缩，与既有 TTFT、平均速度一致。
- `apps/web/tests/turn-tail-actions.e2e.ts` 的英文界面断言（如 `'Turn time and speed'`）与当前客户端仅内置中文（`LOCALE_IDS = ['zh']`）的现状不符，属既有状态；本次仅同步了受影响的计数断言，未扩大范围改写其余英文断言。
