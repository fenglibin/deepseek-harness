/** Host loader entry for the skills settings section exported from `./client`. */

/** Host plugin body — the skills section reads and writes through the Host Remote alone. */
export function apply(): void {}
