- [revision 3] 实现 MCP 设置页优化五项：

1. 去掉底部"添加服务器"按钮：McpSection 移除 addBlock/addButton/adding 状态与 IconPlusOutline16 导入。
2. 自动连接：进入分区时对 enabled 且非 connected 的服务器逐个串行 refresh（status store 单次刷新锁，串行避免丢弃），autoConnected ref 防回环。
3. 编辑改写 mcp.json：Host 新增 updateMcpServer(entry) Remote 方法（读 mcp.json → settingsToMcpJson 转单条 → 替换 → 写回 → 立即 sync），McpStore.update 改走该方法；McpServerDialog 弹窗宽度 570px（+50%，双类选择器覆盖 Modal 默认 380px）。
4. 配置 MCP 改就地编辑器：删除 openMcpDocument 原生打开；Host 新增 readMcpDocument/writeMcpDocument Remote 方法；新增 McpJsonEditor 组件（轻量 JSON tokenizer 高亮层 + 透明 textarea 叠加，格式化按钮，保存前 JSON.parse 校验）；mcp-document-store 重构为 read/write store。
5. 立即生效：writeMcpDocument/updateMcpServer 在 writeFile 后直接 await syncMcpJson()，经 scope.replace→watch→apply→reconcile 立即重连，不依赖 chokidar 去抖；watcher 二次读到相同内容因 deepEqualJson 判等跳过重复 apply。

配套：删除 openMcpDocument 及 native-command 依赖；status.ts 新增 McpDocumentTextValue/McpDocumentWriteValue 并 re-export McpServerEntry 到 ./types 子路径（typert Remote 边界类型要求）；locales 新增 format/editorTitle/invalidJson/saveFailed；更新两个包 README；新增/更新 6 个测试文件。
