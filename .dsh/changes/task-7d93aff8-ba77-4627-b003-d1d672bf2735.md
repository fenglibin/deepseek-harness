- [revision 8] 去重键改为规范化路径：`canonicalMutationPath(path, cwd)` 用 `resolveWorkspacePath` 把相对路径按会话 cwd 解析为绝对路径，统一分隔符并消解 `.`/`..`，`sessionChanges(conversation, cwd)` 以它为折叠键并输出规范化路径——同一文件的绝对/相对两种拼写收敛为一条，不同目录的同名文件仍各占一行。
- [revision 9] 列表行改成可点击按钮，展示完整路径（目录段可省略、文件名段完整），点击经注入的 `openFile` 调 `remote.session.openWorkspacePath` 打开；宿主拒绝时在条带下方渲染一行失败原因。`locales.ts` 新增 `open` / `openFailed` 两条文案，CSS 新增 `.dirName`/`.baseName`/`.openError`。
- [revision 10] 注册新增 `SessionChangesInjected { cwd, openFile }`（从 `sessions.list` 读工作区根，opener 只转接已规范化的绝对路径），注入服务加入 `sessions`/`remote`/`remote.session`；`package.json`、`tsconfig.json` 的包边同步。测试扩到 26 条（含 apply 注入），改动的两个 client 源文件在覆盖率下 100%；README 与 Agent Note 同步。
- [revision 13] coverage confirmation: 每一条记录的 design/spec 都已实现并有测试锚定：D1 规范化去重 → `canonicalMutationPath` + `sessionChanges(conversation, cwd)`，测试覆盖「相对/绝对同一文件」「分隔符与点段变体」「不同目录同名文件各占一行」；D2 cwd 注入 → `apply` 的 inject 工厂读 `sessions.list`，测试断言 `injected.cwd === '/proj'`；D3 openFile 单次转接 → 测试断言 `openWorkspacePath` 收到 `{ path: '/proj/src/a.ts' }`，且 `!ok` 时以宿主消息 reject；D4 目录段+文件名段 → 测试断言 `/proj/src/`、`/proj/lib/` 与两个带完整路径 aria-label 的按钮；D5 失败就地成行 → 测试覆盖 Error 与非 Error 两种拒绝及成功后清除。未跑 `test:web`（需要全仓库 build + 浏览器，且当前工作树有他人正在编辑的 session-controller 改动），这是本次验证唯一未覆盖的环节。
