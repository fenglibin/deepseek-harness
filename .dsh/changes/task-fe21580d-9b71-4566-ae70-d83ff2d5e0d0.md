- [revision 11] 实现完成。新增 `HiddenToolFailureProjector`（`packages/client/ui-chat/src/client/conversation-nodes/hidden-tool-failure.ts`），按 `tool/result` 上持久化的 `error.code` 无条件隐藏 `FS_EDIT_NOT_FOUND` / `FS_AMBIGUOUS_EDIT` 的顶层工具调用行，并在 `chat-snapshot-builder.ts` 的 `replace` / `apply` 两条链上装配。

测试：`conversation-node-definitions.client.spec.ts` 新增四条用例（两个码在无后续成功时即 hidden 且离开可见顺序；增量到达同样隐藏；白名单外六个错误码保持 visible；成功与无 error 的失败保持 visible），并把既有那条「非可恢复失败即使后续成功也保持可见」改用 `FS_PERMISSION_DENIED`。`pnpm vitest run packages/client/ui-chat/` 26 文件 334 条通过，`npx tsc -p tsconfig.client.json --noEmit` 通过。

文档：`packages/client/ui-chat/README.zh.md` 新增「不可操作的工具失败行」章节与 PTC 子调用的 Known Limitation；新增 Agent Note `2026-09-16-unactionable-edit-failure-rows-hidden.zh.md`，并就地更新 `2026-09-04-chat-ux-recovered-mutation-errors-hidden.zh.md` 中与现状矛盾的两处表述。

模型侧未改动：`tool/result` 的 `content` 与 `error`、会话事件格式、快照、两个 SDK 期望输出全部不变。
