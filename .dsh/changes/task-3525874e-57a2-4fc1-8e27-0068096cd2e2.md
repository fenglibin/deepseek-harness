- graded l1 by model (127 chars vs floor 200; model judgement)
- [revision 4] D1：交付分级判定移出 agent/pre-step 关键路径。packages/delivery/tool-delivery/src/index.ts 的 pre-step 监听改为同步 next()，判定由新增 scheduleAutoDetect/runAutoDetect 后台执行：按 agent 去重一处在飞判定；判定落地后重新读取当前任务，l1/l2 与模型自建任务都不替换（只处置「无任务或可替换的 l0」）；signal 或插件 lifetime 已中止则不提交任务；失败沿用 logger.warn 不影响 turn。because user/message 只在 pre-step waterfall 解析后 append，原先同步 await 判定会让用户自己的消息在整个判定期间不出现在 transcript（实测 6908ms）。

测试：既有 148 条全部通过（其中 4 条把「pre-step 返回即任务已建」的旧断言改为等待后台落定，preStep 辅助函数新增 settleAutoDetect）。新增 2 条直接证明解耦的用例：StubLlm 增加 beforeResolve 闸门，「releases the step before the grading call resolves」证明判定挂起时 waterfall 已返回且此时无任务、放行后任务才落地；「leaves a task the model created while the judgement was pending」证明后台判定不覆盖模型自己在挂起期间创建的 l1 任务。命令：npx vitest run packages/delivery/tool-delivery/tests/tool-delivery.spec.ts → 150 passed。
- [revision 5] D2：回显改为「队列出现不再退休，只在 durable 落盘或用户移除时退休」。
- packages/api/session-controller/src/client/sessions/session.ts：删除 observeSubmissionQueue（队列帧不再退休回显）及其两处调用；新增 retireRemovedQueueEcho，由 updateQueue 在 action.kind === 'remove' 成功后调用。observeSubmissionEvent 的 JSDoc 说明为何队列出现不退休。
- packages/client/ui-chat/src/client/chat/ChatView.tsx：observedRpcIds 只把 placement === 'steering' 的队列行计入「已渲染」，queued 不再隐藏回显，因为 queued 行渲染在 QueueDock（composer 上方）而 steering 气泡渲染在消息流尾部（与回显同一位置，不隐藏会双渲染）。

测试：npx vitest run packages/api/session-controller/tests → 463 passed；npx vitest run packages/client/ui-chat/tests packages/client/ui-conversation/tests → 706 passed。改写的断言：session-pending-submissions 的「queue occurrence retires the echo」改为「keeps the echo while a queued occurrence is pending, then retires it on removal」；chat-view 的「hides an echo once its queue occurrence carries the rpcId」改为「keeps an echo while its queued occurrence is pending」并新增 steering 反例。
- [revision 6] D3：补齐 subagent 会话的回显路径（原路径完全不走 beginSubmission）。
- packages/client/ui-conversation/src/client/service.ts：sendSession 的 subagent 分支改为先 beginSubmission 注册回显，再 await nextPaint + contentFor 序列化，并把 submission.requestId 传给 prompt；序列化失败走 submission.abandon()。
- packages/api/session-controller/src/client/sessions/session.ts：prompt() 的 subagent 分支原先无条件 randomUUID() 覆盖 requestId，改为 requestId ?? randomUUID()，否则回显注册的身份永远无法与其 durable 消息关联（原 Agent Note 记的「transport 分配了另一个 RPC id」即此）。

核查记录：2026-08-26 的 local-submission-echoes Note 称 subagent 路径「transport 会分配另一个 RPC id」因而不注册回显。核对 packages/subagent/subagent/src/index.ts:454 后确认该 transport 其实把 request.requestId 原样写入 source.rpcId，只是客户端 prompt() 没把回显身份传下去——前提不成立，故补上回显。

同批核查（未改动）：@ 引用序列化路径（facade.ts sinkSerialized）在引用序列化完成后才调用 defaultSink，从而推迟回显发布。查全部 registerSource 实现后确认当前唯一提供 codec 的 ui-reference 是同步的（serialize: ref => Promise.resolve(ref)），ui-cordis / ui-commands / ui-skill 均无 codec，因此该路径当前不产生可观测延迟，未作改动；其结构性风险（未来引入异步 codec 即重现）记入设计文档。

测试：service-orchestration 的「sends a subagent continuation without registering an unobservable echo」改写为「registers a subagent echo and prompts under its identity」，断言 beginSubmission 被调用且 prompt 收到回显身份。
- [revision 7] D4 + 并发缺陷修复 + 文档。

D4：回显气泡在等待 durable 落盘超过 400ms 时显示「已发送，等待处理…」。packages/client/ui-chat 的 MessageItem.tsx 新增 useDelayedFlag（组件内行为，不订阅外部，不进入 reactive-read 通道）与 UserStyleBubble 的 notice 位；MessageItem.module.css 新增 .waitNotice；locale.ts 新增 message.echo.sending。阈值延迟是必要的：普通发送一两帧内即被替换，一闪而过的提示读起来像缺陷。

并发缺陷修复（跨包并发测试暴露，单跑不复现）：scheduleAutoDetect 原先在 pass 在飞时直接 return，导致连续两次发送中第二次永远不会被分级——因为 create 拒绝在存在非 accepted 当前任务时新建，且没有工具能清空任务，那条请求会既不建任务也不被替换。改为 per-agent AutoDetectState 记住最新一条待续请求并由 follow-up pass 续接；落地时重读当前任务，模型期间自建的任务不被覆盖。settleAutoDetect 的宏任务轮次从 8 提到 60（该 pass 结尾经真实文件系统写分级依据，并发下 8 轮不够）。

文档：新增 .agents/notes/implemented/architecture/2026-09-17-message-visibility-decoupled-from-recognition.zh.md（本次反转了既有决策，按规则需新 Note 并交叉链接）。就地更新三条受影响的 Note：local-submission-echoes（队列 occurrence 不再退休回显、subagent 现注册回显、验证与后果章节同步）、model-graded-delivery-tiers（判定不再阻塞 pre-step、代价章节同步）、delivery-discipline-auto-trigger（永不阻塞步骤的确切语义）。同步三条 README：session-controller（回显退休规则）、ui-chat（queued/steering 的渲染位置差异与等待提示）、tool-delivery（判定不阻塞步骤与并发约束）。同步 contract/session.ts 的 PendingSubmissionRetirement 与 beginSubmission/prompt 的 JSDoc 契约。

验证命令与实际结果：npx vitest run（session-controller + ui-chat + ui-conversation + tool-delivery 四包）→ 99 files / 1345 tests passed；npx tsc -b tsconfig.client.json → exit 0；npx tsx scripts/run-oxlint.ts（改动文件）→ 0 warnings 0 errors；verify-agent-note-format / verify-md-links / verify-client-ui-i18n → 我新增与改动的文件均无违规（其余报错为工作区既存问题与其它会话的并发改动，非本次改动引入）。
- [revision 10] acceptance: deep-selfcheck: 用真实 agent-loop 做对照实验，推翻了两处「测试通过但语义错误」，并修正了一处探针自身的统计口径。

一、需求完整性：D1–D4 均有实现与测试。req/1（消息立即显示、与识别互不影响）由 D1（判定移出 pre-step）+ D2（回显不被 queued 队列帧收走）共同覆盖；识别与显示互不影响在真实 loop 上得到证明（见下）。

二、实现正确性——对照实验（关键证据）。既有包测试用手工派发 agent/pre-step 的 stub agent，观察不到本改动所针对的时序，因此我写了临时探针 zz-selfcheck-ordering.probe.ts：真实 Context + LlmRuntime + SessionStore + AgentLoop + DeliveryService + toolDelivery + 最小 shell 服务，用一个可闸门的 adapter 挂起分级调用，记录真实 append 顺序。随后 git stash 我的 index.ts 改动，跑同一探针做对照（探针已删除，非交付物）：
- 改动前：grading-request → delivery/change → step/start → user/message；userMessageLandedWhileGradingPending=false。
- 改动后：grading-request → step/start → user/message → … → delivery/change；userMessageLandedWhileGradingPending=true。
即：改动前用户消息必须等判定落地才落盘，改动后判定仍挂起时消息已落盘。这正是 req/1 的机制断言，且与实测日志的 6909ms 窗口成因一致。探针另确认只发起 1 次分级调用（callShapes: maxTokens=16 的分级器 + maxTokens=0 的主 turn），无重复分级。

三、自检修复的两个真实缺陷（均非测试暴露，是走查 + 探针暴露）：
1. 移除队列项用 reason:'failed' 会触发草稿还原。failed 语义会让 sendSession 返回 error，facade 据此把消息塞回输入框；但队列删除是用户主动丢弃，不该回来。且只有图片消息会这样（纯文本因 retirement===undefined 不走该分支），行为不一致。修法：新增独立语义 reason:'removed'（contract/session.ts），session 走 retireRemovedSubmission，service 侧对 removed 返回 success 并 revoke 预览而不交给持久缓存。补测试：service-orchestration「reports a user-removed queue occurrence as success so the draft is not restored」。
2. 编辑队列项后消息流回显仍显示旧文本。核实 host 侧 commands.ts 的 edit 分支为 {...message, content:[...]}，保留 source（含 rpcId），故 durable 落盘前回显不更新——用户改了内容、transcript 却显示旧的。修法：updateQueue 对 edit 也读 rpcId 并调 retargetSubmissionText 改写回显 text/parts（retiring 的跳过硬写以免复活）。补测试：session-pending-submissions「rewrites the echo content when its queued occurrence is edited」与「leaves a steered echo pending」。
3. 探针自身的统计口径错误（已随探针删除）：用 maxTokens<=16 判定分级调用，把主 turn（maxTokens=0）也算成一次，导致误报 gradingRequests=2。经 callShapes 逐条核对确认为 1 次。

四、业务正确性：回显跨越 turn 开局后，需要自己解释等待（D4 的 400ms 阈值提示）与显式结算路径（remove/edit/durable 三种），这正是本轮两个修复的来源。steering 行渲染在消息流尾部与回显同址，故仍由它让位；queued 行渲染在 QueueDock，与消息流是不同表面，两处并存（用户已确认）。

五、残余风险：a) 分级判定落地时重读当前任务，若模型在此期间自建 l1/l2 任务则不覆盖，属刻意取舍；b) facade.ts 的 sinkSerialized 仍在 @ 引用序列化后才发 defaultSink，当前唯一 codec（ui-reference）同步，故无可见延迟，未来引入异步 codec 会重现——已记入设计文档待定项与 Agent Note 未决节；c) 图片描述仍留在准入路径，按设计不改（用户已确认模型请求仍需等描述就绪）。

六、验证命令与结果：npx vitest run（session-controller + ui-chat + ui-conversation + tool-delivery 四包）→ 99 files / 1348 tests passed；npx tsc -b tsconfig.client.json → 无输出（通过）；npx tsx scripts/run-oxlint.ts（改动文件）→ 0 warnings 0 errors；实测证据来自本机 session-4194cc9a 日志与上述对照探针。
- [revision 12] coverage confirmation: 每条已记录设计与需求点均已实现并有测试锚定：design/D1（分级判定移出 pre-step，改后台 pass，含续接与竞态处置）由 tool-delivery 的「releases the step before the grading call resolves」「grades a request that arrives while an earlier judgement is still pending」「leaves a task the model created while the judgement was pending」及真实 agent-loop 对照实验覆盖；design/D2（回显不被 queued 队列帧收走、steering 让位、remove/edit 自行结算）由 session-pending-submissions 的六条用例与 chat-view 的 queued/steering 正反两例覆盖；design/D3（subagent 注册回显并透传 requestId）由 session 侧的 requestId 转发与 service-orchestration 的「registers a subagent echo and prompts under its identity」覆盖；design/D4（400ms 阈值等待提示）由 chat-view 的「says an echo is still unconfirmed only after the wait threshold」覆盖。自检新增的两处语义修复（removed 不还原草稿、edit 改写回显）各有专门用例。req/1（消息立即可见且与识别互不影响）由真实 loop 对照实验直接证明：改动前 user/message 必须等 delivery/change 之后（userMessageLandedWhileGradingPending=false），改动后判定仍挂起时消息已落盘（true）。验证依据为实际运行的命令：四包 1348 测试通过、tsc -b tsconfig.client.json 通过、改动文件 lint 0 警告 0 错误。图片描述按设计保留在准入路径（用户已确认模型请求仍需等描述就绪），不属于本任务改动，已在设计文档与 Agent Note 的未决节记录。

## 后续请求：交付任务卡片撤出转录

- 需求：交付纪律自动建任务时在会话页面插入的任务卡片（红框区域）不再展示，后台执行即可，其它逻辑不影响。

- [变更] packages/client/ui-delivery/src/client/delivery-definition.ts：`deliveryTaskDefinition.buildViewNode` 的 visibility 由 'visible' 改为 'hidden'。节点仍完整折叠 delivery/change（匹配、折叠、状态构造全部保留，Chat 快照仍持有目标/分级/阶段/计数/逐条事件），只是 orderedVisibleChatNodes 把它排除在渲染顺序之外，卡片不再占用消息流。
- 选择 hidden 而非注销 Definition：delivery/change 是 append-surface 事件，无人匹配时会落到 registerUnknownConversationFallback，渲染成「未知 surface 事件：delivery/change」行，比原卡片更糟。hidden 是保折叠、去渲染的唯一路径，也是仓库既有的「撤下该行」惯用法（request-prompt.ts 同法）。
- [变更] packages/client/ui-delivery/tests/delivery-task.client.spec.tsx：新增「builds the node hidden so the task card stays out of the transcript」，断言节点存在、visibility==='hidden'、折叠仍跟踪到 'designed'。反向验证确认有效：把实现改回 'visible' 时立即失败。
- [端到端验证] 临时探针（验证后删除）用真实 ConversationNodeAssembler + ChatSnapshotBuilder 喂入 create/advance 两条 delivery/change：node.visibility==='hidden'、node.data.phase==='designed'（折叠照常）、built.order 不含该节点 key（确实不渲染）；改回 'visible' 时同一探针失败。
- [文档] 新增 Agent Note 2026-09-17-delivery-task-card-withdrawn-from-transcript.zh.md（记录反转决策、四个被否决方案与验证）；就地更新 2026-09-04-delivery-timeline-and-float-card.zh.md（卡片改为节点且不再在转录中渲染，交叉链接新 note）；ui-delivery README 概述/使用/实现/限制四节同步。
- [产物] 重建 client bundle：DSH_BUILD_FACE=client npx tsdown --env.DSH_BUILD_FACE client；packages/client/ui-delivery/lib/client.js 已由 visibility: "visible" 更新为 "hidden"。
- [验证命令] npx vitest run packages/client/ui-delivery/tests packages/client/ui-chat/tests packages/client/ui-conversation/tests → 63 files / 752 tests passed；npx tsc -b tsconfig.client.json → 通过；oxlint 改动文件 → 0 warnings 0 errors；verify-agent-note-format / verify-md-wrap / verify-md-links / verify-doc-budgets → 我新增与改动的文件均通过。

- [相邻缺陷，未修] 探针发现：会话已有 accepted 任务时，新的直接请求不会被自动建任务替换（createReplacingL0 只在 current.level==='l0' 时放行，accepted 的 l1/l2 会抛 DELIVERY_TOOL_TASK_EXISTS，而 runAutoDetect 的 catch-local 分支把它记成 warn 后静默跳过）。探针输出 PROBE_RESULT phase=accepted objective=first（新请求 'a brand new request' 未生效）。按本次需求「仅去掉展示、其它逻辑不影响」未作改动，仅在此记录。

## 后续请求：修复「相邻缺陷」（accepted 任务占死会话）

- 缺陷：会话存在已 accepted 的 l1/l2 任务时，新请求既不自动建任务、模型也建不了。三处规则叠成死结：domain（DeliveryService.create）本就允许 accepted 之后新建、fold.ts 也只要求前一任务 accepted；create_delivery_task 工具描述写明「An accepted task may be replaced」；但策略 createReplacingL0 只豁免 l0（current.level !== 'l0' 即抛 DELIVERY_TOOL_TASK_EXISTS）。accepted 的 l1/l2 既非可替换 l0、又没有任何工具能 clear，会话被一个已完成任务永久占住。runAutoDetect 里同样的 level !== 'l0' 判断让它在分级调用前就 return，把失败记成一次 warning 后静默跳过。

- [变更] packages/delivery/tool-delivery/src/index.ts：新增 holdsClaim(current) 谓词（current !== undefined && level !== 'l0' && phase !== 'accepted'），把「任务是否仍占用会话」收成一处归属，由 createReplacingL0 与 runAutoDetect 共用。accepted 因此与 l0 一样放行。clear 只在 l0 分支执行：accepted 由 domain 自己的规则放行，多写 clear 会留下没有消费者的事件。runAutoDetect 的两处 return 都改用该谓词（进入时判断是否需要分级；判定落地后重新判断，因模型可能在此期间建了或完成了任务）。
- [变更] tests/tool-delivery.spec.ts 新增三条：auto-detect 路径在 accepted 的 l1 之后建出新任务；create_delivery_task 同路径建出任务；替换 accepted 任务时只写一条 create（不写 clear tombstone）。原有「still refuses to replace an unfinished l1 task」与「does not create a second task when one already exists」锁定未完成 l1 的占用不变。
- [反向验证，关键] 把 holdsClaim 改回 level !== 'l0' 后前两条立即失败（expected 'finished work' to be 'a brand new request'；工具调用 expected true to be false）。第一版测试用 l0 构造 accepted 场景，反向验证**未能失败**——l0 本就走在放行侧，属假绿；改用 l1 后反向验证才成立。真实 agent-loop 探针同向确认：修复后任务 id 与 objective 均为新值，还原缺陷后仍是旧任务。
- [顺带修复] tool-delivery.spec.ts 里上一轮遗留的两处 lint 错误：settleAutoDetect 的箭头函数参数缺括号、MockAdapter 的冗余 as StreamChunk。
- [类型] npx tsc -b tsconfig.host.json 曾报 index.ts(371) 'current' is possibly 'undefined'——普通布尔函数不做类型收窄，已在调用处补 current !== undefined &&；随后 host 类型检查干净通过。
- [文档] 新增 Agent Note 2026-09-17-accepted-task-no-longer-blocks-session.zh.md（含四个被否决方案与「l0 假绿」教训）；更新 2026-09-17-l0-selfcheck-and-ordered-acceptance-commands.zh.md（补 accepted 走 domain 路径的事实与死结成因）、tool-delivery/README.zh.md（替换规则两处）、docs/design/delivery-l0-selfcheck-and-command-ordering.zh.md（第 39 行当前实现描述）。
- [验证命令] npx vitest run（tool-delivery + delivery + session-controller + ui-delivery）→ 50 files / 773 tests passed；npx tsc -b tsconfig.host.json → 通过；oxlint 改动文件 → 0 warnings 0 errors；verify-agent-note-format / verify-md-links → 我新增与改动的文件通过（md-wrap 报的两处为既存问题，位置与改动前一致）。
