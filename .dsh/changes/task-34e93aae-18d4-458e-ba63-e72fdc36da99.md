- graded l2 by character-floor (359 chars vs floor 200; medium:replac, weak:优化, weak:页面)
- [revision 10] ## 交付纪律卡片与验收命令（change `update-delivery-card-usability`）

### 客户端（`packages/client/ui-settings-plugins`）
- `DeliveryCard.tsx`：删除 `</PluginCard>` 之后的游离 `)`（原先被 React 渲染为文本节点）；三个信号词表由文本域改为 `TagField`；新增 `VerificationCommands` 复选框选择器（含空态、只读禁用、已消失命令仍可取消勾选）；字段包入两列栅格。
- `card-form.ts`：新增 `tagField`，值形状仍是 `string[]`（Host schema 不变），重复条目在 parse 阶段被拒绝；删除了因此失去唯一消费者的 `listField`。
- `fields.tsx`：新增 `TagField`（逐条标签 + 删除按钮 + 回车/按钮提交，待输入为组件局部状态）；`ListField` 随 `listField` 一并删除。
- `fields.module.css`：新增 `.grid`（两列、窄视口单列）、`.wide`（整行）、标签与复选框样式。
- `PluginCard.tsx` / `.module.css`：描述行与帮助链接改为同行（两者是兄弟节点——头部整体是展开按钮，嵌套按钮无效且破坏键盘可达性）。
- `delivery-card-controller.ts`：`postHooks` 字段改为 `verificationCommands`；绑定 `prompt-commands` 作用域读出候选命令，并投影 `verificationCandidates` / `verificationMissing`；新增 `toggleVerificationCommand`。
- `index.ts`：为交付卡片额外绑定 `prompt-commands` 作用域。
- `locales.ts`：补齐信号词表的匹配规则（忽略大小写、子串包含、每条计 1 次、编号列表额外计 1 个中等信号）、可配置取值与新增方式，解释 `replac` 类词干是刻意写法，新增验收命令相关文案，更新帮助对话框对应条目。

### 宿主（`packages/delivery/tool-delivery`）
- `index.ts`：`Config.postHooks` → `verificationCommands`（提示词命令名），`resolveConfig` 校验非空字符串；新增 `acceptanceBodies`（经 `ctx.settings.describe()` 读 `prompt-commands` 取提示词正文）与 `acceptanceRequirementMessage`；`advance_delivery_task` 的 `verified` 分支新增验收记录门禁，未留记录即阻止并把提示词写进门禁消息（`deferContext` 只在成功路径附加，抛错会丢弃）。
- `verification.ts`：`verificationCommandFailure` 只跑 L2 的 `openspec validate <changeId> --strict --json`（退出码判定不变）；新增 `ACCEPTANCE_RECORD_PREFIX` 与 `acceptanceGap`（按当前任务过滤、逐命令计数，接受 `/name:` 装饰写法）。
- `grading.ts`：核实并保留 `replac`/`upgrad` 等刻意词干（实测三个默认词表内不存在互相包含的条目对，因此不会重复计数；换成整词反而会漏掉 `replacing` 并与 `replacement` 重复计数）。

### 测试
- `tool-delivery.spec.ts`：原 post-hooks 用例重写为验收门禁用例，覆盖未留记录阻止、留下记录放行、逐命令计数、命令已消失、未配置时自由通过、空命令名注册前拒绝、L2 结构校验失败阻止、L2 仍按 change id 定向执行、解析边界（`/name:` 装饰、他人任务记录不生效、普通变更记录不算验收）、提示词为空与未挂载设置服务时的说明。
- 客户端：`delivery-card.client.spec.tsx`（游离字符断言、帮助同行、两列/整行、复选框候选与勾选/空态/只读/已消失）、`fields.client.spec.tsx`（TagField 增删、重复拒绝、只读禁用）、`stores.client.spec.ts`（tagField round-trip 与去重、控制器候选与 toggle/保存）、`apply.client.spec.ts`（字段面）。

### 文档
- 更新 `packages/delivery/tool-delivery/README.zh.md`（配置表、验收记录格式与判定说明）、`packages/client/ui-settings-plugins/README.zh.md`（帮助同行、两列栅格、标签编辑器、验收命令选择器）、`packages/client/ui-delivery/README.zh.md`（修正已过时的「无配置设置卡片」限制）、`docs/config-catalog.zh.md`（Config 声明与源码逐字一致）。
- 新增 Agent Note `.agents/notes/implemented/feature/2026-09-16-delivery-acceptance-commands-and-card-usability.zh.md`；同步更新被部分取代的 `2026-09-03-delivery-discipline-post-hooks.zh.md` 及其余引用该字段的 note 与设计文档的过时事实。

- [revision 13] coverage review: 四项诉求均已实现并有证据：

1. 游离 `)`：已删除（`DeliveryCard.tsx`），并加了覆盖整个渲染容器的断言；已反向验证——重新插入 `)` 时该用例失败，删除后通过。
2.1 帮助入口：`PluginCard.tsx` 改为描述文案与帮助链接同一行（`css.subline`），两者是兄弟节点（头部整体是展开按钮，嵌套按钮无效）。测试断言二者 parentElement 相同且带 subline 类，并确认链接仍能打开参考对话框。
2.2 两列布局：`fields.module.css` 的 `.grid` 两列 + `.wide` 整行，由交付纪律卡片自己声明（其它卡片布局不受影响）。测试断言短控件落在 grid 内、标签控件落在 wide 内；已反向验证——去掉 wide 声明时该用例失败。
2.3 信号词表规则与 replac：词表改为标签式编辑器；locale 补齐匹配规则（忽略大小写、子串包含、每条计 1 次、编号列表额外计 1 中等信号）、可填取值、新增方式与词干解释。关于 replac——我最初的判断（截断词干与同族词重复计数）被实测推翻：对三个默认词表做两两包含检查，未发现任何互相包含的条目对，因此 replac 正确且优于整词（words 版本会漏掉 replacing 并与 replacement 重复计数）。据此保留词干，并同步修正了 spec/design/proposal/设计文档中我先前的错误结论。
2.4 验收命令：`postHooks` 改为 `verificationCommands`（提示词命令名），客户端从「提示词命令」页勾选（含空态/只读/已消失命令仍可取消）；Host 经 settings 描述符读提示词正文，并在推进到 verified 时按「是否留下验收记录」硬门禁放行。L2 的 `openspec validate --strict` 保留为 shell 确定性校验，测试覆盖其仍按 change id 执行且非零退出阻止。

证据：受影响范围 23 个测试文件 586 项全部通过（含 delivery、ui-settings-plugins、interaction）；两个编译面类型检查对我的包无错误。文档：两处 package README、ui-delivery 过时限制、config-catalog（与源码逐字核对一致）、新增 Agent Note 并同步更新被部分取代的 post-hooks note 及其余引用该字段的 note 与设计文档；agent-note-format / classification / doc-budgets / readme-limitations 对我的文件全部通过。

已知不属本次改动、且在 HEAD 上同样失败的项：`verify-md-wrap`（含 AGENTS.md 与所有同级 design 文档）、`verify-md-links` 中 3 条既有断链、`verify-export-jsdoc` 中 coverage.ts 的既有缺参、以及 session-file-revisions / typert / ui-file-browser / ui-theme / inspector 等其它会话正在进行的改动。这些均未被我触碰。
- [revision 15] coverage confirmation: 设计文档 `docs/design/delivery-card-usability-rationale.zh.md` 的 D1–D7 与 OpenSpec change `update-delivery-card-usability` 的 spec delta（5 条 MODIFIED + 3 条 ADDED 需求，共 14 个 Scenario）逐条均已落地：

- D1 / 「交付纪律卡片不再渲染游离字符」：删除 `DeliveryCard.tsx` 中游离 `)`；断言覆盖整个渲染容器（游离文本在卡片元素之外，只看内部发现不了），反向验证通过。
- D2 / 「帮助入口紧随卡片描述」：描述与帮助链接同行（`css.subline`，兄弟节点避免按钮嵌套）；断言同一 parentElement 且链接仍可打开对话框。
- D3 / 「交付纪律字段分两列排布」：`.grid` 两列 + `.wide` 整行 + 窄视口单列，由卡片自声明以免影响其它卡片；断言 grid/wide 归属，反向验证通过。
- D4 / 「信号词表的命中判定对用户可见」：`tagField` + `TagField` 逐条增删、拒绝重复；locale 写明忽略大小写、子串包含、每条计 1 次、编号列表额外计 1 中等信号、可填任意中英文、新增方式，并解释 `replac` 类词干是刻意写法（实测词表内无互相包含对，不会重复计数）。
- D5 / 「验收命令从提示词命令中勾选」+「提示词验收留下记录才放行」：`verificationCommands` 取代 `postHooks`；客户端经 settingsScope 读 `prompt-commands` 勾选（含空态、只读、已消失命令仍可取消）；Host 在 `verified` 按验收记录硬门禁放行，门禁消息携带提示词正文与 `record_change` 要求（因 `deferContext` 在抛错路径被丢弃）。测试覆盖阻止/放行/逐命令计数/命令消失/未配置自由通过/解析边界/提示词为空/未挂载设置服务。
- D6 / 「L2 结构校验不随验收命令变更而丢失」：`verificationCommandFailure` 仅跑 `openspec validate <changeId> --strict --json` 并按退出码判定，与提示词验收互不替代；测试覆盖仍按 change id 执行、非零退出阻止、非 l2 不执行无目标命令。
- D7 / 客户端勾选交互：控制器绑定 `prompt-commands` 并投影候选/缺失，`toggleVerificationCommand` 写入草稿；测试覆盖候选渲染、勾选、空态、只读禁用、已消失命令、保存后落 `verificationCommands`。

证据：受影响范围 23 个测试文件 586 项全部通过；两个编译面对我的包无类型错误；文档门禁（agent-note-format、classification、doc-budgets、readme-limitations、md-links 我的文件）通过。剩余红灯均为 HEAD 上既存或其它会话正在进行的改动，未被本次触碰。
