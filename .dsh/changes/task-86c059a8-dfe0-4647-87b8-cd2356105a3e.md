- [revision 5] 按用户拍板补充设计决策 D7/D8/D9，并记录一项治理发现。

D7 分级阈值改为中文友好默认值：specChars 代码默认 200 字符对中文过低（本次原始请求即因此被自动判为 l2），与 README 示例 1200 亦不一致；长度降为辅助信号，主判定由信号命中承担，强信号词表中「重构/迁移/优化/替换」等高频词收窄或改为组合命中。

D8 l1 纳入自动创建：判定为 l1 时同样自动创建任务，使交付纪律覆盖所有非小微需求，l0 保持自由执行。

D9 分层边界：不拆为三个独立包，改为 tool-delivery 内按模块分层，另新增注册 settings.plugin.item 中 key 为 delivery 的客户端设置卡片包。理由是三个关注点共享同一份 ResolvedConfig 与工具注册流程，拆包只带来形式边界而引入跨包契约。

治理发现（重要）：openspec/changes/add-delivery-openspec-split/tasks.md 五组任务全部标记 [x]，但其 D3「双进度呈现」要求的"悬浮卡片按阶段显示子任务完成度"在当前代码中不存在。实际落地的是 add-delivery-analysis-progress 的 D3（按语义四组展示），按阶段分组的 done/total 从未实现，delivery-tasks 的 progress 字段客户端一处未用。这既解释了用户反馈的问题 1，也证明当前交付纪律无法保证其声明的完成度与实际代码一致，故 D2 的覆盖性检查必须核验产物而非模型自报状态。
- [revision 6] 实测推翻并纠正 D7 的错误前提：分级误判的主因是编号列表规则，不是长度阈值。

实测（直接调用 grading.ts 的 gradeObjective/scanSignals）：
- 本次原始请求 410 字符，在 specChars 取 200/300/1200 三档下均判定 l2，仅 600 档为 l1。故调高长度阈值不能修正该误判。
- 主因是 scanSignals 中 `numberedItemCount(objective) >= 3 ? 1 : 0` 计入 strong 命中，配合 `hits.strong > 0 return 'l2'`，使任何含 3 条及以上编号项的需求无论多短、阈值多高一律为 l2。实测 26 字符、无任何信号词的 "1、修复登录按钮/2、修复注册按钮/3、修复退出按钮" 即被判 l2。
- 本次请求的信号明细为 strong=1(编号)、medium=0、weak=3(拆分/页面/展示)。

决策修订：将编号列表信号由 strong 降级为 medium，使"3 条编号"表达的可拆分性对应 l1（出设计文档）而非 l2（出 OpenSpec 四件套）；降级后本次请求为 l1。长度阈值须在该规则修正后才成为有效主导因素，其取值连同强信号词表中「重构/迁移/优化/替换」等高频词的收窄，一并列为待定，取决于部署对 l2 成本与留痕完整度的偏好。

同时修正上一轮汇报中的失实表述："改成 1200 即可修正本次误判" 不成立。
- [revision 11] D3 机制修订并实现（用户质疑驱动）。原机制（tool-delivery 监听 session/event 捕获 todo/write 后追加持久 delivery/tasks）经实测不可行：Session.append 禁止重入（packages/core/session/src/index.ts 的 `if (entry?.appending) throw new Error('session append cannot reenter while another append is being published')`），而 session/event 监听器正是在 appending=true 期间被调用。实测输出该错误且 delivery-tasks 投影保持 null，写入完全失败。原实现还用 try/catch + logger.debug 吞掉了该错误，属静默失效。

改用方案 C：delivery-tasks 投影自身 fold todo/write，零写入、零重入风险、零延迟。实现要点：
- DeliveryTasksState 增加 level 与 phase 两个字段，从 delivery/change 跟踪（投影 fold 只能读自身 state，故必须自持任务位置）。
- mirrorTodos 仅在 level==='l1' 时镜像；l0 无清单义务、l2 被禁止调用 todo_write。
- 镜像项继承任务当前阶段，created/designed/specified 归入 implemented。
- 已记录的 l2 清单（changeId 非空）不被 todo 覆盖。
- DeliveryTasksView 新增 source: 'recorded' | 'mirrored' 供 UI 区分来源；stateVersion 1→2。
- delivery 包新增 @deepseek-ai/dsh-tool-todo 的 type-only peer/dev 依赖与 tsconfig 引用（先例：packages/session-query/session-query/src/extraction.ts）。
- tool-delivery 中新增的错误监听器与 liveAgentForSession/todoPhaseFor 辅助函数已全部移除。

测试防线修正（回应上一轮缺陷）：原来的 tool-delivery 测试 harness 未挂 SessionStore，导致 attachments 为空、重入检查不触发，功能在生产必失败而单测全绿。现于 packages/delivery/delivery/tests/projection.spec.ts 新增 7 条真实组装测试（走 SessionStore），覆盖：l1 镜像且不新增事件、整体替换、l0 不镜像、l2 不镜像、已记录 l2 清单不被覆盖、镜像项阶段归属、清除后不再镜像。并已用临时探针确认该台架确实触发 cannot reenter，证明防线有效而非假防线。

UI 侧（D6 部分）：DeliveryFloatCard 移除 items.length===0 ? todos??[] : [] 双源择一，只读 delivery-tasks；新增按阶段分组渲染 done/total 与来源标记（来自当轮清单/来自任务清单），补齐 phaseGroup/phaseHead/phaseLabel/phaseCounts 样式与 locales 文案 progress.tasks.source.recorded。

验证结果：delivery 包 73+7=80 测试通过；tool-delivery 137 测试通过；ui-delivery 40 测试通过；三者类型检查通过。已重建 delivery 包的 lib/types 产物（客户端类型检查依赖产物面）。
- [revision 12] 深度自检发现并修复 4 个业务层缺陷（均非"代码报错"，而是功能在真实场景不成立）。

缺陷1（致命·模型不可发现）：covers: 键名格式 req/<n>、design/<Dn>、<capability>/<Scenario name> 只存在于 coverage.ts 的源码注释中，guidance() 与 record_tasks 工具描述完全未提及。模型无法猜到该格式，任何 l1/l2 任务都会永久卡在 verified。修复：在 guidance() 与 record_tasks 描述中写明三类键名与写法示例（含 "the work (covers: req/1, design/D2)"）。

缺陷2（致命·注解格式与渲染互斥）：renderTasksMarkdown 与 orderItemsByMarkdown 用剥离注解后的行内容 (lineContent) 去比对 item.content，而 item.content 本身包含注解（工具描述要求模型写），导致永不匹配：① 每次 record_tasks 都向 tasks.md 追加重复行；② checklistMismatch 永不一致，l2 任务永久无法推进到 implemented。修复：新增 withoutCovers() 并对比较双方统一剥离注解，两处调用点一并修正。

缺陷3（致命·l2 scenario 覆盖静默失效）：coverageGap 依赖 `openspec show <id> --json`。实测该命令对中文标题的 proposal.md 直接报错 "Change must have a Why section"（要求英文 ## Why / ## What Changes），而中文本地化部署写 "## 为什么" 是自然的（既有 add-workspace-file-browser 也用中文标题）。命令非零退出被静默当作"无需检查"，导致所有 l2 的 scenario 点永久消失。修复：改为 discoverCapabilities() 直接 listDir 读 openspec/changes/<id>/specs/ 目录发现 capability，不再依赖对标题语言敏感的命令；openspec validate 仍是结构合法性权威。

缺陷4（既有·D8 阶段表双份定义）：LEVEL_PHASES 在 delivery/src/fold.ts 与 client/ui-delivery/src/client/delivery-phases.ts 各一份，可静默分叉。经核实客户端无法值导入（dsh-delivery/client 是纯类型出口，产物仅 export {}），故保留客户端副本并新增 tests/delivery-phases.client.spec.ts 漂移护栏，断言两份表相等。已用变异测试确认该护栏有效（改坏客户端表后 2 条断言立即失败），非假防线。

新增测试 11 条（tool-delivery 由 137 → 148）：
- tool-delivery verification inputs：6 条，覆盖三类输入真实生效——req+design 全覆盖可验证、缺 req 项阻断（报出 req/2）、缺 design 决策阻断（报出 design/D2）、未完成项不计覆盖、l0 无清单可验证、仅具体确认可放行（"done" 被拒）。
- tool-delivery l1 mirror path：2 条，走 todo_write 镜像路径完成验证 + 缺 design 决策时阻断。
- tool-delivery l2 scenario coverage：2 条，覆盖完整时放行 + 缺 scenario 时阻断（且不依赖 openspec show）。
- ui-delivery 阶段分组与来源标记 3 条；delivery-phases 漂移护栏 3 条。

另修复一处编辑事故：gateAdvance 的 JSDoc 与函数声明被挤到同一行。

验证：delivery 80 通过、tool-delivery 148 通过、ui-delivery 43 通过，三者类型检查通过。
- [revision 13] D5 补测并修复作用域缺陷；D6 设置界面亲自实现完成。

D5 修复：reviewRoundsSpent 原先只按 text 前缀计数，完全没用事件里已有的 ref.id，导致同一会话 clear 后新建的任务继承上一个任务的已用轮次、开局即无复核预算。改为先由 currentTaskIdFromLog() 从日志回溯当前任务 id，再按 ref.id 过滤。补 3 条测试（日志计数+留痕、跨会话预算独立、新任务不继承旧任务轮次），并用变异测试确认有效（移除 ref.id 过滤后正是那条测试失败）。

D6 设置界面（原指派子代理，其长期未产出任何文件，已 interrupt 并亲自实现）：
- 关键架构发现：子代理报告 ui-delivery 无法值导入 ui-settings-plugins 的 CardForm，此结论经源码核实成立（packages/client/tsdown.client.ts 的 bundle-purity 拒绝未声明 external 的值导入；scripts/verify-client-packages.ts:357 明确禁止功能包声明 runtime external；docs/subsystems/web-client.zh.md 列为硬规则）。正确解法是 D9 所说的"包内分层 + 设置卡片包"，而既有 4 张卡（shell/agent-loop/subagent-model-selection/web-search-deepseek）全部内置在 ui-settings-plugins 自身包内，无任何功能包注册 settings.plugin.item 的先例，故 delivery 卡片也放入该包，符合包注释自述的职责（"the host-plane sections the deployment already exposes"）。
- card-form.ts 新增 booleanField/enumField/listField，CardFieldSpec 增加 control/options 字段（control 缺省即既有文本输入，4 张既有卡片零改动）。
- 新增嵌套字段路径支持（designThreshold.todoCount 等）：读取用 readPath 走点路径；写入按段数分流——单段走 scope.set/unset，多段走 scope.mutate 的 {op,path[],value}（核实 SettingsPathOpView 契约）；override 徽标改为走到父节点后 Object.hasOwn 末段，否则中间对象恒存在会让徽标恒亮。
- fields.tsx 新增 ChoiceField（下拉，含"使用默认值"空项）与 ListField（多行文本，每行一条）。
- 新增 delivery-card-controller.ts 与 DeliveryCard.tsx，覆盖 14 个字段（enforcement/enabled/autoDetect/requireOpenspecForBugs/maxReviewRounds/designThreshold×3/openspecThreshold×2/三个信号词表/postHooks），并在 index.ts 注册 key='delivery' 的 settings.plugin.item 卡片；补 locales 与导出面。

D6 验证（共 11 条新测试）：控件单测（布尔区分 false 与 clear、枚举拒绝非法值、列表过滤空行、嵌套写入走 mutate 而非 set、嵌套 override 徽标）、端到端 apply 测试（delivery namespace 被服务时卡片被派发、face 暴露全部字段）。两处用变异测试确认防线有效（嵌套路径退回标量 set 即失败；移除 ref.id 过滤即失败）。同时更新 2 条既有断言（卡片清单由 4 增为 5）。

回归：ui-settings-plugins 120、delivery 系列与 ui-delivery 合计 397 通过，类型检查通过。
- [revision 14] 收尾：D8.2 常驻入口、D8.3 分级依据可跟踪、D7 包内分层、文档与 Agent Note 全部完成，tasks.md 33/33 勾选。

D8.2 常驻入口：可见性由"默认隐藏 + 仅 Ctrl+Shift+P"改为三态偏好 auto/shown/hidden。auto 在有当前任务时渲染，使进度无需发现快捷键即可见；快捷键改为循环三态；偏好持久化。cardVisible() 为纯函数便于测试。修正了文件头过时注释（原述"Hidden by default"与多读 todos 投影）。

D8.3 分级依据可跟踪：grading.ts 新增 explainGrading() 返回 GradingRationale（level、decidedBy ∈ character-floor|signal-scan|nothing、chars/charFloor、matched{strong,medium,weak} 的全部命中模式、numberedList 标志），命名决定分级的具体证据而非仅给结论。两条创建路径（autoDetect 与 create_delivery_task 的 inferred 分支；显式 level 不记录）在 create 提交后把该依据追加到 .dsh/changes/<task-id>.md，不写任务快照——快照解码器拒绝未知字段，且依据是创建时的一次性事实而非可变任务状态。补 5 条测试覆盖四条判定路径与"报告全部命中而非仅决定项"。

D7 包内分层：验证逻辑（复核轮次计数、清单完整性、覆盖性、产物核验、检查点比较）抽为 src/verification.ts（385 行，8 个导出符号），index.ts 由 1529 降至 1213 行，并清理随之失效的 coverage.ts 导入。导出面与工具契约未变，402 个测试全绿证明零行为变更。

文档：tool-delivery/README.zh.md 修正配置示例与默认值的偏差（300/1200 → 60/200，与代码一致）、更新 autoDetect 语义（l1 也自动创建）、新增"实现验证"整节（三类输入四道检查、covers 键名、l0 豁免、镜像来源）、源码地图补 verification.ts/grading.ts/coverage.ts/openspec.ts 与设置界面指引、已知限制补 enabled 与 enforcement:off 的区别及 l0 豁免理由。docs/subsystems/delivery.zh.md 补 delivery-tasks 投影消费三类事件（含 todo/write 镜像与 source 标记）与服务行为中的策略归属。

Agent Note：新增 .agents/notes/implemented/feature/2026-09-16-delivery-discipline-grading-mirror-and-verification.zh.md，记录五项决策、被推翻的 session/event 镜像机制、备选方案与后果；已通过 verify-agent-note-format。按政策完成 supersession 检查：2026-09-07-delivery-openspec-split 的"用 openspec show --json 作验证点来源"被部分取代，按"部分取代保持活跃并交叉链接"处理，已加双向链接。

另修复一处既有门禁违规：tool-delivery/package.json 缺 @deepseek-ai/dsh-invariants 的 devDependency（经 git stash 在干净树复现确认为既有缺陷），补上后 verify-package-invariants 274 个 companion 全通过。

最终验证：402 个测试通过（delivery 系列 + ui-delivery + ui-settings-plugins，17 个测试文件）；四个改动包 tsc 全部通过；openspec validate --strict 通过；verify-package-invariants、verify-doc-budgets、verify-client-ui-i18n 通过；verify-md-links 无本次相关错误。
- [revision 17] 交付纪律设置界面新增帮助弹窗与字段级说明。

一、帮助弹窗（DeliveryHelp.tsx + DeliveryHelp.module.css）：
照 ui-settings-mcp 的 McpHelp 先例实现，用 ui-primitives 的 Modal（宽 820px、正文可滚，标题与关闭按钮固定）。内容覆盖用户要求的全部五类信息：交付纪律介绍；三档分级的判定条件与语义（特别写明"含 3 条及以上编号项只计 1 个中等信号，单独出现时判为 L1"，以及 L2 改动的是别人依赖的约定）；每级的执行流程与必需记录；会产生的交付物（.dsh/changes、.dsh/design、openspec 四件套，含分级依据本身也写入 .dsh/changes）；实现验证的三类输入与四道检查（含 l0 豁免理由与命令核验不可豁免）；covers 注解的三种键名与可粘贴示例；配置项作用范围与设置服务缺失时的回退。入口为 PluginCard 新增的可选 onHelp，在卡片头部渲染"帮助"链接；其余四张卡片未提供 help，不渲染该链接。

二、字段级说明（问号悬停）：
fields.tsx 抽出共享的 FieldHead（标签 + 问号 + 覆盖徽标/重置），ValueField、ChoiceField、ListField 三处内联 head 统一改用它；SecretField 因 head 含密钥状态徽标而单独加同样的问号。问号用 Tooltip + IconQuestionOutline14，元素为 span（role=img、aria-label 为说明原文）而非 button——它是标签内的悬停亲和元素，不应为当前字段增加第二个 Tab 停靠点；help 未提供时不渲染。FieldProps 新增可选 help。交付纪律 14 个字段全部配说明，每条讲清控制什么、取值如何影响行为、与其他字段的关系，例如 enforcement 三态差异及改动立即生效、enabled 与 enforcement:off 的区别（前者加载期不注册工具，后者仍注册但不阻断）、specChars 为何是最强长度闸门。

三、测试（新增 18 条，全套 411 通过）：
字段问号 3 条（无 help 不渲染、有 help 时 aria-label 正确且元素为 SPAN、三种控件均提供）；DeliveryHelp 3 条（关闭不渲染、各区块与四道检查齐全、footer 可关闭）；DeliveryCard 端到端 3 条（Help 链接打开弹窗、14 字段各带问号且说明非空、控件改动经 edit 暂存）；并更新 2 条既有断言（卡片清单 4→5、tier 名改走 locale）。已用变异测试确认问号测试有效：让 help 分支恒不渲染后 3 条测试立即失败。

四、门禁修复：
verify-client-ui-i18n 正确拦住 DeliveryHelp 中硬编码的 L0/L1/L2，已改为 locale 键 deliveryTierL0Name/L1Name/L2Name（带中文语义描述）。locales.ts 的 PluginsSettingsLocaleKey 声明按文案对象实际 key 重新对齐，消除两处 key 名不一致。

五、文档：ui-settings-plugins/README.zh.md 补"字段说明与帮助入口"一节（含为何用 span 不用 button、为何帮助用对话框而非内联），卡片清单补上 delivery。

最终验证：411 测试通过（18 个测试文件）；tsc 通过；verify-client-ui-i18n 596 文件合规；verify-package-invariants 274 companion 合规；verify-doc-budgets 8 文档合规；verify-md-links 无本包死链。
