- [revision 5] 按用户拍板补充设计决策 D7/D8/D9，并记录一项治理发现。

D7 分级阈值改为中文友好默认值：specChars 代码默认 200 字符对中文过低（本次原始请求即因此被自动判为 l2），与 README 示例 1200 亦不一致；长度降为辅助信号，主判定由信号命中承担，强信号词表中「重构/迁移/优化/替换」等高频词收窄或改为组合命中。

D8 l1 纳入自动创建：判定为 l1 时同样自动创建任务，使交付纪律覆盖所有非小微需求，l0 保持自由执行。

D9 分层边界：不拆为三个独立包，改为 tool-delivery 内按模块分层，另新增注册 settings.plugin.item 中 key 为 delivery 的客户端设置卡片包。理由是三个关注点共享同一份 ResolvedConfig 与工具注册流程，拆包只带来形式边界而引入跨包契约。

治理发现（重要）：openspec/changes/add-delivery-openspec-split/tasks.md 五组任务全部标记 [x]，但其 D3「双进度呈现」要求的"悬浮卡片按阶段显示子任务完成度"在当前代码中不存在。实际落地的是 add-delivery-analysis-progress 的 D3（按语义四组展示），按阶段分组的 done/total 从未实现，delivery-tasks 的 progress 字段客户端一处未用。这既解释了用户反馈的问题 1，也证明当前交付纪律无法保证其声明的完成度与实际代码一致，故 D2 的覆盖性检查必须核验产物而非模型自报状态。
- [revision 6] 实测推翻并纠正 D7 的错误前提：分级误判的主因是编号列表规则，不是长度阈值。

实测（直接调用 grading.ts 的 gradeObjective/scanSignals）：
- 本次原始请求 410 字符，在 specChars 取 200/300/1200 三档下均判定 l2，仅 600 档为 l1。故调高长度阈值不能修正该误判。
- 主因是 scanSignals 中 `numberedItemCount(objective) >= 3 ? 1 : 0` 计入 strong 命中，配合 `hits.strong > 0 return 'l2'`，使任何含 3 条及以上编号项的需求无论多短、阈值多高一律为 l2。实测 26 字符、无任何信号词的 "1、修复登录按钮/2、修复注册按钮/3、修复退出按钮" 即被判 l2。
- 本次请求的信号明细为 strong=1(编号)、medium=0、weak=3(拆分/页面/展示)。

决策修订：将编号列表信号由 strong 降级为 medium，使"3 条编号"表达的可拆分性对应 l1（出设计文档）而非 l2（出 OpenSpec 四件套）；降级后本次请求为 l1。长度阈值须在该规则修正后才成为有效主导因素，其取值连同强信号词表中「重构/迁移/优化/替换」等高频词的收窄，一并列为待定，取决于部署对 l2 成本与留痕完整度的偏好。

同时修正上一轮汇报中的失实表述："改成 1200 即可修正本次误判" 不成立。
