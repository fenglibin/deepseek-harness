- [revision 5] 新增 `packages/client/ui-settings-skills/src/client/SkillImportPreviewDialog.tsx`：独立的只读预览弹窗，左侧 `SkillFileTree`、右侧 `SkillFileEditor`（恒 `editable={false}`），承接文件数/总字节/目标路径元信息行与 `replaces` 警告；宽度取编辑弹窗的 `min(1100px, 92vw)`，底部只有「返回导入」。
- [revision 6] 重写 `ImportSkillDialog.tsx`：表单缩回 `importDialog` 的 680px，底部固定为「取消 | 预览 | 保存」三个按钮（`busy` 由布尔改为 `ImportAction`，只有发起动作的那个按钮显示进度）；预览弹窗叠加在表单之上并复用已解析的 preview；`inspectingRef` 守卫 Escape，使 document 级监听只关最上层弹窗；「保存」= resolve + commit 两步连做，`replaces` 时就地显示 `importReplaces` 且不发起 commit。`SkillsSection.module.css` 新增 `.secondary`（带边框的次级按钮，区别于 `.link`）、`.previewDialog`、`.previewHead`。
- [revision 7] 更新 `locales.ts`：`importCommit`/`importCommitting` 改为 `importSave`/`importSaving`；新增 `importPreviewTitle`（预览 {name}）与 `importPreviewBack`（返回导入）；删掉已无引用的 `importBody`。`importPreviewClose`（关闭）保留给 Modal 的关闭图标标签。
- [revision 8] 更新 `tests/skills-section.client.spec.tsx`：预览改为独立只读弹窗（断言 textarea 的 `readOnly`）、表单在预览打开期间保留输入、重开预览只解析一次、Escape 只关最上层、保存直通写入且不弹预览、`replaces` 时保存被就地拒绝且不调用 commit。移除已不存在的 `importCommit` 引用。新增第 5 条 Escape 用例经负向验证：移除 `inspectingRef` 守卫后该用例失败。
- [revision 9] 文档收尾：更新 `packages/client/ui-settings-skills/README.zh.md` 的导入弹窗描述、源码地图（新增 `SkillImportPreviewDialog.tsx`，并修正既有的 `SkillsTable.tsx` 断链为 `SkillsList.tsx`）与「已知限制」中「导入需要先预览」一条。更新既有 Agent Note `.agents/notes/implemented/feature/2026-09-15-skill-disable-parking-directory.zh.md`（该 note 已拥有技能设置页呈现这一决策域，按规则更新而非新建）：标题、问题陈述追加第四处缺口、新增「导入的预览与保存是两个动作」小节、补两条曾考虑的替代方案，并同步后果段。supersession 检查：`2026-09-14-skill-management-plugin.zh.md` 拥有的是 Host 侧两阶段契约，本次未改动该契约，无需归档。
- [revision 12] 修复验收 post-hook 缺陷（`packages/delivery/tool-delivery/src/index.ts:1263`）：原条件 `changeId === undefined` 只挡住「没有清单」，而 l0/l1 任务的 `record_tasks` 记录的是**空字符串**，于是拼出 `openspec validate  --strict --json`（目标丢失），openspec 报「Nothing to validate」并必然非零退出，使所有 l0/l1 任务无法到达 accepted。改为 `changeId === undefined || !isValidChangeId(changeId)`：空白与不合法的 id 一律按「没有变更可校验」处理，不插入该命令；l2 任务（记录时已强制 kebab-case）行为不变。该守卫同时堵住一条命令注入路径——`record_tasks` 只在 l2 任务上校验 change_id（`index.ts:1116`），l0/l1 可记录任意字符串，而它会被拼进 shell 命令。测试：`StubShell` 改为像真实 CLI 一样对无目标 validate 返回非零（否则缺陷不可观测），并新增两条用例（非 l2 任务携带空 changeId 可验收且不产生无目标命令、l2 任务仍产生 `openspec validate add-thing --strict --json`）；经负向验证，回退修复后前者如预期失败。
- [revision 13] 文档：`packages/delivery/tool-delivery/README.zh.md` 补上验收前定向校验的契约（l2 任务先跑 `openspec validate <change_id> --strict --json`，非 l2 任务没有变更故不跑该命令）。`.agents/notes/implemented/architecture/2026-09-03-delivery-discipline-post-hooks.zh.md` 补第三条决策与一条备选方案（该 note 拥有「验收前执行哪些命令」这一决策；supersession 检查确认无重复持有者）。验证：`packages/delivery` 三包 209 测试通过（提权后，3 个 sandbox 用例需在 home 建临时目录，被 workspace-write 沙箱以 EPERM 拒绝，与本次改动无关）；`tsc -b tsconfig.host.json` 通过；agent-note-format / md-links / md-wrap 门禁对本 note 与 README 无违规。
