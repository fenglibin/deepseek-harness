- [revision 6] dsh-llm 新增 collectImageRefs 与 attachImageDescriptions（递归、按出现顺序、不 mutate），content.spec 补 5 个用例；agent-loop 的 appendToolResult 变 async 并在落盘前经 describeForRoute 描述图片，executeToolCalls 新增 inputModalities 参数并贯穿 runGroup 与 appendSkippedToolCall，agent.ts 传 preparedCall.inputModalities，package.json 加 image-understanding peer/dev 依赖、tsconfig 加 reference；MockAdapter 增加可选 inputModalities；tool-calls.spec 补 3 个用例。typecheck、lint、verify-changed（240 文件 3855 测试）全绿。
- [revision 10] 深度自检修复（P3 范围）：(B) 本地回显与 durable 用户气泡按 composer 部分列表交错渲染——`PendingSubmission.parts` 贯穿 beginSubmission → sendSession(`submissionPartsOf`) → `MessageItem`(`contentRuns`/`partsRuns`)，使图片渲染在其周围文字之间而非一律前置，并同步更新 api-catalog 与 chat-view 测试 fixture；(C) 3 个 apps/web e2e 测试从已退役的附件栏断言迁到 inline chip 断言（command-image-envelope / image-display / submission-echo）；(D) SDK server `durablePromptContent` 现在对已 durable 的 attachment-ref 图片块（非 inline 编码）在 text-only 路由下同样调用 `describeForRoute` 生成描述，并新增 server.spec 覆盖。
