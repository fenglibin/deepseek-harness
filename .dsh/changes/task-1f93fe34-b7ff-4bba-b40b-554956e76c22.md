- [revision 5] 实现「先读后写由工具在变更前自动完成」。

源码：
- packages/fs/tool-fs/src/error.ts：新增 mutateWithObservedBasis（意图分发 + 提供方变更的包裹；捕获 FS_NOT_OBSERVED 后经 observeTarget 记录观察并重新分发意图槽位）、observeTarget（普通文件→读取并记 present；缺失→记 absent；其他→unusable 且不改判）、isUnobservedRefusal。recoverMutationFailure 收窄为只处理仍会失败的防护变更（陈旧/缺失），并在发现缺失时记录 absent 以便后续 write 走防护创建。
- packages/fs/tool-fs/src/write.ts、edit.ts：接入 mutateWithObservedBasis；系统提示词改为「工具自行结算观察，不要仅为满足它而读文件」，保留「edit 需内容才能写出匹配 old_string」。
- packages/fs/tool-str-replace-editor/src/index.ts：str_replace 与 insert 改为先读取并发出 fs/observed、再分发意图槽位（insert 分支此前完全不做读取）。

测试：
- tool-fs/tests/error.spec.ts、integration.spec.ts：未读 write/edit 断言由「必须失败」改为「不产生 isError 且无需任何 read 调用」；新增缺失目标（edit→FS_NOT_FOUND、write→照常创建）、外部删除后重建、目录回落等用例。
- tool-str-replace-editor/tests/tools.spec.ts：未 view 的 str_replace 直接成功；字面量不匹配仍 FS_EDIT_NOT_FOUND；外部改动按刚读到的内容应用。
- fs 分组 580 passed / 1 skipped；两个包 tsc 干净。

快照与文档：
- snapshots：29 个 system-prompt.expected.md 同步提示词；fs-policy-reject/session.jsonl 刷新。
- README：tool-fs、fs-observation-policy、tool-str-replace-editor 的策略/失败恢复/源码地图/提示词章节。
- Agent Note：新增 2026-09-15-observation-settled-before-mutation.zh.md；在 2026-09-15-in-call-reread-on-guarded-mutation-refusal.zh.md 头部标注被取代并说明仍准确的部分。

产物面（关键）：
- 此前两轮改动从未进入运行中的 GUI：dsh 经包 exports 解析到 lib/index.js，而 lib 构建于本轮之前。已执行 pnpm run build:lib:host 重建，并用纯 node（与 dsh 相同解析路径）从 lib/index.js 实际加载验证：未读 edit 与未读 write 均 isError=false 且落盘，edit 缺失目标为 FS_NOT_FOUND。
- [revision 12] 需求确认与诊断证据（补记）：

用户原始需求：编辑时页面不要出现红色报错；并询问"写入之前先读取文件"这条规则能否修改。

诊断结论（基于用户自己的会话日志 ~/.dsh/sessions/<cwd>/<session>/session.jsonl.zstd）：
- 本会话工具错误码统计中 FS_NOT_OBSERVED 出现次数最多，样例文本为 'Error: edit requires reading "<path>" first'。
- 用户描述的"红字出现后消失"是错误码指纹：只有 RecoveredMutationProjector 做条件式隐藏（要求同一文件后续变更成功），其集合恰为 FS_NOT_OBSERVED / FS_STALE_VERSION；FS_EDIT_NOT_FOUND / FS_AMBIGUOUS_EDIT 由 HiddenToolFailureProjector 无条件隐藏，从不会"消失"。故用户所见为 FS_NOT_OBSERVED，本轮修复方向正确。

纠正此前两处错误结论（不得据以行动）：
1. 曾判定 GUI 加载 lib/ 产物、需重建。错误：GUI 经 run.sh → pnpm dsh → 'node --import tsx/esm apps/cli/src/bin.ts' 启动，tsx 按 tsconfig.base.json 的 paths 把 dsh-tool-fs 解析到 src/index.ts。已实测 import.meta.resolve 在该启动路径下返回 src/index.ts。lib 重建无害但非必需。
2. 曾判定用户所见为 FS_EDIT_NOT_FOUND、本轮修错了问题。错误，源自向用户提出的一个以错误码文字为选项的问题，用户无法据以回答。正确结论以会话日志与隐藏机制为准。

生效条件：tsx 仅在进程启动时加载模块，run.sh 无 watch/HMR，因此运行中的 GUI 进程仍持旧代码；需用户重启（./stop.sh && ./run.sh）后生效。
- [revision 16] coverage confirmation: 全部设计记录（D1–D9）与 OpenSpec 记录均已实现并验证：变更前结算观察（D1/D2/D3）、缺失目标交策略作答（D4）、str_replace_editor 同规则（D5）、提示词同步（D6）、策略规则不变（D7），以及 D8/D9 两处错误结论的更正记录。代码、测试、README、Agent Note 与快照均已落地，两个包 tsc 干净、fs 分组 580 passed/1 skipped、与 GUI 同路径实测未读 edit 无 isError。已知未实现项已在记录中明确声明：FS_STALE_VERSION 与字面量不匹配错误（FS_EDIT_NOT_FOUND/FS_AMBIGUOUS_EDIT）仍会失败，后者由他人并行进行的客户端 HiddenToolFailureProjector 无条件隐藏，不属本次范围；快照 replay 的 delivery/change 时间戳不稳定来自他人未提交改动。
- [revision 18] 改动面复核（用户要求"再次验证本次修改的文件有没有多修改的"）的结果与两处修正：

一、逐文件审查结论——无越界改动
1. packages/fs/tool-fs/src/edit.ts（+12/-9）：仅两处——import 增加 mutateWithObservedBasis；意图分发+变更改为经 mutateWithObservedBasis 包裹，并同步注释与提示词。
2. packages/fs/tool-fs/src/write.ts（同形态）：同上，无其他改动。
3. packages/fs/tool-str-replace-editor/src/index.ts：仅 replaceInFile 与 insertInFile 两处，各自把「读取+发出 fs/observed」移到 intent 分发之前，并加注释。
4. README 三个文件：fs-observation-policy 10 行、tool-fs 14 行、tool-str-replace-editor 4 行，集中在「策略与沙箱行为 / 失败与恢复 / 源码地图 / 提示词逐字引用」四类章节，无越界。
5. snapshots：29 个 system-prompt.expected.md 的改动去重后只有那两句提示词的旧→新替换，另加 fs-policy-reject/session.jsonl 的两条 tool/result 语义变化。无其他内容。
6. 我改的 2026-09-15-in-call-reread-on-guarded-mutation-refusal.zh.md：仅头部新增被取代标注，正文未动。
7. 归属澄清：工作区中 2026-09-04-chat-ux-recovered-mutation-errors-hidden.zh.md、2026-09-16-unactionable-edit-failure-rows-hidden.zh.md、2026-09-16-workspace-file-browser.zh.md 及 hidden-tool-failure.ts、ui-workspace、api/file-browser 等改动来自并行工作，非本次产出（其文件内不含本次任何符号）。未提交的同名清单里 2026-09-04 note 的 mtime 为 20:33，早于本次改动起点 20:40。
8. lib/ 已被 .gitignore 忽略，重建不进入改动面。

二、复核发现并修正的两处问题
1. isUnobservedRefusal 原被 export，但全仓库仅 error.ts 内部一处调用——属不必要的公开面。已收窄为模块内私有函数（不再 export）。lib/types 中的旧声明属构建产物，重建后消失。
2. 本轮核心函数 mutateWithObservedBasis 与 observeTarget 缺少单元测试：error.spec.ts 此前只覆盖第一轮的 recoverMutationFailure。已补 5 个用例（结算后重新分发并变更、首次成功时不读取也不重分发、缺失目标记录 absent 后由策略作答、非观测类失败直接透传且不读取、非普通文件保留原拒绝）。

三、变异验证（证明新增测试有效，非恒真）
- 变异 1「补读后不再重新分发意图」→ 2 个用例变红；
- 变异 2「缺失目标不记录 absent」→ 1 个用例变红；
- 恢复源码后 16 个用例全绿，且 diff 确认源码与备份一致。

四、最终验证
- 两个包 tsc 干净；verify-export-jsdoc 对 tool-fs 无违规；
- packages/fs 分组 585 passed / 1 skipped（较修正前多 5 个新增用例）；
- 与 GUI 相同解析路径（node --import tsx/esm → src/index.ts）端到端：未读 edit/write 均 isError=false 且落盘，edit 缺失目标为 FS_NOT_FOUND。
