- graded l2 by character-floor (1013 chars vs floor 200; objective exceeds the length floor)
- [revision 10] ## D2 越界读取单独成码

- `packages/fs/fs/src/types.ts`：`FsErrorCode` 新增 `FS_OFFSET_OUT_OF_RANGE`，附 JSDoc 说明它与 `FS_NOT_FOUND` 的补救方向相反（越界是请求方错误，目标缺失是关于文件树的事实）。
- `packages/fs/tool-fs/src/read-render.ts`：`finish()` 的越界分支改抛新码；`buildWindow` 的 JSDoc 同步。
- 测试：`tests/read-render.spec.ts` 越界断言改用新码，并新增「空文件 offset 1 不报越界」；`tests/tools.spec.ts` 新增工具层测试，断言越界返回 `FS_OFFSET_OUT_OF_RANGE`、目标缺失仍返回 `FS_NOT_FOUND`。

## D1 反转可见性判定

- `packages/client/ui-chat/src/client/conversation-nodes/hidden-tool-failure.ts`：判定由「黑名单隐藏」反转为「可处置白名单展示」。白名单为 `FS_SANDBOX_DENIED`、`FS_PERMISSION_DENIED`、`NO_PROVIDER`、`GOAL_TOOL_AUTHORITY_REQUIRED`、`SEARCH_FAILED`；其余已结算顶层 `isError` 调用标记 `visibility: 'hidden'`，含无 `error.code` 者。类与 JSDoc 改写为陈述新方向与白名单制理由。

## 删除被架空的 RecoveredMutationProjector

- 删除 `conversation-nodes/recovered-mutation.ts`，并从 `chat-snapshot-builder.ts` 的 `replace` / `apply` 两条装配链与 import 中移除。其两个错误码在新判定下已无条件隐藏，该投影不再改变任何输出。

## 测试改造

- `tests/conversation-node-definitions.client.spec.ts`：新增「模型自纠失败无论何码都隐藏」（含已删除的历史码 `DELIVERY_POST_HOOK_FAILED`）与「可处置五码保持可见」两条；无 `error.code` 的失败断言由 visible 改为 hidden；原「非可恢复失败保持可见」用例改用白名单内的 `FS_PERMISSION_DENIED` 保留原意；原两条「条件式恢复」用例合并为无条件隐藏并保留成功调用不受影响的覆盖；`code: 'failed'` 的 key 稳定性用例改用 `FS_PERMISSION_DENIED` 以免可见性判定干扰其原意。

## 文档

- 新增 `.agents/notes/implemented/feature/2026-09-18-tool-failure-visibility-by-actionability.zh.md`。
- `packages/client/ui-chat/README.zh.md`：重写「不可操作的工具失败行」章节为白名单制描述。
- `packages/fs/fs/README.zh.md` 与 `packages/fs/tool-fs/README.zh.md`：登记新错误码语义。

## 证据

- `npx vitest run packages/client/ui-chat/ packages/fs/tool-fs/ packages/fs/fs/`：34 个文件、551 条通过。
- `npx tsc -b packages/client/ui-chat/tsconfig.json` 与 `packages/fs/tool-fs/tsconfig.json` 通过（后者连带重建 `dsh-fs` 类型产物）。
- `verify-agent-note-format` 与 `verify-md-links` 在本次新增/改动文件上无 violation（报出的条目均为既有文件）。
- [revision 15] acceptance: deep-selfcheck: 按 /deep-selfcheck 的四步执行完毕。查了什么：逐条核对原始 objective 的四个示例与两条约束；用 140 个真实会话、29,038 次 tool/result（492 次失败）重放新判定；真实运行测试而非只看代码。发现并修复了一个真实缺口：`sandbox escalation to "<mode>" requires approval, but no approval channel is available` 在 `packages/sandbox/sandbox/src/escalation.ts:206` 抛裸 `Error`，不带 error.code，因此按「无码默认隐藏」规则会被隐藏——但它的处置方是用户（需配置审批通道），与「必须用户参与才展示」的要求直接冲突。修法：该文件新增导出 `SANDBOX_APPROVAL_UNAVAILABLE`（覆盖三种审批路径不可用状态：未组装审批服务、调用无 agent 可路由、无可用应答者），`index.ts` 同步导出，客户端白名单纳入该码，并在 `escalation.spec.ts` 补测试断言三种状态携带该码、而 rejected/cancelled 不携带（用户对自己那次询问的答复属噪音，与部署事实有本质区别，该区分只有按码才能表达）。同时清理了 lib/ 中已删除文件的陈旧产物。复核证据：修复后 vitest 跑 packages/sandbox/sandbox/ + client/ui-chat/ + fs/tool-fs/ + fs/fs/ 共 37 文件 570 条全通过；真实数据重放得到 486 次隐藏 / 6 次可见（1.2%），与设计预期精确吻合。剩余风险三项，均为已知且有依据的边界而非未处理缺陷：(1) 白名单需随能力演进维护，遗漏的后果从「多一行噪音」变成「少一行必要信息」——缓解事实是这类失败另有审批面板与 turn 终态行承担呈现；(2) core/tools 的 serviceAsk 通用审批路径产生同文本却无码，经核实该分支实测 0 次且能显示页面的部署必定组装了审批服务，headless 部署没有读者在场，故有意不加码；(3) PTC 嵌套子调用因 tool/code-dispatch 不带 {name, code} 仍不覆盖，实测该事件在本部署为 0 次，沿用既有延后结论。另有一个既有失败（ui-theme/scrollbar-styles 指向 ui-settings-plugins/DeliveryHelp.module.css）已核实与本次改动无关：该 CSS 与测试文件均未被本次触及，且 bg-layer-2 在 HEAD 中已存在。
- [revision 16] coverage review: 本次实现的记录与验证情况（含深度自检后的更新）：

**设计记录覆盖**：`.dsh/design/task-05a3b01f...md` 的 D1（反转可见性判定）、D2（拆出精确码）、D3（不引入 turn 终态兜底与阈值汇总）、D4（不动持久化格式）逐条落地；`openspec/changes/update-tool-failure-row-visibility/design.md` 的 D1–D4 与之编号一致；`docs/plans/2026-09-18-...-design.md` 为完整设计稿。

**spec delta 覆盖**：`openspec validate --strict` 通过。两个 Requirement 的六个 Scenario 全部被已完成的清单项认领——「模型自纠失败默认隐藏」「无错误码的失败默认隐藏」「成功的调用不受影响」由 2.1/2.2/3.1–3.4 覆盖；「需用户介入的失败保持可见」与新增的「用户自己拒绝的提权不保持可见」由 5.4 及 2.1/3.1 覆盖；「越界错误单独成码」由 1.1/1.2/1.3 覆盖。

**深度自检的修复（新增）**：`/deep-selfcheck` 发现 `sandbox escalation ... no approval channel is available` 抛裸 `Error` 无码而会被误隐藏，但它需要读者配置审批通道。已在 `packages/sandbox/sandbox/src/escalation.ts` 新增 `SANDBOX_APPROVAL_UNAVAILABLE`（覆盖三种审批路径不可用状态），`index.ts` 导出、客户端白名单纳入、`escalation.spec.ts` 补测试固定该码并断言 rejected/cancelled 不携带它。该修复已同步到 spec delta 的新 Scenario、tasks.md 的 5.4/5.5、Agent Note 与 ui-chat README。

**此前经用户确认的实现**：删除 `RecoveredMutationProjector`（其错误码已无条件隐藏，投影不再改变输出）；按 Agent Note 规范合并两份被完全取代的记录，其独有依据已搬入新 Note 后删除原文件。

**验证证据**：37 个文件、570 条测试通过；`tsc -b` 通过；真实 492 次失败重放得 486 隐藏 / 6 可见（1.2%）。
- [revision 18] coverage confirmation: 每条已记录的设计与 spec 均已实现，逐条确认：

**设计记录（4 条，全部实现）**
- D1 反转可见性判定：`hidden-tool-failure.ts` 已改为「可处置白名单展示」，白名单六码（含自检新增的 `SANDBOX_APPROVAL_UNAVAILABLE`）保持可见，其余 `isError` 与无码失败默认 hidden。
- D2 拆出精确码：`FsErrorCode` 新增 `FS_OFFSET_OUT_OF_RANGE`，`read-render.ts:97` 改抛该码，`FS_NOT_FOUND` 保留给文件不存在。
- D3 不引入 turn 终态兜底与阈值汇总：未引入任何兜底机制，依据是 `turn-error` 与 `ui-approval` 已承担呈现。
- D4 不动持久化格式：`ToolErrorInfo` 与事件格式未改动，无需重录快照。

**spec delta（2 个 Requirement / 6 个 Scenario，全部实现且经测试固定）**
- 「模型自纠失败默认隐藏」→ 测试覆盖 `FS_STALE_VERSION`/`FS_NOT_OBSERVED`/`DELIVERY_GATE_BLOCKED`/`FS_EDIT_NOT_FOUND` 及历史码。
- 「无错误码的失败默认隐藏」→ 测试覆盖裸 `Error` 失败。
- 「需用户介入的失败保持可见」→ 测试覆盖白名单六码。
- 「用户自己拒绝的提权不保持可见」→ 自检新增 Scenario，`escalation.spec.ts` 断言 rejected/cancelled 不携带该码。
- 「成功的调用不受影响」→ 测试覆盖 `isError: false`。
- 「越界错误单独成码」→ `read-render.spec.ts` 与 `tools.spec.ts` 各自断言越界与新码、缺失与旧码。

**额外实现（经用户确认，已记录）**：删除 `RecoveredMutationProjector`；按 Agent Note 规范合并两份被完全取代的记录。

**验证证据**：`openspec validate --strict` 通过；37 文件 570 条测试通过；`tsc -b` 通过；真实 492 次失败重放得 486 隐藏 / 6 可见。
