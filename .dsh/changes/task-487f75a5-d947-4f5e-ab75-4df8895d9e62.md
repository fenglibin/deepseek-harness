- [revision 10] 新增 packages/client/ui-session-changes/src/client/accept-store.ts：用 defineStore 声明会话作用域的接受记录 store，状态为 Record<path, seq>，声明 accept/acceptMany 写入集，persist 键 dsh.session-changes.accepted（框架按会话 id 加后缀，会话消亡时清键）。导出 AcceptedChanges 类型与 ACCEPTED_CHANGES_PERSIST_KEY。
- [revision 11] SessionChangesDock.tsx 改造：适配器改从 props.useStore 读接受记录、经 props.actions 写入，删除组件本地 useState 接受集（原 :298），修掉"重挂载后全部已接受文件复活"的根因；新增 isPendingChange 单一谓词（pendingChanges 与行内接受控件共用，避免规则重复），保留 pendingChanges 语义不变。SessionChangesPanelProps 增加视图状态，面板新增「当前变更 / 全部变更」tab；全部视图列出全部文件并把已接受行标记为「已接受」且不再提供接受按钮；去掉 pending.length === 0 时的整块 return null，改为仅在 changes.length === 0（本会话从未变更）时不渲染，使全部接受后仍可进入全部视图。SessionChangesDockProps 增加 PropsStore 份额。
- [revision 12] 注册与文案：index.ts 的 dock 注册声明 store: acceptStore（createAcceptedChangesStore() 在 apply 内构造，满足"生产代码不在 apply 外调用工厂"的规则），并导出 store 工厂与类型；locales.ts 新增 summaryAll/accepted/view.pending/view.all 四个键；SessionChangesDock.module.css 新增 .views/.viewTab/.accepted 样式；package.json 声明 @deepseek-ai/dsh-client-store 开发依赖，tsconfig.json 增加 ../store 引用。
- [revision 13] 测试：改写 tests/session-changes-dock.client.spec.tsx 与 tests/e2e-business.client.spec.tsx 的 dock 道具为真实 store seat（bindSnapshotSelector 绑定真实实例，测试驱动 shipped 的 persist 键与写入集），两个文件各自 beforeEach 清 localStorage（store 持久化会跨用例污染）。改写原「shows every change again after the dock remounts」（原 :539，固化了本次要修的缺陷）为「keeps every accept when the dock remounts」；新增跨页面刷新保持（同一 handle 重建实例）、会话间独立、全部视图含已接受文件且已接受行不再提供接受按钮、全部接受后 strip 仍可达、从未变更时不渲染、isPendingChange 单一谓词用例。结果：53 项通过；typecheck 干净；oxlint 0 警告 0 错误；verify-client-packages 与 verify-client-ui-i18n 通过。
- [revision 15] 收尾文档：packages/client/ui-session-changes/README.zh.md 改写接受语义（接受只从当前变更隐藏、已接受文件仍留在全部变更并标记）、新增「当前变更 / 全部变更」章节、实现说明改为 store 归属与两视图派生、已知限制把「无跨会话持久化」替换为「接受记录按绝对路径持久化（localStorage 键 dsh.session-changes.accepted.<会话 id>）」与「seq 回退即视为已接受」。新增 Agent Note .agents/notes/implemented/feature/2026-09-17-session-changes-accept-store-and-views.zh.md（full 档：Problem/Decision/Alternatives considered/Consequences/Testing/Deferred/Related）；就地更正前身 2026-09-14-session-changed-files-whole-log.zh.md 中被取代的 D6「接受状态保持组件本地」、Consequences 的「随页面刷新重置」、Alternatives 的持久化否决理由、Testing 的「重新挂载后全部变更重新待处理」与 Deferred 的「接受状态不跨刷新」，并双向交叉链接。OpenSpec 侧以 REMOVED 声明取代前身 change 的「接受状态不跨页面刷新保留」要求。覆盖率：该包 src/** 8 个文件行覆盖 100%。
