- [revision 10] 新增 `packages/client/ui-file-browser/src/client/preview.ts`：`previewKindOfPath` 按扩展名判定预览类型（`.md`/`.markdown` → markdown，`.html`/`.htm` → html，其余 undefined），与既有 `languageOfPath` 并列且同样做 own-property 检查。新增 `ContentPreview.tsx` 与 `ContentPreview.module.css`：markdown 臂经 `MarkdownText` 渲染并开启 `diagrams`（mermaid 流程图），html 臂用 `sandbox="allow-scripts"` 的 `srcDoc` iframe。`CodeEditor.tsx` 增加预览复选框与两视图切换（共用同一缓冲区，切换重置发生在打开另一个文件时），预览态隐藏保存/重新加载；`CodeEditor.module.css` 工具栏改为 dense 行并新增 `.toggle`；`FileBrowserModal.module.css` 把搜索框收到 26px 高、13px 字号、max-width 260px；`FileBrowserModal.tsx` 把预览相关文案透传给 `CodeEditorLabels`；`locales.ts` 新增 6 条文案。新增 `tests/preview.client.spec.tsx`（26 项）：类型判定、复选框显隐、切换与缓冲区保留、GFM 表格、mermaid 流程图、KaTeX、iframe 隔离属性、只读形态同样可预览。README 与 Agent Note 同步更新。验证：`vitest run packages/client/ui-file-browser` 99 项通过；`tsc -b` 该包与 `tsc -p tsconfig.client.json` 通过；`oxlint` 0 错；相关消费方 1029 项通过；两个新文件覆盖率 100%。
- [revision 13] coverage confirmation: 记录的设计与规格全部实现并已锚定到代码：D1（尺寸调整只在 ui-file-browser 作用域内：FileBrowserModal.module.css 的 .search 覆盖 26px/13px，CodeEditor 两个按钮改 size="sm" 12px）对应「搜索框与内容区动作的尺寸与相邻文字一致」的 2 个 Scenario；D2/D3（预览落在 CodeEditor、切换而非分栏、共用缓冲区）对应「预览是内容区的切换视图」的 3 个 Scenario，由 preview.client.spec.tsx 的切换与缓冲区保留用例覆盖；D4（previewKindOfPath）对应「内容区的预览类型由扩展名决定」的 3 个 Scenario，覆盖 .md/.markdown/.html/.htm 与其余文件的显隐；D5（MarkdownText 管线）对应「Markdown 预览支持完整语法」的 2 个 Scenario，由真实管线的 GFM 表格、mermaid 流程图与 KaTeX 断言覆盖；D6（srcDoc + sandbox="allow-scripts"）对应「HTML 预览在隔离源中运行脚本」的 2 个 Scenario，断言 iframe 的 sandbox 与 srcdoc 属性。MODIFIED 的「会话文件链接在 Web 端只读查看」第三个 Scenario（只读形态同样可预览）由 read-only preview 三个用例覆盖，并已在实现侧确认两处入口共用同一个 FileBrowserModal 内容区与同一个 CodeEditor。设计中的 4 条任务清单项全部完成。未覆盖项：无——但有一条证据边界需说明，GUI 需要 launch token 鉴权，我未读取该凭据，因此未做真实浏览器截图；行为证据来自渲染真实 MarkdownText 管线的单测，以及已重建并确认含新代码（allow-scripts、max-width:260px）的 lib/client.js。
- [revision 15] 深度自检后的修正（用真实 Chromium 验证渲染结果，而非只跑测试）：

1. 修复真实缺陷——深色主题下 HTML 预览不可读。我最初的 `ContentPreview.module.css` 给 iframe 设了 `background: var(--dsw-alias-bg-layer-1)`；深色主题下它是 rgb(35,35,36)，而产出 HTML 通常不声明颜色、UA 样式表给黑字，于是黑字压在深灰底上。实测截图可见，已改为 `background: rgb(255 255 255)`（文档自己的底色）。真实服务器提供的 bundle 已确认含 `.WeDnuW_frame{background:#fff...}`。

2. 回退了一处针对不存在问题的多余复杂度。我一度把预览开关改成 path-keyed 状态以消除「切换文件时闪现上一文件预览」，但探针证明 `FileBrowserModal.openFile` 先 `setFile(undefined)`，`CodeEditor` 本就重新挂载，两种实现行为完全等价，且那个逐帧测试无法区分两者。已回退为朴素的 `useState` + `useEffect` 重置，并删除无证据价值的测试。

3. 新增 `tests/content-preview-styles.client.spec.ts`（3 项），把两处用户可见修复锁进样式断言：HTML 框架必须是文档白底而非主题表面（并显式拒绝 `background: var(...)` 回归）、搜索框 26px/13px/260px、预览开关 12px。每条断言都做过反向验证：回退实现后对应测试确实失败。

4. 在 `preview.client.spec.tsx` 补充写动作按钮的 `sm` 尺寸断言（12px，与「已保存」一致）。

真实浏览器证据（隔离实例 http://127.0.0.1:3987，`DSH_HOME` 指向工作区内目录，未触碰运行中的 3080 实例）：
- `sandbox="allow-scripts"` 语义实测：脚本运行、`localStorage`/`document.cookie` 抛 SecurityError、`window.origin === 'null'`、宿主读不到沙箱 iframe 的 DOM。
- 宿主 `html{color-scheme:dark}` 不会继承进 srcdoc 文档（`docRootScheme` 为 `normal`、默认文字色恒为黑），这是底色必须固定为白的原因。
- iframe 在 flex 列中填满剩余高度（900×568，长文档内部滚动）。
- 真实服务器加载了 file-browser bundle 且页面无 JS 错误。

验证：`vitest run packages/client/ui-file-browser` 103 项通过；ui-primitives/ui-chat/ui-workspace 1061 项通过；`tsc -b` 该包与 `tsc -p tsconfig.client.json` 通过；`oxlint` 0 错；i18n 门禁 5 项通过；Agent Note 格式/链接/换行门禁干净。临时探针脚本、隔离实例与 `.dsh-verify/` 已全部清理。
