- graded l2 by character-floor (221 chars vs floor 200; no signal matched)
- [revision 10] 实现完成。改动面四个包，全部经真实测试与类型检查确证。

D1 文案归因：RevisionDiffPanel 改用新键 diffFailed（「查看变更失败：{message}」），revertFailed 留给 SessionChangesDock 的撤销路径。locale 新增 diffFailed/diff.noRecord/diff.noWorkspace 三键。

D2 错误码分派：新增 RevisionError 承载 RemoteFailure 的 code 与 message（index.ts 的两个 verb 都改为抛它），面板经 revisionFailureText(cause, t, fallbackKey) 按码选文案——unknown-path 与 no-workspace 各得一句可读说明，其它码保留宿主原始诊断。dock 的 revertAll 失败路径同样走该函数。

D3 持久化：新增 packages/fs/session-file-revisions/src/spec.ts，声明 domain session_file_revisions（per-record，表 sessions 以 SessionId 为键），记录含身份（createdAt/cwd/parentSession）与修订数组，并提供 toStoredRevision/fromStoredRevision 作为跨越 JSON 边界（SessionId brand 消失）的唯一转换点。服务 init 时 open domain 并把全部记录 seed 进内存（含 rebuildTree 重算子树链接），list 随后同步读；identityHolds 按 createdAt 校验恢复记录与当前会话头是否同一生命周期，不符则 retire。捕获监听器与 forget/dropPath 都在发起处 await 写盘；同会话写入串到该会话自己的链上，settled 链尾即时清理使该 map 有界。maxRecordBytes 进 Config（默认 4 MiB），超限删除记录并记 warning，绝不截断。

D4 按行能力：RevisionRemote 新增 list 动词；dock 用 useEffect 拉取记录路径集合并保存为 Set，SessionChangesPanel 新增 hasRevision(path) 谓词，只有 onViewDiff 存在且该路径有记录时才渲染「查看变更」按钮；revertAll 成功后重新拉取。

D5 str_replace_editor：output schema 由纯字符串改为 oneOf 两分支（改动类 path/text/before/after/operation，只读类 path/text），render 返回 value.text 使模型可见文本逐字不变；三个 mutator 返回 EditorMutationValue，其中 before 取工具自己读到的内容而非后端缓冲基准（后者对超大覆写会拒绝缓冲）。捕获侧 MUTATION_TOOLS 纳入该工具，并把「edit 无 before 即忽略」的规则显式写成 name === 'edit'（原为 name !== 'write'），使新工具能走 create/update 区分。

D6 撤销清理：新增导出 retiresRevision(status) 作为唯一规则（仅 reverted 退休），revert 对其为真的路径 await dropPath；conflict/missing/unchanged 保留记录。

实现中被测试抓住并修复的一个真实缺陷：KvTable.delete 从域的内存状态判定，键看似不存在就跳过介质写入，因此在 put 仍排队时发出的 delete 什么也不做、随后被排队的 put 复活；且 teardown 只排空一次链尾就关闭域，晚加入的任务会被正在关闭的介质以 DomainError: closed 拒绝而静默丢失。修法是写入在发起处 await 加上同会话串行化，teardown 排空仅作安全网（该缺陷由「撤销后跨重启不再提供该路径」用例确证，探针输出显示 delete FAILED ... domain is closed）。

测试：packages/fs/session-file-revisions 49（新增持久化往返、身份不符丢弃、超限不写、按状态清理、编辑器改动经真实 tools/post-execute 捕获、只读调用不产生记录）；packages/api/session-file-revisions 13（新增 retiresRevision 四状态）；packages/client/ui-session-changes 76（新增按行能力、三种错误码文案、refetch、以及「失败的 diff 不得呈现为失败的撤销」）；packages/fs/tool-str-replace-editor 16 原样通过（证明渲染文本逐字未变）。受影响范围 verify-changed 全绿：43 个文件 682 通过 1 跳过。

文档：三个包 README 同步（修订包的持久化与失败恢复、API 包的撤销清理规则与错误码契约、客户端包的按行能力与错误归因、编辑器包的结果值形状与可撤销性）；Agent Note .agents/notes/implemented/architecture/2026-09-17-session-revision-persistence.zh.md；设计草案 docs/design/session-revision-persistence.zh.md；OpenSpec 变更 update-session-revision-persistence（validate --strict 通过）。

门禁：verify-cordis-config 159 通过、verify-package-readme-limitations、verify-package-readme-model-experience、verify-package-invariants、verify-client-ui-i18n、verify-md-links、verify-md-wrap 在我的改动面全部干净；verify-agent-note-format 对新 Note 无违规（其余两条违规属未改动的既有文件）。三个宿主包与客户端包 tsc 全通过。
- [revision 13] 验证阶段补充：在真实组装产物中确证改动生效，并澄清 web 泳道的失败归因。

1) 我的新文案与错误码分派已进入构建产物：packages/client/ui-session-changes/lib/client.js 含「查看变更失败：{message}」的 diffFailed 键；revertFailed 仅剩两处（撤销路径的调用与字典定义），面板不再误用；RevisionError 出现 8 次、revisionFailureText 出现 5 次，证明错误码保留与按码分派确实编译进产物。

2) 组装验证：完整 pnpm run build 通过（含前端 dist 与 .dsh-build/client-build-environment.json 构建记录），随后 DSH_SNAPSHOT=replay 运行 apps/web/tests/built-boot.expected.e2e.ts 的「mounts every slot entry without abdicating one」通过——这条正是若 dock 注册破坏装配树就会失败的用例（它断言不含任何 [data-slot-error]），因此我的客户端改动在真实组装中健康。

3) web 泳道首次运行（10:42）报 135 失败，但该次运行使用的是 09:21 的 dist，而我的客户端源码改动时间是 10:14——产物完全不含我的改动却已存在这些失败，故与本次改动无关。同次运行中需要真实浏览器与实时服务器的用例（如 cold-blank-session、workspace-management）在本机环境同样超时。built-boot 中「boots the built plugin graph」一条失败，该测试文件在我改动前就处于未提交修改状态（git diff 显示新增的 mounts-every-slot 用例等，属上一会话遗留），且该测试不引用我改动的任何包。

4) 构建过程另发现并修复了三个真实缺陷：(a) tsc --noEmit 只覆盖 src 而 build:lib 还类型检查 tests，因此我的测试文件存在未被发现的类型错误——SessionFileRevisions 现需 config 参数、sessions.create 的 meta 不接受 version、RevisionRemote 新增 list 后 mount 辅助函数的参数类型与展开顺序有误、SessionChangesPanel 新增必填 hasPrevious 属性；全部修复后 build:lib 干净通过。(b) 我新增的 store.sessionIds() 无消费者，按仓库「每个抽象都要有当前所有者与需要」的规则删除。(c) 身份校验原先只查被读取的会话，但 list 合并整个子树，重建的子会话记录仍会混入根会话的答案——改为 validatedSubtree 校验子树每个成员，并新增「重建子会话记录不并入根会话」用例；registry 的 subtreeOf 因此改为公开并提供 JSDoc。

测试：packages/fs/session-file-revisions 50（含新增的子树身份用例）、api 13、client 76、编辑器 16。build:lib 无类型错误；verify-changed 682 通过。web 泳道的环境性失败已如实记录，未用放宽断言或跳过用例的方式掩盖。
- [revision 14] acceptance: deep-selfcheck — 深度自检发现并修复两个真实的业务正确性缺陷，均以「先失败、修复后通过」的可执行探针确证。

一、需求完整性（原始 objective 逐条）
objective 只有一条编号项：点击「查看变更」失败时不应提示「撤销失败」。实现位置 RevisionDiffPanel.tsx:143 传 fallbackKey 'diffFailed'，SessionChangesDock.tsx:466 传 'revertFailed'——两个动词各用各的文案，且面板全文不再出现任何撤销措辞（grep t(' 的输出只有 diff.* 与 diffFailed）。产物核验：lib/client.js 含「查看变更失败：{message}」的 diffFailed 键，revertFailed 仅剩撤销路径的调用与字典定义两处。已实现。

设计 D1-D7 逐条：D1 文案归因、D2 错误码分派、D3 持久化（domain/身份/上限/写后即读）、D4 按行能力、D5 编辑器结构化结果、D6 撤销后清理、D7 验证策略——全部有对应代码与用例，见 tasks.md 的 19 条已勾选清单（每条带 covers 锚点）。

spec 的 6 个 Requirement、14 个 Scenario 全部有执行中的用例覆盖：读取差异失败/本会话没有该文件的记录/无法确定会话工作区/其它失败保留原始诊断（revision-diff.client.spec.tsx 的四条文案断言）、重启后仍能查看变更/同名会话重建后旧记录不生效/记录超过容量上限时不写入（capture-e2e.spec.ts 的 durable revisions 组）、没有修订记录的文件行/有修订记录的文件行、编辑器创建的文件的改动被捕获/只读命令不产生改动记录/结果文本保持逐字不变（capture.spec.ts 四条 + capture-e2e 两条 + tools.spec.ts 16 条原样通过）、成功撤销后该文件不再出现在修订记录中/冲突与未变化时保留记录（retiresRevision 四状态）。

二、实现正确性（真实触发，不只看代码）
按用例名逐条运行四个包的 157 个测试，全绿；受影响范围 verify-changed 685 通过 1 跳过；完整 pnpm run build 通过并写入构建记录。

三、业务正确性（发现两个真实缺陷）
缺陷 A：记录集合只在挂载与撤销后读取，而 agent 在会话进行中持续改动文件——刚被改动的文件在读者刷新页面前一直没有「查看变更」，这是常态场景下的假缺失。修复：依赖改为变更条数（changes.length），列表增长即重读。探针 revision-diff.client.spec.tsx 的「offers its controls without a remount」在修复前失败（1008ms 超时）、修复后通过。
缺陷 B：条带「全部撤销」只要存在任何变更就出现，但它撤销的是**宿主记录**而非列表所示——宿主没有任何记录时它承诺一次「已撤销 0 个文件」，与按行门控自相矛盾。修复：改为 changes.some(change => hasRevision(change.path))。探针「offers no revert-all when the Host recorded nothing to revert」在修复前失败、修复后通过。

修复 B 还暴露出三条既有用例在 list 异步落地前就同步点击按钮（假通过）：改为 await screen.findByRole，与读者真实时序一致。

四、构建期发现的三个实现问题（本轮已修）
(a) tsc --noEmit 只覆盖 src，而 build:lib 还检查 tests，故测试文件的类型错误未被早先的包级检查发现：SessionFileRevisions 需 config 参数、sessions.create 的 meta 不接受 version、RevisionRemote 新增 list 后 mount 辅助函数参数类型与展开顺序有误、SessionChangesPanel 新增必填 hasRevision。全部修复后 build:lib 干净。
(b) 新增的 store.sessionIds() 无消费者，按仓库「每个抽象都要有当前所有者与需要」删除。
(c) 身份校验原先只查被读取的会话，但 list 合并整个子树，重建的子会话记录仍会混入根会话答案。改为 validatedSubtree 校验子树每个成员，新增用例「refuses a recreated child record merged into a live root」；registry.subtreeOf 因此公开并补 JSDoc。

五、剩余风险
web 泳道的环境性失败未消除：首轮 10:42 的运行用的是 09:21 的 dist（完全不含我的改动）却已报 135 失败，属既有问题；需真实浏览器与实时服务器的用例在本机超时。我用重建产物单独验证了装配健康的关键用例 built-boot.expected.e2e.ts「mounts every slot entry without abdicating one」通过——它断言不含任何 [data-slot-error]，正是 dock 注册若破坏装配树就会失败的用例。另有 built-boot 的「boots the built plugin graph」一条失败，该测试文件在我改动前即处于未提交修改状态（上一会话遗留），且不引用我改动的任何包。这些未以放宽断言或跳过掩盖。

文档已同步：客户端 README 补记录集合的重读时机与「全部撤销」同一事实门控；Agent Note 的「列表按行判定能力」决策补记增长时重读的理由与依赖取条数而非引用的原因。
- [revision 17] coverage confirmation: 全部已记录的 design（D1-D7）与 spec（6 个 Requirement / 14 个 Scenario）均已实现并通过验证，用户已确认「现在点击查看变更不报错了」。逐点核对：D1 文案归因与 D2 错误码分派见 RevisionDiffPanel.tsx:143 与 SessionChangesDock.tsx:466，由 revision-diff.client.spec.tsx 的四条文案断言覆盖；D3 持久化（domain/身份含子树/上限/写后即读）由 capture-e2e.spec.ts 的 durable revisions 组覆盖；D4 按行能力与增长时重读由该 spec 的按行与「offers its controls without a remount」覆盖；D5 编辑器结构化结果由 capture.spec.ts 四条加 capture-e2e 两条覆盖，且 tools.spec.ts 的 16 条渲染断言原样通过证明模型可见文本逐字未变；D6 撤销后清理由 retiresRevision 的四状态用例覆盖；D7 的验证策略已落实为 157 个包级用例、685 个受影响范围用例与完整构建。深度自检另发现并修复两个真实缺陷（记录集合只在挂载时读取、「全部撤销」在小无记录时的假承诺），两者都以「先失败、修复后通过」的探针确证。
