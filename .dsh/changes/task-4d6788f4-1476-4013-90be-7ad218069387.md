- [revision 10] 实现 D1 分级复核：packages/delivery/tool-delivery/src/index.ts 的 autoDetect 钩子改为只在 gradeObjective 判 l2 时自动建任务，l0/l1 都注入一次 GRADING_RUBRIC（每 turn 一次），rubric 文案改写为说明自动评分未判 l2 并给出 l1/l2 判据。提示词：guidance() 与 record_tasks 的工具描述要求每一级（含 l1）都先确认需求、调用 mark_analysis_done 并 record_tasks 落盘清单，todo_write 只作当轮临时清单。测试：tool-delivery.spec.ts 的 auto-detect 组把「单中信号自动建 l1」改为「不建任务且注入 rubric」，并更新注释。
- [revision 11] 实现 D2 设计文档链接：新增 designArtifact(task) 从 designCount 直接推导 .dsh/design/<task-id>.md（deliveryArtifacts 里按扩展名筛选会命中变更文件，是错误路径）；ui-delivery 的 inject 与 dsh.client.inject 增加 remote/remote.session，注册 conversation.side.float 时注入 openFile（内部经 ctx.remote.session.openWorkspacePath，失败抛 result.error.message）；DeliveryFloatCard 在「设计文档」组 done 时渲染该路径的按钮，打开失败就地提示；locales 增加 progress.design.open 与 progress.design.openFailed；CSS 增加 .designLink 与 .openError。tsconfig 与 package.json 增加 api/remotes、api/session-controller、client/store、todo/tool-todo 引用。
- [revision 12] 实现 D3 默认隐藏与快捷键 + D4 任务列表回落：新增 packages/client/ui-delivery/src/client/visibility-store.ts 的 createDeliveryCardStore()（defineStore，init {visible:false}，persist 'dsh.delivery.float-card'，root 作用域），注册时声明 store 座位；DeliveryFloatCard 常驻挂载，在 document 上监听 Ctrl+Shift+P（大小写不敏感，preventDefault）切换，隐藏时返回 null；任务列表组增加 useProjection('todos')，交付清单为空且 todos 非空时渲染 todo 项与三态状态并在组头标注「来自当轮清单」，清单非空时 todos 完全不参与渲染（不混排）；CSS 增加 .groupHead。测试：delivery-float-card.client.spec.tsx 新增 visibility 与 todo fallback 两组共 11 条用例（默认隐藏、其它按键组合不触发、快捷键展示/隐藏、写入 localStorage 键、卸载后不再监听、链接渲染与点击转交 opener、打开失败就地提示、回落展示与不混排）；browser-plugin.client.spec.tsx 增加 remote 伪造与 opener 两条用例（路径原样转交、拒绝变成 rejected promise）。
- [revision 13] 文档与记录：新增 docs/design/delivery-float-card-usability-rationale.zh.md（现状分析、D1-D4 取舍、影响面与验证策略）；新增 Agent Note .agents/notes/implemented/feature/2026-09-15-delivery-grading-review-and-float-card-visibility.zh.md，并按取代检查就地更新两份既有 note 的过期事实并交叉链接（2026-09-05-delivery-discipline-auto-trigger.zh.md 的机械地板现只在判 l2 时建任务；2026-09-09-delivery-discipline-analysis-progress.zh.md 的浮卡默认可见性改由新 note 持有）；更新 packages/delivery/tool-delivery/README.zh.md（autoDetect 行 + 自动判定段）与 packages/client/ui-delivery/README.zh.md（快捷键、四组进度、设计文档链接、todos 回落与三条新的已知限制）。同时把 record_tasks 的工具描述补上「每一级都要落盘清单」。
