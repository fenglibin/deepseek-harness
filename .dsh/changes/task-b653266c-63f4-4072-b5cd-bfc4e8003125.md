- [revision 4] 实现 MCP 设置页「增加MCP」与「编辑」弹框的配置说明与帮助入口。新增 McpHelp.tsx 帮助弹窗组件与 McpHelp.module.css（dialog 宽 760px、仅 body 滚动以固定标题与关闭按钮、pre 保留缩进与换行使示例可直接粘贴）；三节内容为「基础配置」（stdio 与 streamable-http 各一个示例，共用一个标题、以 label「stdio（本地进程）」/「streamable-http（远程端点）」区分，避免同名标题重复）、「OAuth 服务器」（含 authMode/clientId/authorizationUrl/tokenUrl/scopes 的完整示例，以及保存后状态显示「需要授权」→ 点「去认证」→ 浏览器登录 → 自动重连的步骤说明，并说明令牌存于凭据存储而不写入设置文件）、「可选字段」（allowedTools/env/headers/cwd），末尾说明设置页与 mcp.json 两种入口等价。McpJsonEditor 增加 helpOpen 状态与「帮助」链接按钮，并把 editorHint 作为 Modal 的 description 常驻显示；帮助链接置于编辑器上方而非 footer（footer 属于保存/取消决策）。帮助弹窗经 Portal 独立于编辑器弹窗，关闭它不清空编辑器草稿。locales.ts 新增 17 个键（含 4 个多行 JSON 示例），全部文案与示例均归 settings.mcp 字典——verify-client-ui-i18n 门禁要求产品可见文案不得内联于组件。测试新增 3 项：提示常驻且帮助默认关闭、点击打开并含三节说明与逐字示例、关闭帮助后编辑器草稿保持不变；另加结构断言确认「基础配置」标题只出现一次而两种传输有各自 label。受影响范围 308 个测试通过；verify-client-ui-i18n 通过（569 个源文件）；README 补充帮助入口说明。
- [revision 7] 验证阶段修正一处呈现问题并补齐产物重建。发现并修正：初版把「基础配置」标题在 stdio 与 streamable-http 两个示例上各写一次，渲染出两个同名 h3（实测 HEADINGS: ["基础配置","基础配置","OAuth 服务器","可选字段"]），观感重复；改为两例共用一个标题、新增 helpBasicStdioLabel「stdio（本地进程）」与 helpBasicHttpLabel「streamable-http（远程端点）」以标签区分变体，Section 组件相应支持可选 label 并在 heading 为空时不渲染标题元素，渲染实测修正为三个标题加两个标签。另修正 Label 样式（McpHelp.module.css 新增 .label）。产物重建：两次遇到 lib/types 类型产物过期导致 tsc 报 locale 新键不存在（与之前 oauth 阶段同一类问题），经 tsc -b 与 tsdown 重建后 typecheck 干净，lib/client.js 已含 helpTitle。最终 308 个测试通过（ui-settings-mcp 与 mcp 包），verify-client-ui-i18n 通过，Client 面类型检查对 ui-settings-mcp 无错误。未录制 GUI 演示 GIF：技能要求以干净工作区在单一提交上录制真实服务的真实模型轮次，而当前工作区含 51 项未提交改动（含本次与前一任务的成果）、运行中的 3080 服务要求认证且无法取得访问令牌，因此不具备录制条件；已如实报告而非以 fixture 替代。
