- [revision 6] 实现 P2 客户端内联图片：新增 ImageChipNode（editor/image-node.tsx）+ ImageChip 组件 + CSS module；projection.ts 增加 image 段与有序 images 列表；contract/input.ts 用 DraftPart/DraftImageInsert/DraftImage 取代 imageIds、SessionInput/InputActions 改为 addImages(inserts)/removeImage（删除 pruneImages，新增 releaseImage 依赖）；facade.ts 重构为编辑器编辑（addImages 在 caret 插入 chip、removeImage 移除 node 并释放 registry、deriveParts/trimParts/$forEachDescendant/restoreImages/restoreFailedDrafts 按 offset 重建）；service.ts sendSession 改为 (parts, mode, signal) 并按文档顺序映射 PromptContentPart（contentFor/imageIdsOf）；hub.ts sink 签名 + imageLabels/releaseImage 接线；apply.ts 移除 rail 装配并改用 toDraftImageInserts；slots.ts 移除 conversation.input.attachments；InputBar.tsx 用 input.parts 计数并迁移 intakeFiles；keymap.ts 增加 DRAGOVER/DROP 处理；ui-attachment 删除 ComposerAttachments/AttachmentRail/DropOverlay。测试全部迁移到 parts 签名并新增 image-node/image-chip 组件测试，test:gui 302 files 全绿、oxlint/typecheck 通过。
