- graded l2 by character-floor (329 chars vs floor 200; weak:拆分, weak:优化)
- [revision 9] 实现完成。改动落在用户层 `~/.dsh/settings.yaml` 两处，另附仓库内一份设计草案与一套 OpenSpec 变更。

D1 正文改写：`prompt-commands.commands[deep-selfcheck].prompt` 由原来的单句改为四段（自检依据／三问与可观察证据／修复／收尾顺序）。原句被插入语打断、只有目标没有做法，且完全没有交代它实际是在门禁阻止推进时被读到、必须留下记录才能放行。收尾段补上两处模型无法从别处得知的硬约束：记录文本须以 `acceptance: deep-selfcheck ` 开头（前缀取命令名而非标题「深度自检」，`acceptanceGap` 按此 token 匹配）；`record_change` 会使 revision 加 1，推进前必须重新 `get_delivery_task` 取最新 revision 再 `advance_delivery_task`。`title` 与 `name` 保持原值不动，门禁按 `name` 匹配。

D2 级别写实：删除「L0、L1 级别的任务可能没有设计文档」这处与机制不符的描述——读码确证 `gateAdvance` 要求 l1 阶段 `designCount > 0`，即 l1 必然有 `.dsh/design/<task-id>.md`；l0 根本不创建任务。改写后按级别指明：L2 读 `openspec/changes/<change_id>/design.md` 与 `tasks.md`，L1 读 `.dsh/design/<task_id>.md` 与 `todo_write` 清单（covers 注解写在清单条目行尾）。

D3 勾选为验收命令：新增 `delivery.verificationCommands: [deep-selfcheck]`。写命令名而非正文，因此以后在「提示词命令」页调整正文不需要重新勾选——`acceptanceBodies` 每次都从 `prompt-commands` 命名空间现读。

D4 热重载：读码确证 `packages/settings/settings-file/src/index.ts` 默认 `watch: true` 并以 chokidar 监听，外部编辑 settings.yaml 会自动重载，无需重启 GUI。

验证：`/opt/homebrew/bin/openspec validate update-delivery-selfcheck-acceptance --strict --json` 退出码 0、`valid: true`；YAML 用解析器确证可解析（12 个顶层键）、正文块标量 20 行换行保留、`delivery.verificationCommands` 值为 `['deep-selfcheck']`。

过程中发现的环境问题（不影响本变更）：`npx openspec` 因 `~/.npm/_cacache` 存在 root 所有的文件而 EPERM 失败，改用全局 `/opt/homebrew/bin/openspec`；该缓存目录需要 `sudo chown -R 501:20 ~/.npm` 才能修好。
- [revision 13] acceptance: deep-selfcheck 深度自检完成，发现并修复了一处真实缺陷，另有两项边界如实记录。

一、需求完整性
逐条核对本次诉求：(1) 改写 deep-selfcheck 正文 —— 已落盘 ~/.dsh/settings.yaml:84-104，20 行四段结构；(2) 配置到交付纪律的验收命令 —— 已落盘同文件 :108-110 的 delivery.verificationCommands: [deep-selfcheck]；(3) 回答「是否达到预期目标」与「正文是否可优化」—— 已在交付说明中给出，含三条机制边界。三项均有实现，无遗漏。

二、实现正确性（真实触发，非读码推断）
1. 真实调用 advance_delivery_task 推进到 verified，门禁按预期抛出 DELIVERY_GATE_BLOCKED，且错误消息中嵌入的提示词正文逐字等于新写入的 20 行文本。这一次真实调用同时确证了两件事：配置已生效、正文确实按命令名在门禁时刻现读（acceptanceBodies）。这是最直接的端到端证据，不依赖任何单元测试。
2. 用 YAML 解析器复核：文件可解析、12 个顶层键、正文块标量 20 行换行完整保留、verificationCommands 值为 ['deep-selfcheck']。
3. /opt/homebrew/bin/openspec validate update-delivery-selfcheck-acceptance --strict --json 退出码 0、valid: true、issues 空。
4. settings-file 默认 watch: true 且用 chokidar 监听（packages/settings/settings-file/src/index.ts），外部编辑自动重载；实测该文件被服务规范化重写一次（键顺序变化、我的改动全部保留），证明热重载链路确实在工作。

三、修复的真实缺陷
正文收尾第 3 条原写「record_change 会把 revision 加 1，因此推进前必须重新调用 get_delivery_task」。读码确证 recordChange / recordTasks / recordSpec / markAnalyzed 四处都执行 current.revision + 1（packages/delivery/delivery/src/index.ts:271、:294、:320、:346、:374），即每一次 record_* 调用都会推进 revision，而不只是 record_change。原文案把因果收窄到 record_change、又放在「收尾」末条，会误导模型以为补 covers 注解（第 1 条，可能触发 record_tasks）之后不必重取 revision。已改写为「上面每一次 record_* 调用都会让 revision 加 1，因此每次调用之后都必须重新调用 get_delivery_task」。

四、过程中暴露的机制问题（本次未修，属独立问题）
record_spec(kind: "tasks") 走 renderTasksMarkdown，只按内容匹配更新已有行、保留不匹配的旧行（packages/delivery/tool-delivery/src/index.ts:547 起）。我先后两次写清单，第二次的 8 项与第一次的 6 项内容不同，结果磁盘累加成 14 项，直接触发 checklistMismatch 门禁「has 14 checkbox line(s) but the recorded checklist has 8 item(s)」。该行为对「渐进补充清单」是合理的，但对「整体替换清单」会持续累积重复行。本次以直接重写文件解决。是否应提供显式替换路径，建议单独评估。

五、剩余风险
门禁只保证「留下了记录」，不保证记录内容为真；l0 不经由此门禁（自动建任务只覆盖 l1/l2，index.ts:919）；每个任务只触发一次。
- [revision 16] coverage confirmation: 本任务记录的设计与 spec 全部已实现：docs/design/delivery-selfcheck-command.zh.md 的 D1（正文四段改写）、D2（按级别写实并删除与机制不符的 L0/L1 描述）、D3（revision 提醒覆盖全部 record_* 调用）、D4（勾选为验收命令并写明已知局限）四条决策均已落到 ~/.dsh/settings.yaml 与设计文档本身；OpenSpec 变更 update-delivery-selfcheck-acceptance 的 proposal/design/tasks/spec 四件套已写盘并通过 openspec validate --strict。spec delta 的两个需求（提示词验收留下记录才放行、深度自检命令正文陈述执行方式）共 6 个 Scenario 均由已完成清单项通过 covers 注解声明覆盖，无未覆盖点。
