- [revision 10] 实现内置 MCP 管理的 OAuth（授权码 + PKCE）认证。Host 侧：mcp-manager 新增 auth 判别联合（none / oauth，含 clientId 与端点，缺省为 none，stdio 条目拒绝该字段并在此之前被 validate 钩子拦下）；新增 oauth-grant.ts（令牌以 GrantRecord 存进凭据 seam，经 modifyRecord 排他读写，grantIdOf 把 serverName 翻译成凭据 key 允许的 [a-z][a-z0-9-]* 片段）、oauth-client.ts（PKCE 授权请求、code 交换、refresh，4xx 判为 unauthorized、5xx 判为服务端故障而不丢弃 grant）、auth-sink.ts（每服务器串行化刷新，过期 grant 刷新、无 grant 返回 unauthorized）、oauth-flow.ts（webserver 回调路由 + start，无 webserver 时不注册路由且不失败）；manager 提供 mcpAuthSink、暴露 startAuth Remote 方法、callbackOrigin 用 webserver.port 构造。mcp-client 侧：新增 McpAuthSink 接缝与 McpAuthFailure/McpAuthResolution 类型，McpConnectionStatus 加入 needs-auth，createTransport 接受 authorization 并让解析值覆盖同名 header，connectGeneration 在建 Client 与 transport 之前解析凭据——未授权时报 needs-auth 并停止重连（不建 transport、不进退避），resolver 抛错收容为 failed，stdio 不咨询 resolver。mcp.json 对 OAuth 单向：读方向识别 authMode+端点（缺字段报错而非静默降级），写方向不渲染。浏览器侧：types.ts 镜像 auth 与只读字段、serverJsonToEntry 与 Host 同规则、McpStatusStore 新增 startAuth、McpSection 新增 needs-auth 圆点与「去认证」按钮（本地化文案经 locale 字典），并让 needs-auth 服务器不参与进入分区时的自动重连。包 README 补充认证用法与四条已知限制；新增 Agent Note 记录决策与被拒方案。测试：oauth.spec.ts（12）、mcp-json auth 用例（3）、status-sink 凭据解析用例（5）、oauth-composition.spec.ts 的 REAL-composition（2，经 Loader + cordis.yml 启动，断言 needs-auth 与未命名 auth 的服务器行为不变）；既有夹具因 auth 字段补齐。612 个相关测试通过；verify-client-ui-i18n、verify-export-jsdoc（新增文件无违规）、verify-doc-budgets、verify-agent-note-format 均通过。
- [revision 13] 验证阶段补齐规格 §3.3 的真实实现与证据。原实现只按令牌过期时间刷新，未拦截服务器返回的 401，因此规格中「401 响应触发一次 refresh_token 轮换并重试」当时并无实现：改为在 connectGeneration 捕获 SDK 的 StreamableHTTPError(401)，强制换发一次令牌后重试该次连接；换发被拒则转 needs-auth；非 401 的 HTTP 失败（如 500）保持普通连接故障语义、不换发；每次 outage 由 authRetried 保证至多一次强制换发，连接成功时重置。McpAuthSink.resolve 相应增加可选 options.force，manager 侧 sink 在 force 时跳过「仍认为有效」的短路而直接换发。测试补齐 4 个 401 用例（换发后重连成功、换发被拒转 needs-auth、每次 outage 至多一次、非 401 不换发）与 3 个无 webserver 降级用例（start 报明原因、已存 grant 仍可解析、非 OAuth 与未知服务器被拒），既有 6 个 mock 补上 StreamableHTTPError 的真实导出。受影响范围 275 个测试通过（其中 mcp 包 209 个）。
- [revision 15] 按 verified 阶段逐场景核对证据，补齐三处覆盖缺口。(1) 设置页呈现：mcp-section.client.spec.tsx 新增 4 个用例——needs-auth 服务器显示「去认证」入口并打开 Host 返回的授权 URL、Host 拒绝时以 role=alert 呈现原因且不打开任何页面、已连接服务器不提供该入口、needs-auth 服务器不被进入分区时的自动重连刷新（重连无法补上缺失的凭据）。(2) 令牌隔离：oauth.spec.ts 新增直接断言——写入 grant 后 settings 段落文本与 renderMcpJson 输出均不含令牌，把此前只有间接证据的「令牌不落 settings 文档」变为可执行断言。(3) 授权完成路径：oauth.spec.ts 新增端到端用例——提供假 webserver 捕获回调路由，断言授权 URL 携带 S256 challenge 与 redirect_uri 且不含 code_verifier，驱动回调后 mock 的 token endpoint 被调用、grant 经凭据 seam 落库、onAuthorized 触发重连；另断言伪造 state 的回调以 400 拒绝且不写任何 grant。受影响范围 579 个测试通过；Host 面与 Client 面类型检查对 mcp/oauth 相关文件均无错误；verify-client-ui-i18n 与新增文件的 verify-export-jsdoc 通过。
- [revision 16] coverage confirmation: 6 项设计决策与 6 条规格需求（14 个场景）全部实现并各有可执行证据。D1：McpHttpAuth 联合只进 HttpServerSchema，缺省 none，stdio 携带 auth 由 validateServers 拒绝（oauth.spec 3 用例）。D2：令牌存 GrantRecord，oauth-grant.ts 经 credentialKey('mcp-oauth', serverName) 派生 key、readRecord/modifyRecord 读写（oauth.spec grant 往返 4 用例 + 令牌不落 settings 与 mcp.json 的直接断言）。D3：needs-auth 与 failed 并列，判别点为用户动作可否修复（status-sink 3 用例 + 组件 4 用例）。D4：无 webserver 惰性降级，已存 grant 仍解析、start 报明原因（oauth.spec 3 用例 + 组件拒绝用例）。D5：浏览器不实现 OAuth、不持令牌，只调 startAuth 并 window.open（oauth.spec 端到端 2 用例断言授权 URL 带 S256 challenge、不含 code_verifier、回调写 grant 并触发重连、伪造 state 以 400 拒绝；组件 2 用例）。D6：mcp.json 对 OAuth 只读不写（mcp-json.spec 3 用例）。规格 §3.3 的 401 换发在 verified 阶段补齐实现（StreamableHTTPError(401) 触发一次强制换发，authRetried 保证每次 outage 至多一次，非 401 不换发），status-sink 4 用例覆盖。REAL-composition 测试经 Loader 与真实 cordis.yml 启动，断言 needs-auth 及未命名 auth 的服务器行为不变。受影响范围 579 个测试通过，Host 与 Client 双面类型检查对相关文件无错误，verify-client-ui-i18n、verify-doc-budgets、verify-agent-note-format 与新增文件的 verify-export-jsdoc 通过。
- [revision 18] 深度自检（业务正确性层面，非"代码未报错"）发现并修复 7 个真实缺陷，全部以可执行测试固化。(1) 凭据 key 碰撞：grantIdOf 把 serverName 小写化并把下划线换成连字符，导致 `My_Server` 与 `my-server` 映射到同一 grant、互相覆盖令牌；改为"在凭据语法内则原样通过（但避开摘要形态），否则整体摘要"，并在 JSDoc 说明为何规范化会破坏单射性。(2) mcp.json 手编丢配置：渲染省略 OAuth 细节，而同步把"沉默"读作 none，导致用户编辑任意其他条目就把已有 OAuth 服务器的配置清成 none；mcpJsonToSettings 增加 current 参数，沉默时继承当前分节的 auth。(3) 重复造轮子：仓库已有 dsh-authorization seam（flow 注册表、每凭据一次 attempt、notice/prompt 路由、提交确认），初版 oauth-flow.ts 却自建 pending/TTL/回调；改为注册 authorization flow，由此获得 headless 下"粘贴重定向 URL"的授权通道与提交确认语义。(4) redirect origin 归属错误：初版按 start() 调用记录 origin，不经 start() 发起的 attempt（headless 直接经 seam）取不到 origin 而失败；改为按 flow 记录并回退到本机地址。(5) 回调 origin 硬编码：初版用 webserver.port 拼 127.0.0.1，LAN/代理访问的 GUI 会重定向到错误地址；改为设置页报告自身 origin，Host 校验端口一致后拒绝指向别处的重定向，PKCE 仍是安全边界。(6) 可选服务缺失即崩溃：authorization 是可选 seam，缺失组合（headless/ACP）下 registerFlow 抛 TypeError 致服务器挂载失败；改为经 ctx.inject 接入 + 按调用读取 ctx.get('authorization')，缺失时服务器照常挂载、已存令牌照常使用、仅拒绝新授权。(7) refresh token 丢失：RFC 6749 §6 允许刷新响应不含 refresh_token，初版整体覆盖 renewed 会丢掉仍然有效的旧 refresh token，下次过期即需重新授权；改为省略时保留旧值。另修正一处测试替身失真：手写 CredentialProvider 未 fan out credentials/record-updated，使 authorization seam 的提交确认对替身永远失败（NOT_COMMITTED），替身改为调用受保护的 notifyRecordUpdated。测试从 292 增至 593（受影响范围），新增覆盖含单射性、mcp.json 往返保 OAuth、headless 粘贴授权、origin 校验、无 seam 降级、refresh 轮换三态、flow 随禁用撤销。期间还定位到一处非代码问题：REAL-composition 测试经 file URL 加载 lib/index.js 打包产物，源码改动未 bundle 时会呈现"实现失败"的假象，已重建产物验证。
