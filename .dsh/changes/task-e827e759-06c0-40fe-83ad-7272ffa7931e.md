- [revision 10] 修复 MCP OAuth 配置在设置页不可达且编辑即丢失的缺陷（反转 D6）。根因：设置页的编辑功能建立在 mcp.json 往返之上，而写入方向刻意省略 OAuth 字段，导致编辑器看不到它们、保存时把 auth 写回 none——用户可感知的静默数据丢失。实现：Host 侧 mcp-json.ts 新增 oauthFieldsOf，settingsToMcpJson 在 auth.kind === 'oauth' 时写出 authMode/clientId/authorizationUrl/tokenUrl 及仅在携带时的 scopes，none 条目输出逐字不变；客户端 mcp-server-json.ts 以同规则的 oauthFieldsOf 让 entryToServerJson 渲染同样字段（编辑器所见即所存），两侧规则必须一致，因为编辑器输出要交给 Host 的单条目更新重渲染。读方向的沉默继承规则保留，注释与文档理由从「缓解丢配置」改述为「升级与手编兼容」——旧版本 mcp.json 无这些字段而 settings 已有 OAuth 条目时，首次手编仍会丢配置。文档：包 README 新增「如何配置一个 OAuth 服务器」完整路径（粘贴配置 → 保存 → 状态显示需要授权 → 点去认证 → 回调自动重连），并说明三项必填字段的来源与 scopes 可选；已知限制改述为「OAuth 配置只有 JSON 一种入口，尚无独立表单控件」；设计文档把 D6 标注为已被 D7 取代并记录反转理由；Agent Note 同步改述。测试新增 9 项：编辑器显示与保存保留 OAuth、scopes 省略规则、none 条目零变化、粘贴添加、mcp.json 完整往返逐字段相同、以及端到端断言——经 Host 的 updateMcpServer 编辑一个 OAuth 服务器后 settings 中 auth 仍为 oauth 且 flow 仍被提供。受影响范围 602 个测试通过；Host 与 Client 双面类型检查干净；verify-client-ui-i18n 与 verify-md-links 通过；产物已重建（组合测试经 file URL 加载 lib/index.js）。
- [revision 12] 验证阶段补齐证据与两处修正。(1) 新增端到端断言：经 Host 的 updateMcpServer 编辑一个 OAuth 服务器（模拟设置页保存）后，settings 中该条目 auth 仍为 oauth、clientId 未变，且其 authorization flow 仍被 seam 提供——证明修复覆盖真实保存路径而非仅纯函数往返。(2) 修正两处既有 JSDoc 错位：mcp-server-json.ts 中 serverJsonToEntry 的文档块被后插入的 authOfServerJson 文档挤开，导致该导出函数无 JSDoc；两块的顺序与内容已归位，authOfServerJson 的措辞也从「单向识别」改为「与渲染对称」。(3) 修正新测试的类型收窄：ctx.settings.get 返回 unknown 需显式断言，且 auth 只存在于 McpServerEntry 的 HTTP 分支，读取前按 transport 收窄。受影响范围 602 个测试通过（mcp 与 ui-settings-mcp 为 305）；Host 面与 Client 面对 mcp/oauth 相关文件类型干净；本次新增与修改的导出函数在 verify-export-jsdoc 下无违规（该门禁中 ui-settings-mcp 的其余违规为预先存在，与本次无关）；verify-doc-budgets 通过。
- [revision 14] coverage confirmation: 4 项设计决策（D7/D8/D9 与 D6 的反转）与 mcp-server-auth 的 5 个新增/修改场景、2 个保留场景全部实现并各有可执行证据。D7（读写对称）：Host 侧 settingsToMcpJson 经 oauthFieldsOf 写出 authMode/clientId/authorizationUrl/tokenUrl 与仅在携带时的 scopes——由 mcp-json.spec 的「renders oauth details so a settings-page edit cannot erase them」与「round-trips an oauth entry through the document unchanged」（逐字段断言含 headers 与 scopes）覆盖；Client 侧 entryToServerJson 以同规则渲染——由 mcp-server-json.client.spec 的「shows the OAuth fields」「preserves the OAuth configuration across an edit save」「carries scopes only when the entry has them」覆盖；none 条目零变化由两侧各一条断言（「writes no oauth field for an entry that uses none」「writes no OAuth field for a server that uses none」）覆盖，逐字段证明 authMode/clientId/authorizationUrl/tokenUrl/scopes 均不出现。手编可配置由「accepts pasted OAuth config when adding a server」覆盖。编辑保存不丢配置由组件测试「keeps the OAuth configuration when an edit is saved」（断言编辑器预填含 authMode/clientId，且提交给 Host 的条目 auth 仍为 oauth）与 REAL-composition 测试「keeps an OAuth entry configured when the settings page edits and saves it」（经真实 updateMcpServer，断 settings 中 auth 仍为 oauth 且 flow 仍被提供）覆盖。D8（沉默继承改述为升级兼容）：由 oauth.spec 的「keeps an OAuth server authorized across a hand-edit of mcp.json」覆盖；半配置仍报错由「refuses an entry that names oauth without its endpoints」与「refuses a server whose flow is not registered」覆盖。D9（不新增表单控件）：README 已记录完整使用路径，其「已知限制」条目标明 OAuth 仅有 JSON 入口。全部 602 个受影响测试通过，双面类型干净。
