- [revision 3] 实现 openspec 变更 add-deferred-native-dependency-loading 的全部适用部分。

**attachment-local（主目标）**：新增 `src/sharp.ts`（7 行）导出 `requireSharp = createLazyRequire<typeof sharp>('sharp', import.meta.url)`；`src/image.ts` 改为 type-only 导入，`probeImage` 与 `detectImage` 在 `try` **之外**取 `const sharp = requireSharp()`；`src/normalization.ts` 的 `preparedPipeline()` 新增首个参数 `sharp: ReturnType<typeof requireSharp>`，取值放在 `canPassThroughNormalization()` 早返回之后、`try` 之外；`src/request-image.ts` 在 `sourcePipeline()` 内取值；`encoding.ts` 未改（已是 type-only）。移植官方 `tests/lazy-sharp-failure.spec.ts`（40 行，官方注释译为中文），mock `../src/sharp.ts` 断言三个入口以 `rejects.toBe` 原样传播加载错误。`package.json` 加 `@deepseek-ai/dsh-lazy-require`，`sharp` 保留在 `dependencies`；`tsconfig.json` 加 reference。

**win32-process**：新增 `src/koffi.ts`（10 行）导出 `type Koffi` 与 `requireKoffi`；`src/ffi.ts` 移除静态 koffi 导入，PVOID/PPVOID/STARTUPINFOW/PROCESS_INFORMATION 与 ABI 布局断言收进带缓存的 `win32Types()`，导出的常量替换为 `startupInfoType()` / `processInformationType()` 函数（破坏性变更）；`src/process.ts` 两处 `koffi.free()` 改 `requireKoffi().free()`；更新 5 个测试文件，`process-allocation-failure.spec.ts` 增加 `koffiLoader.requireKoffi` 的 spy mock 使断言与生产代码看到同一份 koffi 导出。

**sandbox-windows-acl**：`src/ffi.ts` 移除静态 koffi 导入，内联 `createLazyRequire<Koffi>` 与 `ffiTypes()`，全部调用点改 `requireKoffi().<method>`；`tests/index-failure-paths.spec.ts` 的 `PROCESS_INFORMATION` 改 `processInformationType()`。

**subprocess-local**：`src/index.ts` 把 `import * as nodePty from 'node-pty'` 改为 `import type * as NodePty` 加 `createLazyRequire<typeof NodePty>`；`src/windows-inspector.ts` 惰性加载 koffi 并把 PVOID 收进 `win32Structs()` 缓存；`tests/local.spec.ts` 把 `vi.doMock('node-pty', …)` 改为 mock `@deepseek-ai/dsh-lazy-require` 的 `createLazyRequire`，对未预期的惰性 specifier 直接抛错。

**terminal-bash**：`src/session.ts` 用 `createLazyRequire<typeof import('@xterm/headless')>` 取代手写 `createRequire`，在 `LocalPtySession` 构造函数内取值；`src/index.ts` 移植 `rejectAfterStartupCleanup()`，`createSession()` 抛错时先 `terminal.terminate()` 回收未发布的 PTY，`startupSession` 失败路径复用它；移植官方 `tests/index.spec.ts` 新增用例。

**语料豁免按实测删除**：`packages/experimental/webworker-runtime/tests/compile/transform-corpus-check.ts` 删除 `win32-process/lib/index.js` 的 `BASELINE_EXEMPT` 条目及其顺序说明注释。依据是构建产物重建后的完整 279 文件清扫报 STALE EXEMPTION（该条目只在文件仍无法导入时才合法），删除后清扫为 276 ok / 3 exempt / 0 findings。

**文档**：更新 `win32-process/README.zh.md` 中"模块加载时断言大小"的失效陈述与懒加载描述；`attachment-local/README.zh.md` 源码地图补 `src/sharp.ts`；新增 Agent Note。另修正 design.md 的 D6 与「被拒绝的方案」、tasks.md 的 5.3，使其与用户指令一致（terminal-bash 启动清理由"不移植"改为"一并移植"）。

未触碰：`encoding.ts`、`subprocess-local/src/linux-execve.ts`（本地不存在）、`api/terminal-controller`（本地不存在）、`heap-watch.ts`、`run.sh`、`benchmarks/`。
