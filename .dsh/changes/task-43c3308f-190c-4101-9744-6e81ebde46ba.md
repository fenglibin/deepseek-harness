- [revision 2] 诊断阶段（尚未改代码）：全量 `npx vitest run` 两次，稳定失败 20–22 项，分布于 10 个 spec 文件。逐条归因如下。

【A 类·门禁/生成器中文化后 spec 未同步（提交 6537426d2d 引入，该提交未改任何 spec）】
1. `scripts/cordis-core-api.spec.ts:24` — 生成器 `scripts/cordis-core-api.ts` 的 `CORDIS_CORE_API_PAGES[].out` 已由 `docs/cordis-api/*.md` 改为 `*.zh.md`，spec 仍按 `.md` 取页，`pages.get(...)` 返回 undefined。实测 `renderCordisCoreApiPages()` 五页键全为 `.zh.md`，且 `### ctx.extend(meta?)`／`## DispatchMode` 内容确实存在。
2. `packages/core/session/tests/gen-persistence-catalog.spec.ts:280` — `scripts/gen-persistence-catalog.ts:362` 已改为生成 `<!-- 由 scripts/gen-persistence-catalog.ts 生成… -->`／`# 会话持久化事件目录`／`来源：`，spec 仍断言英文 `Generated by …`／`# Session Persistence Event Catalog`。
3. `scripts/project-doc-site.spec.ts` 2 项 — `docs/cordis-api/inherited.md` 已随该提交重命名为 `inherited.zh.md`，而 `website/docs.ts:433` 的 `mirroredPages` 条目仍写 `inherited.md` 且 `contentLocale: 'en-US'`；(a) 真实清单投影时报 source 不存在，(b) locale 用例判定「有中文对照却投影英文」。
4. `scripts/verify-subsystem-pages.spec.ts` 7 项 — `scripts/verify-subsystem-pages.ts` 已被该提交改为只扫 `packages/*/README.zh.md` 且只认 `*.zh.md` 链接，spec 夹具仍写 `README.md`。实测真实仓库门禁本身通过（52 group / 45 linked / 7 exempt），失败纯属夹具未同步。

【B 类·本 fork 新增包未登记到 Cordis 目录策略】
5. `packages/typert/generator/tests/cordis-catalog.spec.ts` 2 项 — `ctx.fileBrowserController`（`packages/api/file-browser/src/index.ts:136`，fork 提交 cb0d22a168 新增）的 13 个类型，与 `ctx.mcpResources`（`packages/mcp/mcp-resources/src/index.ts:80`，提交 207da07632 新增）的 `McpResourceProvider`，均未登记到 `scripts/gen-cordis-catalog.ts` 的 `LINK_MAP`。实测 `grep fileBrowser scripts/gen-cordis-catalog.ts` 为 0 命中，即两服务完全未进入该目录的策略表。

【C 类·环境/夹具布局】
6. `packages/delivery/tool-delivery/tests/tool-delivery.spec.ts` 3 项 — 测试用 `mkdtempSync(join(homedir(), …))` 在 `$HOME` 建目录，被文件沙箱 EPERM 拒绝。实测把 `HOME` 指向可写目录后该 spec 111/111 全通过，属环境限制而非代码缺陷。
7. `scripts/session-fixture-layout.spec.ts` 1 项 — 97 个 `snapshots/**/session*.jsonl` 未处于规范打包布局；实测这 97 个与本 fork 改动无关（`git log cb0d22a168..HEAD -- <file>` 为空），且首个差异在 assistant/message 的打包行。

【D 类·并发执行下的时序]
8. `packages/test-support/session-snapshot/tests/harness.spec.ts` 2 项 — 单独运行该 spec 时 63/63 全通过；全量并发下 `timeoutMs: 20` 的 `vi.waitFor` 抛出 vitest 自身的 `Timed out in waitFor!`（`packages/test-support/session-snapshot/src/harness.ts:598`、`:660` 自定义诊断文案未及生效），属负载敏感的 flake。

【E 类·待判定】
9. `scripts/verify-changed.spec.ts` 2 项 — `main([], root, run)` 在工作树无包内改动时走 `NO_SCOPE_EXIT_CODE` 跳过分支，`run` 未被调用。该跳过逻辑由提交 ee05849fb6 引入，spec 却假定必有改动。
10. `packages/client/ui-theme/tests/scrollbar-styles.client.spec.ts` 1 项 — `packages/client/ui-file-browser/src/client/FileTree.module.css:9` 的 `.tree` 用 `--dsw-alias-bg-layer-2` 且 `:40` 的 `.body` 声明 `overflow: auto`，同表未 rebind `--dsh-scrollbar-thumb`，疑似真实产品缺陷（fork 新增包 cb0d22a168）。

【并发干扰事实】工作树开局干净；期间另一 agent 会话在同一工作树并发操作，先后产生 `scoped-slots.tsx` 被改后回滚、`zz-probe2.spec.ts`、`scripts/_tmpcheck/`、26 个改动文件与多个 headless 快照进程。上述归因均在具体命令输出上取证，未依赖工作树整体状态。
- [revision 4] 实施记录：21 项失败 → 0 项（全量 `npx vitest run` 从 21 failed 降到实测 3 failed，再修完后 3 项单跑全通过）。逐项改动如下。

【A 类·中文化遗留（提交 6537426d2d 只改了生成器与门禁、未改任何 spec）】
1. `scripts/cordis-core-api.spec.ts`：断言页键 `docs/cordis-api/*.md` → `*.zh.md`（5 处）；并把 `**Returns**` 断言改为 `**返回**`——生成器只本地化标签、正文保留源 JSDoc 原文。
2. `packages/core/session/tests/gen-persistence-catalog.spec.ts`：表头断言改为 `<!-- 由 scripts/gen-persistence-catalog.ts 生成——请勿手工编辑。`、标题改为 `# 会话持久化事件目录`，并补一条 `来源：` 断言覆盖生成器新增的本地化来源行。
3. `website/docs.ts`：`inherited` 条目 source 由已删除的 `docs/cordis-api/inherited.md` 改为 `inherited.zh.md`，`contentLocale` 由 `en-US` 改为 `zh-CN`，同时把 route 显式化为 `reference/cordis-api/inherited.md` 以保持既有 URL。
4. `scripts/project-doc-site.spec.ts`：`keeps Cordis inherited on the English fallback` 改名并改断言为「从中文源发布到规范路由」——英文回退机制已随英文文档树删除。

【A 类附带发现的真实实现缺陷】
5. `scripts/verify-subsystem-pages.ts`：中文化时把原条件 `page !== 'README.md'` 一并删掉，导致「链接子系统索引页」被误判为合法归属。已恢复为 `page !== 'README.zh.md'` 并补 JSDoc 说明；实测修复前链接索引即通过（loophole），修复后正确拒绝。
6. `scripts/verify-subsystem-pages.spec.ts`：7 项夹具由 `README.md`/`*.md` 改为 `README.zh.md`/`*.zh.md`；「索引页」用例重写为只链索引；新增链接页面不存在的对照报错路径。真实仓库门禁保持 52 group / 45 linked / 7 exempt 全通过。

【B 类·fork 新增包未登记到 Cordis 目录策略（`scripts/gen-cordis-catalog.ts`）】
7. `SERVICE_PAGE` 新增 `fileBrowserController: 'filesystem.md'`、`mcpResources: 'mcp.md'`、`skillRoots: 'skills.md'`。
8. `SERVICE_WALK_EXEMPTIONS` 新增 `mcpAuthSink`、`resources`、`workspaceRowMenu`（均为「门面不可渲染」的客户端/可选键，各带文档归属）。
9. `LINK_MAP` 新增 13 个 file-browser Remote DTO → `filesystem.md`（与 `MessageFeedbackListRequest: 'feedback.md'` 等同族先例一致）。
10. 重新生成 7 个产物（`packages/extensions/tool-cordis/src/api-catalog.ts` 与 6 个 `docs/subsystems/*.zh.md` 区域）；`gen-cordis-catalog --check` 报 54 个文件全部最新。违规从 14+6 项降为 0。
11. `packages/typert/generator/tests/cordis-catalog.spec.ts` 与 `scripts/gen-cordis-catalog.ts` 注释中的 `docs/cordis-api/inherited.md` → `.zh.md`（同一中文化遗留，我先前只修了 website 侧）。

【C 类·环境与布局】
12. `packages/delivery/tool-delivery/tests/tool-delivery.spec.ts`：按用户指示删除 `tool-delivery sandboxed artifact writes` 整个 describe（3 个用例在 `$HOME` 建临时目录被沙箱 EPERM 拒绝），并清理随之不再使用的导入 `afterEach`/`homedir`/`rmSync`/`SandboxedFileSystem`/`SandboxPolicyService`。该 spec 108/108 通过。
13. `snapshots/**/session*.jsonl`：执行 `pnpm run migrate:packed-session-fixtures` 重写 97 个文件为规范打包布局。**无损性已证明**：用 `parseSessionLog` 逐文件比对 HEAD 原始字节与重写后字节，剔除 `seq`/`time` 信封后 97/97 逻辑事件完全一致（mismatched 0）；`session-fixture-layout` 门禁 8/8 通过，175 个 fixture 全部规范。

【D 类·并发时序（真实缺陷，非单纯 flake）】
14. `packages/test-support/session-snapshot/src/harness.ts`：新增 `waitForPersisted()` 取代 9 处 `vi.waitFor` 调用。根因经实验确证：`vi.waitFor` 在超时落在「轮询仍在进行中」时丢弃回调诊断、改抛自己的 `Timed out in waitFor!`，而在负载机器上读盘轮询经常超过 20ms，恰好此时场景诊断（哪个会话缺哪条记录）被通用消息取代——正是失败 CI 最需要的可操作信息。新实现捕获最后一条诊断并在到期时重抛，实测「首次轮询 60ms / 超时 20ms」下诊断保住。该 spec 63/63 通过。
15. `scripts/verify-changed.spec.ts`：`main` 的 3 个用例原先断言真实工作树的改动状态（`run` 必被调用、direct-only 必为子集），而无改动时实现按设计走 `NO_SCOPE_EXIT_CODE` 跳过，因此它们只在「检出恰好带改动」时通过。改为自足夹具：临时 `git init` 仓库 + 已提交的两包树 + 一处编辑，并断言确定的目录集合；另补一条「包外改动即跳过运行器」的用例。该 spec 29/29 通过。

【E 类·真实产品缺陷】
16. `packages/client/ui-file-browser/src/client/FileTree.module.css`：`.tree` 以 `--dsw-alias-bg-layer-2` 绘制浮起面、其 `.body` 声明 `overflow: auto`，却未重绑 `--dsh-scrollbar-thumb`，暗色主题下滚动条会落到 l1 样式（人眼与浅色截图都看不出）。按 `ui-session-changes/SessionChangesDock.module.css` 的既有先例补齐 l2 成对 rebind 与说明注释。`ui-theme` 滚动条契约 spec 21/21 通过。

【F 类·文档一致性】
17. `packages/client/ui-settings-skills/README.zh.md`：`### 开发备忘` → `### 开发备注`（含目录链接与正文共 3 处）。仓库其余 326 处均用「开发备注」，`scripts/doc-standard.spec.ts` 亦强制该标题；此 README 是唯一例外。

【排除项·非本任务范围】
- 快照回放（`test:snapshot`）当前 69 项失败，但**与本任务无关且基线即失败**：用 HEAD 原始字节跑未重写的 `text-turn` 场景同样失败（`llm-replay: script exhausted — session requested model call #2 but its script has only 1`），恢复 HEAD 字节后 `fs-read` 亦同样失败。该层属于另一条独立问题线。
- `packages/experimental/inspector/tests/integration.host.spec.ts` 的 5s 超时在全量并发下偶发；单独运行 10/10 通过，属负载敏感用例。
- [revision 5] 最终验证：全量 `npx vitest run` → **1121 passed | 9 skipped (1130 文件)，18686 passed | 113 skipped，0 failed，exit 0**。

三次全量对比（同一命令、同一工作树）：
- 修复前 `full-run.log`：20 failed / 18661 passed
- 中途 `run-after.log`：3 failed / 18683 passed
- 最终 `run-final.log`：**0 failed** / 18686 passed

关键验证方式：
1. **失败面清零**：`grep -c "^ FAIL" run-final.log` = 0。
2. **逐项回归**：受影响的每个 spec 单独运行均通过——`cordis-core-api.spec.ts` 2/2、`gen-persistence-catalog.spec.ts` 25/25、`project-doc-site.spec.ts` 63/63、`verify-subsystem-pages.spec.ts` 7/7、`verify-changed.spec.ts` 29/29、`cordis-catalog.spec.ts`（含 13/13）、`scrollbar-styles.client.spec.ts` 21/21、`tool-delivery.spec.ts` 108/108、`harness.spec.ts` 63/63、`session-fixture-layout.spec.ts` 8/8、`doc-standard.spec.ts`。
3. **门禁**：`gen-cordis-catalog --check` → 54 个产物全部最新；`verify-subsystem-pages` → 52 group（45 linked、7 exempt）全通过。
4. **fixture 无损性**：97 个重写文件用 `parseSessionLog` 逐文件比对 HEAD 字节，剔除 `seq`/`time` 信封后 97/97 逻辑事件一致（mismatched 0）。
5. **flaky 修复的有效性**：以「首次轮询 60ms、超时 20ms」复现原缺陷（旧 `vi.waitFor` 抛 `Timed out in waitFor!`、新实现保住场景诊断），确认加固针对真实机制而非掩盖症状。
6. **排除干扰**：全量第三次运行（0 failed）时另一 agent 会话仍在同一工作树并发写文件，因此每个结论都锚定在具体命令输出与独立复跑上，未依赖工作树整体状态。

遗留（非本任务范围，已在上一记录归因）：`test:snapshot` 回放失败为基线既有问题（用 HEAD 原始字节同样失败，`llm-replay: script exhausted`）；`inspector` 5s 超时用例单跑 10/10 通过，属负载敏感。
- [revision 10] coverage confirmation: 设计记录中的 D1–D7 每一项均已落实：(D1) 4 个 spec 的中文化断言全部改为跟随实现，含 `**Returns**`→`**返回**` 这处只本地化标签的例外；(D2) `verify-subsystem-pages.ts` 恢复 `page !== 'README.zh.md'` 索引排除并补 JSDoc，spec 保留这 3 项有效守卫；(D3) harness 新增 `waitForPersisted()` 取代 9 处 `vi.waitFor`，以竞态复现证明诊断保住，未放宽 20ms；(D4) `verify-changed.spec.ts` 改为自足临时 git 仓库夹具并补跳过分支用例；(D5) 按用户指示删除 3 个 `$HOME` 沙箱用例及其失效导入；(D6) 97 个 fixture 重写前先证明 97/97 逻辑事件等价再落盘；(D7) 3 个服务、3 个豁免键、13 个 DTO 全部登记并重生成 7 个产物。另有 E 类产品缺陷（FileTree.module.css 补齐 l2 rebind）与 F 类文档一致性（开发备忘→开发备注）均已实现。无未实现或未锚定的设计点。
