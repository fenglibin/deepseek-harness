- [revision 10] 实现完成，改动面如下。

Host 侧新增 packages/api/file-browser（@deepseek-ai/dsh-api-file-browser）：
- src/containment.ts：joinWorkspacePath 做词法拒绝，containPath 用 realpath 解析最深已存在祖先并做包含检查（挡住符号链接逃逸与「新建到不存在路径」两种情形）。
- src/workspace-io.ts：listDirectory（目录优先、名称排序、默认隐藏 dotfile 与 node_modules/.git/dist 等重目录、截断标志、跳过断链符号链接）、readFileContent（text/image/binary/too-large 四臂，图片按扩展名识别、超限先判、再按 NUL 与非法 UTF-8 采样判二进制）、writeFileContent（mtimeMs:size 版本守卫 + 同目录临时文件加 rename 原子发布，失败清临时文件）、createEntry/renameEntry/deleteEntry（名称必须是单个非空路径片段，拒绝删除工作区根）、searchNames（递归遍历、只匹配名称、带上限与截断、跳过隐藏树）、readImageBytes。
- src/index.ts：FileBrowserController（Remote namespace fileBrowser 的 list/read/write/create/rename/delete/search）、每个结构化拒绝到 file-browser/* 线上码的映射、静态 Config 的 zod 校验、/api/file.asset 的 GET/HEAD 路由（与 Remote 共享同一份包含检查）。read 的 image 臂由 Host 合成 url，客户端不拼装路由。
- src/invariant.ts 与 src/types.ts（含 RemoteErrorDetailsMap 声明）。

Client 侧新增 packages/client/ui-file-browser（@deepseek-ai/dsh-client-ui-file-browser）：
- src/client/CodeEditor.tsx + module.css：只读 shiki 高亮层叠一个透明 textarea，两层共用行高与内边距；滚动同步、未保存标记、Ctrl/Cmd+S、冲突时给重新加载与强制覆盖。
- src/client/FileTree.tsx + module.css：懒加载展开、显示隐藏项开关、截断提示、行悬停菜单（重命名/删除）、头部新建文件与新建文件夹。
- src/client/language.ts：扩展名到 shiki 别名的映射，未知扩展名回退纯文本。
- src/client/FileBrowserModal.tsx + module.css：持有一切服务端状态（层缓存、缓冲区、版本、冲突、新建/重命名/删除目标），按 read 的四个分支渲染，删除走 RiskConfirmation 二次确认。
- src/client/index.ts：apply 内两次注册——ctx.workspaceRowMenu 贡献「文件浏览器」条目（用 ctx.effect 包裹以获得卸载时移除），shell.overlay 注册弹层；两者共享闭包持有的打开请求源。
- src/client/locales.ts（含字节单位模板，避免组件内硬编码文案）、src/index.ts 空 node 半、src/invariant.ts、README.zh.md。

改动既有包：
- ui-workspace 新增 src/client/row-menu.ts（WorkspaceRowMenuService：注册表 + HostObservable 投影 + 按 order 与注册顺序排序 + 按身份判定的 disposer），在 apply 中 provide 并投进 browserInjected.hooks，Rows.tsx 的 ProjectRowItem 渲染贡献项（重命名之后、破坏性删除之前）并按 contributed:<id> 前缀分派，WorkspaceBrowser 透传。
- 组合接线：web-app 的 cordis.patch.yml 新增 ui-file-browser 行与 package.json 两个依赖；tsconfig.host.json 增 api/file-browser 引用、tsconfig.client.json 增 ui-file-browser 引用；api/remotes 组装 fileBrowserRemote 并导出其类型；tsconfig.base.json 增两组 paths 映射。
- 重新生成 slot-catalog.ts 与 pnpm-lock.yaml。

验证：Host 65 条 + Client 42 条 + ui-workspace 163 条全部通过；两个 tsconfig 编译面干净；改动包 lint 0 警告 0 错误；verify-cordis-config、verify-client-packages、verify-package-dependencies、verify-client-ui-i18n、client bundle 纯净性与 CSS、gen-client-catalog --check、gen-tsconfig-paths --check、verify-package-readme-limitations、verify-md-wrap、verify-md-links、verify-doc-budgets 均通过；Agent Note 格式门禁通过。
