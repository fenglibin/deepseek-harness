- [revision 2] 「查看变更」不再被宿主修订记录逐行门控，改为每行都渲染；「撤销该文件」保持门控。全屏视图的读取失败改走 revisionFailureText，把 session-revisions/unknown-path 映射为「本会话没有记录该文件的改动」，未映射错误保留宿主原文并冠以视图标签。移除每行「写入/修改」标签：删除 SessionChangesDock.tsx 的 operationKeyOf 与 .operation 样式块（含 data-operation 属性选择器），并从 locales.ts 删除 operation.write/edit/delete 三个键。
- [revision 4] acceptance: deep-selfcheck: 查了什么：逐条核对 objective 两项。(1) 根因确认为 SessionChangesDock.tsx 的 onViewDiff !== undefined && hasRevision(change.path) 门控——无记录时整块不渲染（非禁用、无提示）。现场取证：~/.dsh/storages/ 下 session_file_revisions 目录在 12:56 一次真实 write 之前根本不存在，而 12:20 前跑的会话已有 15 次 edit + 1 次 write 成功且 changedFiles 投影列着这些文件——即那些会话每一行都无记录、每一行都没有图标。捕获链路本身健康（capture-e2e 26 项、controller-restore 32 项、客户端 135 项全绿；现场写入的修订路径与投影路径逐字一致）。改了什么：查看变更改为每行都渲染；撤销保持门控（它写盘，无基线无法兑现）；RevisionDiffOverlay 的失败消息从裸 cause.message 改为 revisionFailureText(cause, t, 'diff.viewerFailed')，使 unknown-path 得到「本会话没有记录该文件的改动」、no-workspace 得到工作区说明、未映射码保留宿主原文并冠以视图标签；删除 operationKeyOf、.operation 样式块（含 data-operation 属性选择器）与 operation.write/edit/delete 三个 locale 键。发现问题并修复：自检脚本初版用 Session.append 直接写 user/message 触发 surfaceOp 校验失败、且用 ctx.sessionProjections.get 取投影（正确 API 是 snapshot(session).values.changedFiles）、并在组件仍挂载时先 dispose 装配导致 useProjection 读到已注销服务——三处均按既有 e2e-business spec 的装配改正，并把这条真实投影驱动的验证固化为正式测试 revision-view-control.client.spec.tsx。第二轮又发现新增的 _edge 临时文件与既有 revision-viewer spec 覆盖重复（unknown-path 与 unmapped 已覆盖），遂只把 no-workspace 一条并入既有文件并删除临时文件。验证：138 项通过（新增 1 个测试文件 + 1 条 no-workspace 用例）；反向验证把 hasRevision 门控加回后 2 条用例立即失败；构建产物经运行中服务器实际返回验证（IconEyeOutline16/IconUndoOutline16 在位、data-operation 与 operation.write 归零）；npx tsc -b tsconfig.client.json 通过，verify-client-ui-i18n / verify-doc-budgets / verify-package-readme-limitations / verify-package-invariants / verify-agent-note-format 均通过。剩余风险：对无记录的路径读者需点击一次才知道该文件不可对照——这是有意取舍（静默隐藏让他连「可以问」都不知道），且视图失败路径现同时承担「真的坏了」与「这个文件没有记录」两种语义，靠错误码区分；历史会话中那些从未落盘的修订无法追回，若要让它们重新可查看需另开一个变更从工作区 git 对象补基线。
