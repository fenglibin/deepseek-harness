- graded l2 by character-floor (392 chars vs floor 200; weak:页面, weak:展示)
- [revision 9] 实现会话文件变更的网页内查看与撤销，以三个新包加装配改动完成，主工程零改动。

新增 packages/fs/session-file-revisions：监听既有的 tools/post-execute waterfall 捕获 write/edit 结果携带的 before/after 全文（exec.agent.session 即归属会话），每 path 只保留首次 before 作基线、最后一次 after 作末态；子代理沿 SessionHeader.parentSession 聚合到根会话。撤销用 jsdiff 反向 patch 逐 hunk 应用，保留外部改动、真冲突拒绝不半改文件。

新增 packages/api/session-file-revisions：sessionFileRevisions Remote 命名空间提供 list/diff/revert。撤销读磁盘当前内容而非捕获末态，逐路径独立应用并汇总结果；写盘前经 realpath 包含检查，回写走临时文件加 rename；超 512KB 只报 oversized 不下发正文但保留可撤销性。

扩展 packages/client/ui-session-changes：dock 每行加「查看变更」、头部加「全部撤销」，复用既有 DiffBlock 原语展示累积差异；两个控件仅在装配捕获时出现，未装配时保持原有列表与接受行为。

装配改动限于 packages/bundle/web-app/cordis.patch.yml；另补包依赖、tsconfig.base.json 源路径映射与 invariant companion 以通过 verify-cordis-config 与 verify-package-invariants。

验证：105 个测试通过（含经真实 ToolRuntime 的端到端捕获与撤销）；三个包类型检查与 lint 通过；host 与 client 两面构建通过；两个新包 README 已补并登记模型体验豁免。
