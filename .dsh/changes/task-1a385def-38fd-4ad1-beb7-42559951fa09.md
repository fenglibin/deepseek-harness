- [revision 2] 把会话页输入框上方的「任务」面板（TodoPanel）从默认展开改为默认收缩：

1) packages/client/ui-conversation/src/client/skeleton/TodoPanel.tsx：`useState(false)` 改为 `useState(true)`，并重写该处注释——原注释陈述的是「计划条是读者持续参照的操作态，所以默认展开」，与新行为相反；新注释说明面板紧贴输入框，表头的计数摘要是常驻读数，条目列表一次点击可达。折叠态形态不变：标题 + `·` 连接的计数摘要 + 折叠切换按钮，条目列表不渲染。

2) packages/client/ui-conversation/tests/todo-panel.client.spec.tsx：把依赖默认展开的断言改写为依赖默认收缩——首条改为断言 `aria-expanded=false` 且列表与条目均不渲染；「展开后每项带状态图标」「并行三项进行中」「全部完成只剩完成计数」三条原先直接读 `listitem`，改为先点击展开；「无交付任务时回落到 todos」「持有任务时读交付清单」两条通过表头计数在收缩态即可判定选中了哪份清单，再展开校验条目正文。

3) packages/client/ui-tool/tests/assembly-surfaces.client.spec.tsx：装配测试原先断言投影驱动的条目行直接可见，改为先断言收缩态无 `li`，点击展开后再断言三态顺序。

4) 未改动：apps/web/tests/todo-row.expected.e2e.ts 已用「若 aria-expanded=false 则先点击」的容错写法，folding 输出不变；apps/web/tests/queue-actions.e2e.ts 只断言面板与 queue/goal 的几何对齐，折叠不改变卡片存在与外框。

验证：`npx vitest run packages/client/ui-conversation packages/client/ui-tool` → 47 文件 / 679 条全部通过。

未覆盖（用户明确要求暂不处理）：交互反馈中的第 2 点「点击变更列表只剩 conversation.input.dock 崩溃占位」。已完成根因定位——ui-session-changes 展开后在存在修订记录的行上渲染 IconEyeOutline16，该图标是本轮新增的 ui-primitives 源码导出，但 ui-primitives/lib/index.js 与 apps/web/dist 均未重建；ui-primitives 是 PLATFORM_MODULES 平台单例，由 shell 静态 import 后共享，因此 session-changes/lib/client.js 取到的该成员为 undefined，渲染抛错被 slot 错误边界收敛为 crash plate（本地构建渲染槽位名）。扫描全部 client bundle 引用的 101 个 ui-primitives 成员，仅 IconEyeOutline16 在 lib 与 shell bundle 中均缺失；ui-chat 产物落后源码约 96 分钟、ui-session-changes 落后约 53 分钟。用户说明当前有任务在执行中，故本轮不重建产物、不改动该链路。
- [revision 3] 修复左侧交付悬浮卡片（DeliveryFloatCard）默认展示的问题——恢复「默认不展示，Ctrl+Shift+P 切换」：

根因不是缺少默认值，而是实现在后续变更中漂移、文档停留在旧契约。packages/client/ui-delivery/src/client/visibility-store.ts 的偏好是三态 `auto`/`shown`/`hidden`，默认 `auto`，而 `cardVisible('auto', hasTask)` 在会话存在当前交付任务时返回 true，因此一有任务卡片就自动出现。然而权威契约一直是二态默认隐藏：openspec/changes/update-delivery-grading-and-float-card/specs/delivery-discipline/spec.md 的「进度栏默认隐藏且快捷键切换」要求「悬浮卡片 SHALL 默认不展示，Ctrl+Shift+P SHALL 在展示与隐藏之间切换」，packages/client/ui-delivery/README.zh.md 与 .agents/notes/implemented/feature/2026-09-15-…zh.md 也一直这样描述，且 DeliveryFloatCard.tsx 的文件头注释至今写着「Hidden by default … stays out of the way until Ctrl+Shift+P asks for it」——注释、README、规格三处与实现互相矛盾。三态改动见 .dsh/changes/task-86c059a8-dfe0-4647-87b8-cd2356105a3e.md 的 D8.2，它翻转了这一默认值但没有同步规格与 README。用户明确选择回到二态。

改动：
1) visibility-store.ts：`DeliveryCardPreference` 收窄为 `'shown' | 'hidden'`；`init` 改为 `{ preference: 'hidden' }`；动作 `cycle` 改为 `toggle`（shown ↔ hidden）；`cardVisible` 去掉 `hasTask` 参数，只看偏好。持久化键由 `dsh.delivery.float-card` 改为 `dsh.delivery.float-card.v2`——旧值可能持有已不存在的 `auto`，复用同键会把无法描述的值 rehydrate 进来；按用户选择「忽略旧值一律重置为隐藏」，换键使所有读者（含显式选过 shown 的）从新默认值开始。版本化键在仓库有先例（ui-workspace 的 `dsh.workspace.view.v7`）。
2) DeliveryFloatCard.tsx：快捷键调用改为 `actions.toggle()`；可见性判定改为只读偏好，并说明任务判定是之后独立的一步（shown 但无当前任务时无可汇报）。
3) delivery-float-card.client.spec.tsx：原「shows the card by default while a task is current」改写为「stays hidden by default even while a task is current」；三态循环用例改为二态往返（并固定第三次按键回到 shown）；「其它按键组合」与「卸载后不再监听」的期望偏好由 `auto` 改为 `hidden`；新增一条用例固定旧键的三态值不会把卡片复活成可见。
4) README.zh.md：更新持久化键名与二态事实（末段原文写的是旧键）。README 前两段「默认不展示 / 展示与隐藏之间切换」本就正确，未改。
5) Agent Note 2026-09-15-delivery-grading-review-and-float-card-visibility.zh.md：该 note 是本决策的持有者，按 implemented/AGENTS.md「就地更新过时事实、不追加变更史」改写键名与偏好取值，并在备选方案补入「让卡片跟随任务自动出现」的被否决理由（auto 等于让卡片默认压回转录，正是本 note 要解决的遮挡问题），防止将来再走回头路；Verification 段同步新断言。

验证：`npx vitest run packages/client/ui-delivery` → 5 文件 / 45 条全部通过（其中 visibility 组 6 条）；`npx vitest run packages/client/ui-delivery packages/client/ui-conversation` → 37 文件 / 417 条通过；`npx tsc -b packages/client/ui-delivery/tsconfig.json` 退出码 0；`npx tsx scripts/verify-agent-note-format.ts` 对我改动的该份 note 零违规（输出中另两条违规属 HEAD 既存问题，文件在工作区未被我修改）。

未处理：跨包引用检查确认 cardVisible / DeliveryCardState / 该持久化键均只在 ui-delivery 内使用，无跨包耦合；apps/ 下无 e2e 引用浮卡。生效仍需重建该包 lib/client.js 与 apps/web/dist（与本任务前一条所述原因相同，用户说明暂不重建）。
