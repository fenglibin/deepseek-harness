- [revision 5] profile pin：~/.dsh/profiles/web/cordis.patch.yml 新增 `- id: response-language` / `config: { language: zh }`，并附三行注释说明本机为何需要它（不存 preference、宿主报 en-US、`en` 无已发布指令）。经 loadProfile + composeEntries 内存组合验证该行解析为 `{"id":"response-language","name":"@deepseek-ai/dsh-response-language","config":{"language":"zh"}}`。
- [revision 6] `packages/context/response-language/src/index.ts` 的 `DIRECTIVES.zh` 在散文条款与注释条款之间插入思考语言条款：「你的思考过程也用中文，包括推理、计划、自检与取舍说明，不要先用英文推理再转述成中文。」字符数 282 → 324。
- [revision 7] `packages/context/response-language/tests/response-language.spec.ts` 新增用例 `names the reasoning channel and forbids English-first reasoning`，断言指令包含「你的思考过程也用中文」与「不要先用英文推理再转述成中文」。整文件 19 条、整个包 20/20 通过（含 loader.spec.ts 的真实 Loader 组合用例）。
- [revision 8] 文档收尾：`packages/context/response-language/README.zh.md` 同步指令围栏块、正文描述（新增「模型的思考过程」）、Token 影响（约 180 → 约 200 token）与已知限制（新增思考语言同为软约束一条）；`docs/design/chat-ux-polish.zh.md` 的指令围栏块同步（该块自述权威源是 `DIRECTIVES.zh`）。
- [revision 9] 新增 Agent Note `.agents/notes/implemented/feature/2026-09-14-reasoning-follows-response-language.zh.md`，记录思考条款的决策、推翻既有否决的理由、三个被否决的备选（单列段落 / `Config.reasoningLanguage` / 只写 persona），以及 `auto` 在本机解析为无段落这一上游事实与部署侧修法。同时按部分取代规则，在 `2026-09-11-code-comments-follow-response-language.zh.md` 与 `2026-09-03-chat-ux-response-language-directive.zh.md` 各自的否决条目上补双向交叉链接，两份旧 note 保留不合并。
- [revision 12] 固化默认值：`packages/context/response-language/src/index.ts` 的 schema 默认值 `auto` → `zh`（并同步模块 JSDoc 与 `Config` 字段 JSDoc），`packages/bundle/base/cordis.patch.yml` 中该行的显式 `config.language` 同步改为 `zh`——后者会覆盖前者，只改一处不生效。`packages/bundle/base/tests/base.spec.ts` 新增断言锁定该行 config 为 `{ language: 'zh' }`。移除 `~/.dsh/profiles/web/cordis.patch.yml` 中现已冗余的个人 pin，使默认值只由工程承担一处归属；经 `loadProfile` + `composeEntries` 验证组合结果仍为 `{"language":"zh"}`。
- [revision 13] 测试：`response-language.spec.ts` 默认值断言改为 `{ language: 'zh' }`，并新增 `directs Chinese from the schema default alone, whatever the host locale is`——在 `LC_ALL=en_US.UTF-8` 下以 `Config()` 装配，断言渲染出的提示词仍含 zh 指令。`docs/config-catalog.zh.md` 的 `ts config-catalog` 围栏块同步新 JSDoc 与来源行号。
- [revision 14] 文档收尾：`packages/context/response-language/README.zh.md` 概述、配置示例与字段表、`auto` 信号解析节、段落缺失节全部改述为「zh 是默认值」；新增 Agent Note `2026-09-14-chinese-is-the-response-language-default.zh.md` 记录默认值变更、三个被否决的备选与代价（非中文部署须显式设 en/off）；`2026-09-02-deployment-response-language.zh.md` 中「默认值 auto」「英文桌面逐字节不变」等已失效陈述改写并双向交叉链接；`2026-09-14-reasoning-follows-response-language.zh.md` 删除已被取代的部署侧 patch 修法段落。12 份快照 composition 与 2 份 fixture/scaffold 中解释 en 固定值的注释随新理由改写（固定值本身未动）。
