- [revision 4] 实现完成。改动面三个包：Host 的 tool-delivery、core/session（事件目录）、Client 的 ui-settings-plugins。

req/1 删除词表：Config 的 strongSignals/mediumSignals/weakSignals 字段、settings schema 三项、DEFAULT_STRONG/MEDIUM/WEAK_SIGNALS 三份常量、以及 countHits/scanSignals/SignalHits/explainGrading/gradeObjective/GradingPolicy/GradingRationale 全部删除。src/grading.ts 由 256 行收缩为约 60 行，只剩 gradeByLength 与 parseGradedLevel 两个纯函数。核对了 src 下无残留引用。

req/2 长度闸门：gradeByLength(objective, floor) 保留为唯一机械判据；同时删除了 designThreshold.descriptionChars（原 ≥60 字强制 L1），因为它会在模型判定之前把大量普通请求升为 L1。过程中发现并修复一处真实缺陷：初版 gradeByLength 硬编码 200，忽略可配置的 openspecThreshold.descriptionChars——设置联动测试当场抓到，改为接收 floor 参数。

req/3 LLM 判定与可配置规则：pre-step 在无任务（或当前为 l0）时先定级再建任务，因此任务一开始就是最终级别。gradingPrompt 作为分级调用的 system 提示词，空值在 resolveConfig 被拒绝（空提示词会让每次判定返回空、每次回退 l1，静默劣化所有分级）。判定失败——无可用路由、抛错、超时（DELIVERY_GRADING_TIMEOUT，30s）或响应无分级标签——一律回退 l1。parseGradedLevel 先找独占一行的标签、找不到才取第一个，因为「this is not l2, it is l1」同时点名两个 tier。路由优先级 lightweightModel → 会话请求头 → agent 自身。新增 delivery/grading-request 会话事件（dispatch 前 append，含路由/system/messages/输出上限），并经 gen-persistence-catalog 重新生成 KNOWN_SESSION_EVENT_TYPES 与 docs/persistence-catalog.zh.md（verify-persistence-catalog 通过）。

req/4 客户端：三个标签控件替换为一个多行文本框（gradingPrompt）。过程中发现 ValueField 只渲染 <input> 且不应用 .wide，卡片传的 textarea 被静默忽略——为 ValueField 补上 textarea 支持与对应 CSS，并加了两条测试。locale 删除 8 个词表相关键、新增 2 个。

req/5 文档：tool-delivery README（配置表、分级机制、create_delivery_task、grading.ts 说明）、客户端 README、locale 帮助文案（分级规则/tier 说明/autoDetect）、docs/design/delivery-llm-grading.zh.md（含实现期修正）、新 Agent Note 2026-09-17-model-graded-delivery-tiers.zh.md，并为两份被取代的方案文档（auto-trigger-rationale、openspec-split-rationale）与卡片 Note 加状态指引/交叉更新。

测试：packages/delivery 254 通过（grading.spec 重写为长度闸门与解析 13 例；tool-delivery.spec 148 例，新增脚本化分级模型 StubLlm + 显式 agent 路由，覆盖命名分级、配置提示词生效、无标签回退、抛错回退、自估值只升不降、依据写入 artifact）；packages/client/ui-settings-plugins 177 通过。两个编译器面均无错误（host 面需先重建包 lib/types，因声明文件是旧的）。

门禁：verify-agent-note-format 未点名新 Note；verify-doc-budgets 8 项在限内；verify-client-ui-i18n 通过；verify-persistence-catalog 通过。verify-md-links 仅剩两处既有断链（delivery-discipline-analysis-progress 与 ui-settings-commands README），均在 HEAD 中已存在、非本次引入；verify-md-wrap 对 docs/design 下 24 份文档一致报出表头引用行的既有样式问题，我的两份与其余 22 份同为该约定。

未决（沿用上一任务）：record_spec(kind: "tasks") 经 renderTasksMarkdown 只按内容匹配更新行、保留旧行，连续整体改写清单会累加并触发 checklistMismatch。
- [revision 7] acceptance: deep-selfcheck 深度自检完成。逐条回答三个问题，证据均为实际执行的命令或测试输出。

一、需求完整性
逐条核对五项诉求，均有实现：req/1（删词表）源码中 strongSignals/DEFAULT_STRONG_SIGNALS 等引用计数为 0；req/2（长度闸门）gradeByLength 是唯一机械判据，且已改为读配置阈值；req/3（LLM 判定 + 可配置提示词）gradingPrompt 在 src 中出现 10 处，含 schema 默认值、resolveConfig 校验、pre-step 与 create_delivery_task 的调用点；req/4（客户端）DeliveryCard 与 fields 中 gradingPrompt/textarea 命中；req/5（文档与测试）见变更记录。

二、实现正确性（真实触发，非只读码）
1. 分级调用真实发出且携带配置文本：用例「sends the configured grading prompt」断言 llm.requests[0].system === 'always answer l2'，通过。这条同时证明了「规则来自配置而非代码内建」。
2. 失败路径真实回退：用例「falls back to l1 when the grading response names no tier」与「falls back to l1 when the grading call throws」均通过，确认不可解析与抛错都回退 l1 而非放行。
3. 长度闸门真实短路：用例断言超过阈值时 llm.requests 长度为 0，确认不发调用。
4. 全量测试：packages/delivery 254 通过、packages/client/ui-settings-plugins 177 通过，合计 433。两个编译器面无错误。
5. 事件目录一致性：gen-persistence-catalog --check 通过，delivery/grading-request 已进入 KNOWN_SESSION_EVENT_TYPES 与 docs/persistence-catalog.zh.md。

三、业务正确性（站在使用者角度）
用脚本核对新提示词是否真的覆盖了旧词表误判的两类场景，7 项检查全部 OK：l0/l1/l2 判据各自明确；正面写明了「只是提到敏感路径或文件名的小修仍是 l0」——这直接对应旧机制把「改 auth.ts 里的注释」判成 L2 的误判；要求「判断意图而非词面」，对应旧机制的子串匹配缺陷；要求只回一个标签。提示词 1115 字符、17 行，不再包含任何关键词清单。另核对了用户 settings.yaml 中 delivery 段没有已删字段残留，不会在加载期因未知键报错。

四、过程中发现并修复的真实缺陷
1. gradeByLength 初版硬编码 200，忽略可配置的 openspecThreshold.descriptionChars——设置联动测试当场抓到，改为接收 floor 参数。
2. ValueField 只渲染 <input> 且不应用 .wide，卡片传入的 textarea 与 wide 被静默忽略，提示词会挤在单行输入框里。已为 ValueField 补 textarea 支持、对应 CSS 与两条测试。
3. StubLlm.stream 缺少 override 修饰符（host 面类型检查报 TS4114）；包 lib/types 声明文件过期导致 host 面按旧签名校验（TS2554），重建后消除。

五、剩余风险
1. 每个 ≤200 字的直接请求多一次模型调用与相应延迟（pre-step 会等它返回，与 compaction-basic 同一模式）。l1/l2 任务存续期间不重复调用。
2. 分级路由持续不可用时，小修也会被回退判为 l1、带上一条设计记录。这是刻意选定的保守方向，但确实会在故障期增加负担。
3. 分级调用的实际判定质量取决于部署所配模型，仓库测试只能覆盖「拿到什么标签就建什么级别的任务」这一链路，无法覆盖语义判断本身是否准确。
4. 既有未决问题（沿用上一任务）：record_spec(kind: "tasks") 经 renderTasksMarkdown 只按内容匹配更新行、保留旧行，连续整体改写清单会累加并触发 checklistMismatch。
- [revision 9] coverage confirmation: 本任务记录的设计文档 docs/design/delivery-llm-grading.zh.md 中 D1-D4 四条决策全部已实现：D1（删除三张词表、只保留长度闸门、并删除 60 字 L1 闸门）——src/grading.ts 收缩为 gradeByLength 与 parseGradedLevel，Config/settings schema/客户端控件/三份 DEFAULT_* 常量删除，源码引用计数为 0；D2（判定由独立辅助 LLM 调用执行、失败回退 l1、新增会话事件、路由优先级）——pre-step 先定级再建任务，DELIVERY_GRADING_TIMEOUT 30 秒，四类失败均回退 l1，delivery/grading-request 事件已注册并经 gen-persistence-catalog 校验；D3（判定规则是可配置提示词、空值拒绝、只回一个标签）——gradingPrompt 为 schema 默认字段，resolveConfig 拒绝空值，parseGradedLevel 先找独占一行的标签；D4（create_delivery_task 保留模型自估值与 bug 强制且只升不降）——raiseByEstimates 实现该规则并有「lets no estimate lower the tier the model named」用例覆盖。原始需求五项均有清单项声明覆盖，packages/delivery 254 与 packages/client/ui-settings-plugins 177 个测试全部通过。
- [revision 11] 深度自检中发现并修正一处我自己写错的事实陈述：grading.ts 的 parseGradedLevel docstring 原本声称「路径里含 l2 不会被读成答案」，但我在测试里实测发现 `src/l0/fixture` 确实会匹配——`\b` 是词边界而非路径分隔符。已把该句改为准确表述：能防住的是 `l2x`／`l10n` 这类更长 token，而路径片段会匹配；并说明契约要求的是裸标签。测试同步保留该真实行为的用例。
