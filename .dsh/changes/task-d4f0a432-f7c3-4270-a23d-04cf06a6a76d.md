- [revision 10] 实现完成：会话插件行开关（含 system 预设、保注释局部写入、success 色开关底色）；新增 pluginInventory/readme Remote 与清单 readme 投影；README 模态弹窗复用 MarkdownText；ui-primitives 增加可选 mermaid 渲染分支与依赖。宿主/客户端 tsc 与 tsdown 均通过，受影响包测试全绿（agent-presets 200、plugin-inventory 20、ui-settings-plugin-inventory 23、ui-primitives 557 含新增 mermaid 2 例）。
- [revision 13] coverage confirmation: 全部 22 项 openspec 任务与 7 条设计决策均已落地并有测试/构建佐证：D1（system 预设可写）由 composition-edit.spec「writes a preset the deployment ships」与 components.client.spec「offers a switch on a row of a preset the deployment ships」覆盖；D2（保注释局部编辑）由「leaves every other byte of the file alone」覆盖；D3（success 色）落在 CSS 并随浅色/深色主题自动切换；D4（复用 MarkdownText + mermaid）由 mermaid.client.spec 覆盖，且未传 diagrams 的会话消息路径行为不变；D5（readme 按需取）由 inventory.spec 的 readme 投影/Remote 用例覆盖；D6（弹窗）由组件测试的 read-more/dialog 用例覆盖；D7（权限收紧保留）由 delete 拒绝与 !!js 拒绝用例覆盖。宿主与客户端 tsc/tsdown 及 web shell 均构建通过。