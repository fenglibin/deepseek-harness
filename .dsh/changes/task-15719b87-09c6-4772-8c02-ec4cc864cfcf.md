- [revision 5] 实现"受防护变更失败在当次调用内重新读取目标"。

**源码（packages/fs/tool-fs/src/）**
- `error.ts`：新增 `recoverMutationFailure`，取代 write/edit 中直接调用 `remediateFsError` 的位置。对 FS_NOT_OBSERVED / FS_STALE_VERSION 重新读取目标（新增私有 `rereadTarget`），把文件当前内容以 read 同款信封附在失败消息里（`<原消息> — its current content follows; retry now.\n<path>…</path>…`），保留错误码并链入 cause。目标无法读取时回落到 `remediateFsError`（纯恢复指令）。`remediateFsError` 保持导出不变。
- `read.ts`：从 `read` 执行器抽出 `readFileWindow`，作为 read 与恢复路径共享的有界窗口读取（含流式决策与 fs/observed 发出）。
- `write.ts` / `edit.ts`：接上恢复路径，新增 `caps: ReadToolCaps` 参数。
- `index.ts`：构造一次 `caps` 并传给 read/write/edit。

**测试**
- `tests/error.spec.ts`：新增 `recoverMutationFailure` 覆盖（内容入消息、错误码与 cause 保留、恢复读取发出 present 观察、FS_STALE_VERSION 同路径、内容受 limit 约束、目标缺失与目录回落、不可恢复错误码与非 FsError 透传且不触发读取）。
- `tests/integration.spec.ts`：更新 4 处旧补救文案断言为新行为；新增 2 个用例断言"不插入任何 read 调用重试即可成功"。

**快照与文档**
- `snapshots/session/fs-policy-reject/`：会话 JSONL 刷新（第二次 edit 由失败变为成功），workspace.expected/settings.txt 由 blue 变为 green。已还原 refresh 引入的无关时间戳噪声。
- `packages/fs/tool-fs/README.zh.md`、`packages/fs/fs-observation-policy/README.zh.md`：更新失败与恢复、源码地图、Token 影响章节。
- `.agents/notes/implemented/feature/2026-09-15-in-call-reread-on-guarded-mutation-refusal.zh.md`：新增 Agent Note，交叉链接 2026-08-03-fs-tool-error-remedy 与 2026-06-26-file-context-as-event-gate。
- `packages/client/ui-chat/src/client/conversation-nodes/recovered-mutation.ts`：注释同步为新的补救语义。

**验证**：tsc 干净；packages/fs 分组 573 passed / 1 skipped；fs-policy-reject 快照 refresh 与 replay 均通过（replay 另有既有 delivery/change 时间戳噪声，改动前后同样失败，与本次无关）；verify-doc-budgets 通过；verify-agent-note-format 与 verify-export-jsdoc 对本包无违规。
- [revision 8] coverage confirmation: 每条设计记录均已实现：D1 恢复位于 dsh-tool-fs/src/error.ts 且策略插件源码未改（保持零文件系统 I/O）；D2 返回内容而非静默重试；D3 重新读取经 readFileWindow 发出 fs/observed，使重试无需额外 read 调用；D4 isError 与 FsError 错误码保留、cause 链入；D5 目标缺失/目录/不可读回落到纯恢复指令；D6 readFileWindow 由 read 与恢复路径共享，read 对外行为与一次 stat 预算不变。唯一未实现项 dsh-tool-str-replace-editor 的同类恢复已在设计与 Agent Note 后果章节明确记录为同问题的另一半，未声称覆盖。
