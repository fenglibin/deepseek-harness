- [revision 7] 新增 packages/llm/image-understanding 能力缝：抽象服务 ImageUnderstanding（resolveRoute / describe）+ LLM Provider LlmImageUnderstanding（路由校验与自动发现、按附件与路由的有界缓存、deadline 超时、逐图降级）+ 准入侧 Consumer describeForRoute（按目标路由声明的 inputModalities 决定是否需要描述，任何失败退化为无描述）。在 packages/bundle/base 的 cordis.patch.yml 与 package.json 中挂载该插件。
- [revision 8] dsh-llm：ImageBlock 增加可选 description（含 ImageDescription 与 ImageDescriptionSource），导出 attachmentDigest 与 describedImageText，replaceImagesForTextModel 在有描述时渲染描述文本、否则保留原省略占位符；GenerateOptions.purpose 增加 'image-understanding'（deepseek-llm-api-extensions 的 purpose 联合同步放宽）。
- [revision 9] session-controller：durablePromptContent 接收目标路由的 inputModalities 并在准入时调用 describeForRoute，把结果按序附加到图片块；纯文本模型的 session/attachment-invalid 拒绝收窄为“没有描述器可用”的情形。补充单元测试、基于 Loader 的 REAL 组合测试、准入路径测试，以及中英双语包 README 与 Agent Note。
- [revision 12] 补齐 SDK 准入路径与 keyless snapshot：sdk-server 的 durablePromptContent 走同一 describeForRoute（含 routeExcludesImages + canDescribeImages 准入检查），并让 describe/describeForRoute 全程携带 owning sessionId（requestDescription 的 GenerateOptions.sessionId），使 llm-replay 把描述调用与主请求绑定到同一 recorded session。新增 snapshots/sdk/inline-image-description 场景（authored recording）pin 住 describedImageText，并 refresh sdk/session 两个 text-turn 的 header sidecar 以纳入 delivery/goal 工具。清理 image-understanding src 下的编译残留，避免 vite 加载旧 consumer 副本。
