- [revision 6] ## Host 侧

**新包 `packages/core/lightweight-model`（`@deepseek-ai/dsh-lightweight-model`）**
- `lightweight-model` 设置域，结构 `{ provider, model }`，两者同时为空 = 未启用；`installSection` 的 `validate` 钩子在设置边界拒绝「只给 provider 或只给 model」的写入。
- 服务 `ctx.lightweightModel`：`currentSelection()` 未启用时返回 `undefined`；`saveSelection()` / `clearSelection()` 通过 `ctx.settings` 写入，无设置提供方时为空操作。
- 组合配置项可作为部署基础路由，用户设置层压在它上面；设置提供方卸载后回落到组合项。
- 双语 README、`README.i18n.yaml`、`src/invariant.ts`（空实现 + `No runtime invariant:` 说明）、7 条单元测试。

**消费方接入（解析顺序：自身显式覆盖 → 轻量模型 → 继承会话路由）**
- `packages/session/session-title-llm/src/index.ts`：`resolveRoute()` 增加 `ctx.get('lightweightModel')?.currentSelection()` 分支；新增 2 条用例（优先级 + 继承）。
- `packages/compaction/compaction-basic/src/summarizer.ts`：`configured ?? lightweight ?? latest ?? agentTarget`；新增 2 条用例。
- 两处都不改 `inject`（可选服务用 `ctx.get`）。

**组合与编译面**
- `packages/bundle/base/cordis.patch.yml`：挂载 `lightweight-model`（无 config，默认空路由）；标题行 `targetCjkCharacters` 10→20、`maxOutputTokens` 64→512（附实测依据注释）。
- `packages/bundle/base/package.json` 加依赖；`tsconfig.host.json` 加 reference；`tsconfig.base.json` 路径别名已由 `gen-tsconfig-paths` 生成并校验通过。
- `session-title-llm` 与 `compaction-basic` 的 `package.json`（peer + dev）与 `tsconfig.json`（project reference）补齐新包。

## Client 侧（`packages/client/ui-settings-models`）

- 新增 `lightweight-model-store.ts`：绑定设置域 + `remote.session.modelCatalog()` 目录，`provider\0model` 不透明 key 只查表不切分，保留已下线路由以便清空，revision 栅栏 + generation 并发守卫，`mode === 'memory'` 时只读且绝不报告未发生的保存。
- 新增 `LightweightModelCard.tsx` + CSS module：纯 props，`<select>` + 「不指定」+ 保存/放弃，全部文案走 `t()`。
- `ModelsSection.tsx` 把卡片插在提供方列表与添加区块之间；`index.ts` 的 `inject` 加 `'remote.session'`，构造/刷新/dispose 全链路接好。
- `locales.ts` 新增 10 个 `en`/`zh` 镜像同序的键。
- 新增 24 条 store 用例 + 8 条卡片行为用例，并补齐 `apply`/`components`/`provider-form` 三处既有用例的注入面。
- `package.json` / `tsconfig.json` 加 `@deepseek-ai/dsh-api-session-controller`。

## 文档

- 新增 Agent Note（EN/ZH/`.i18n.yaml`）：`2026-09-04-lightweight-model-routing`，记录实测证据（hy4-preview 在 64/128/…/4096 全部 `finish_reason: length` 且正文为空，六种关闭思考的参数全被忽略；glm-5.3-flash 在 `max_tokens: 256` 下 6.8 秒产出 20 字标题，思考 228 token）、决策、五条放弃项与后果。
- 新包双语 README 记录该决策；`session-title-llm` 与 `compaction-basic` 双语 README 同步新的路由顺序；`session-title-llm` README 新增「思考 token 计入 `maxOutputTokens`」的已知限制。
- `packages/core/README.md`（组页）中新包一行（双语）。
- 重生成 `docs/config-catalog*.md`（中英同步新包条目）、`docs/module-graph*.md`；`scripts/verify-package-readme-model-experience.ts` 为新包登记 sentence-form 豁免理由。

## 验证

- `npx tsc -b tsconfig.host.json` 与 `tsconfig.client.json`：0 错误。
- 受影响包全量测试：53 个文件 / 785 条全绿。
- 新包 `src` 覆盖率 100%（语句/分支/函数/行）；`session-title-llm/src`、`compaction-basic/src` 无新增未覆盖行；Models 包新文件 100%。
- `verify-cordis-config`（155 个配置文件）、`verify-tsconfig-paths`、`verify-agent-note-format`（686 条）、`verify-client-ui-i18n`（539 个文件）、`verify-export-jsdoc`（本包无违规）、`verify-doc-budgets`（本包无违规）、`verify-package-readme-model-experience`（本包已不再报违规）均通过。
- `pnpm --filter @deepseek-ai/dsh-client-ui-settings-models bundle` 与 `tsdown`（新包 `lib/index.js` + `lib/invariant.js`）构建成功。

## 遗留（均非本次改动引入）

- `gen-doc-graphs` 因既有的 `packages/delivery` WIP（`delivery/changed` 事件类型未登记）抛出，`docs/capability-seams*.md` 与 `docs/subsystems/core*.md` 的 `cordis-surface` 区未能重生成。
- `docs/config-catalog` 语料配对仍报两处既有分歧（EN 多出 `command-prompt-config` 一节与 3 条列表项，ZH 未跟进）。
- `verify-package-dependencies`（`ui-session-changes`、`ui-settings-commands`）、`verify-package-invariants`（`tool-delivery`）、`verify-package-readme-*` 的若干既有违规。
- `Models` 包 `ProviderEditor.tsx:487` 分支未覆盖，由该包 WIP 把内联编辑器换成 `EditProviderDialog` 后失去覆盖，属既有缺口。
- `pnpm run test:snapshot` 91 条失败，全部来自 `packages/delivery` 的 WIP（快照回放使用归档组合，本次 bundle 变更不进入回放）。
