- [revision 9] 实现 MCP 页面「增加MCP」按钮与 JSON 化「编辑」：

1. 新增纯函数模块 `packages/client/ui-settings-mcp/src/client/mcp-server-json.ts`：`parsePastedServers`（剥离 mcpServers 包裹）、`parseDocument`、`parseSingleServer`、`entryToServerJson`、`serverJsonToEntry`、`renderDocument`。
2. `types.ts` 新增 `McpJsonServer` 跨厂商形状类型。
3. `McpJsonEditor.tsx` 新增可选 `title` prop（默认 editorTitle）。
4. `McpSection.tsx`：新增「增加MCP」按钮（配置MCP 左侧），增加/编辑都改为 JSON 编辑器；增加保存走 `parsePastedServers`→同名检测（mcp.json 原始键）→覆盖确认 Modal→`writeMcpDocument` 合并写回；编辑保存走 `entryToServerJson`→`parseSingleServer`→`serverJsonToEntry`→`updateMcpServer`。
5. 删除 `McpServerDialog.tsx` 及其 `.module.css`。
6. `locales.ts`：删除 17 个表单专属键与 4 个残留键，新增 addMCP/addServerTitle/overwriteTitle/overwriteDescription/overwriteConfirm/addInvalid。
7. 测试：新增 `mcp-server-json.client.spec.ts`（17 例），扩展 `mcp-section.client.spec.tsx`（增加/覆盖/编辑流程），扩展 `mcp-json-editor.client.spec.tsx`（title prop）。
8. 更新 `README.zh.md` 与两条 Agent Note（新增 2026-09-08 记录，更新 2026-09-05 的过时「结构化表单」表述）。

验证：`pnpm exec vitest run packages/client/ui-settings-mcp/tests/` 60 通过；`pnpm run typecheck` 通过；`pnpm exec oxlint packages/client/ui-settings-mcp/` 0 错误；`pnpm run verify-client-ui-i18n` 通过；`pnpm run test:gui` 4095 通过。
- [revision 12] coverage confirmation: 所有已记录的设计决策与 OpenSpec 场景均已实现并验证：

设计覆盖：D1（复用 writeMcpDocument）→ mcp-server-json.ts renderDocument + McpSection.saveAdd；D2（单服务用 McpJsonServer 形状，enabled↔disabled）→ entryToServerJson/serverJsonToEntry；D3（纯函数模块）→ mcp-server-json.ts 17 例单测；D4（同名覆盖确认）→ McpSection.saveAdd/confirmOverwrite + overwrite Modal；D5（McpJsonEditor title prop）→ 测试覆盖 default/custom title；D6（删除 McpServerDialog）→ 已删除 .tsx/.module.css 及表单专属 locale 键。

Spec 场景覆盖：ADDED Requirements 的 4 个 Requirement（增加、同名覆盖、编辑、输入边界）及其 Scenario 分别由 mcp-section.client.spec.tsx 的 add flow（mcpServers 包裹剥离、bare map、同名覆盖确认/覆盖）、edit flow，以及 mcp-server-json.client.spec.ts 的边界测试覆盖。

验证证据：vitest 60 例通过、typecheck 通过、oxlint 0 错误、verify-client-ui-i18n 通过、test:gui 4095 通过。test:web（浏览器冒烟+重放 e2e）经用户指示未等待完成即假设通过。
