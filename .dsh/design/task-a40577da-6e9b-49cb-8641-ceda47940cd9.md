- [revision 3] 设计草案落盘于 docs/design/delivery-selfcheck-command.zh.md（含已确证事实表、D1-D3 决策、备选方案、保留局限）。要点：

D1 正文改写：原正文是被插入语打断的单句，只陈述目标、不含做法，也没交代它是在门禁阻止时被读到、必须留下记录才能放行。改写为四段——自检依据（按级别写实：L2 读 openspec/changes/<id>/design.md 与 tasks.md，L1 读 .dsh/design/<task-id>.md 与 todo_write 清单）、三个必须逐条回答并给可观察证据的问题（需求完整性／实现正确性／业务正确性）、直接修复、收尾顺序与 acceptance: 记录格式。原正文「L0、L1 级别的任务可能没有设计文档」与事实不符：l1 必然要求 designCount > 0（gateAdvance），l0 根本不建任务，改写后按级别写实。

D2 勾选为验收命令：写入 delivery 命名空间的 verificationCommands，取值 deep-selfcheck（命令名，不含前导斜杠）。写名字而非正文，因此以后在「提示词命令」页调整正文不需要重新勾选。

D3 保留的已知局限（不改但必须写明）：l0 不触发（自动建任务只覆盖 l1/l2）；门禁只保证「留下了记录」不保证记录内容为真；每个任务只触发一次。

关键事实（已逐条读码确证，非推断）：验收命令存命令名，正文每次经 acceptanceBodies 从 prompt-commands 命名空间现读；触发点是 advance_delivery_task 推进到 verified，不在回合结束时；判定依据是 acceptanceGap 逐命令检查是否留下 acceptance: <name> 前缀的 record_change 记录；阻止时门禁消息携带提示词正文（因 deferContext 只在成功结果上被带走）；settings-file 默认 watch: true，外部编辑热重载，无需重启 GUI。
