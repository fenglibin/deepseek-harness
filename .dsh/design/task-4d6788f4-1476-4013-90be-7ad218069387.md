- [revision 3] 完整设计见 docs/design/delivery-float-card-usability-rationale.zh.md（背景、现状分析、四个决策的取舍、影响面与验证策略）。核心决策摘要：D1 分级复核范围改为「只有强信号或超长才自动建 l2，l0 与 l1 都注入 GRADING_RUBRIC 交模型判定」；D2 设计文档链接由 ui-delivery 插件自己在 apply 里包 ctx.remote.session.openWorkspacePath 成 openFile 经 inject 交给组件（与 ui-session-changes 同构），而非由 conversation.side.float 的 owner 提供；D3 隐藏状态用 defineStore + persist（localStorage 键 dsh.delivery.float-card，root 作用域），组件在 document 上监听 Ctrl+Shift+P 切换并常驻挂载；D4 任务列表组在 delivery-tasks 为空时回落到 todos 投影，不与交付清单混排；另有 guidance()/record_tasks 文案强化要求 l1 也先确认需求并落盘清单。非目标：不改交付执行逻辑与门禁、不改 agent-loop、不解决清单随需求调整而过期的问题、不做跨设备同步。
