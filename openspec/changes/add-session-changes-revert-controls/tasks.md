# 实现清单

- [x] `fs/session-file-revisions` 新增 `line-counts.ts` 以 `diffLines` 计算真实增删行数（`existing` 两侧差异、`absent` 纯新增、`unknown`/`deleted` 为 0），`api/session-file-revisions` 转发该实现并删除按总行数计算的旧实现，补单测证明单行修改报 `+1 -1` (covers: session-file-revisions/整体替换的内容, session-file-revisions/只改一行的内容, session-file-revisions/无基线的文件, session-file-revisions/会话新建的文件, design/D6)
- [x] Host 删除识别：`origin`/`operation` 增加 `deleted`/`delete`、`revisionsDomainSpec.version` 1→2、`git-deletions.ts` 以 `runNativeCommand` 调 `git status --porcelain -z -uall` 并先用 `rev-parse --show-toplevel` 取仓库根再解析成工作区绝对路径（只保留工作区内路径）、按会话在首条可执行代码的命令之前建立基线、增量写 `origin: 'deleted'` 记录取代同路径写记录，配真实仓库单测 (covers: session-file-revisions/会话期间被删除的文件进入列表, session-file-revisions/脚本与间接删除同样被识别, session-file-revisions/会话开始前已删除的文件不列入, session-file-revisions/工作区不是 git 仓库, session-file-revisions/工作区是仓库的子目录, session-file-revisions/删除发生在会话工作区之外, design/D1, design/D2, design/D4)
- [x] Host 删除恢复：`git-restore.ts` 以 `rev-parse --show-prefix` 拼出仓库相对路径后先取 `HEAD` 内容、未命中再取 index，`revertOne` 接入该分支并经原子写落盘，失败分类为 `not-in-git`/`not-a-repository` 两种 `blocked` 判别式，成功后退役记录，配真实仓库单测 (covers: session-file-revisions/恢复已提交的文件, session-file-revisions/恢复尚未提交的新文件, session-file-revisions/工作区是仓库的子目录时恢复, session-file-revisions/文件未加入 git 时报告不可恢复, session-file-revisions/工作区不是 git 仓库时报告不可恢复, design/D3)
- [x] 新增控制器级测试 `controller-restore.host.spec.ts`：以真实 git 仓库与真实文件系统驱动 `SessionRevisionController`，覆盖已提交/仅 index/未跟踪/非仓库/嵌套工作区/已存在/未知基线/会话新建/单路径九类情形与 `list`/`diff` 两个动词 (covers: session-file-revisions/恢复已提交的文件, session-file-revisions/恢复尚未提交的新文件, session-file-revisions/工作区是仓库的子目录时恢复, session-file-revisions/文件未加入 git 时报告不可恢复, design/D3)
- [x] `ui-primitives` 新增 `IconEyeOutline16`（16px 网格与既有描边惯例）并更新图标集计数与来源分类注释 (covers: session-changes-dock/查看变更以眼睛图标呈现, design/D7)
- [x] `locales.ts` 新增 operation.delete、revertOne、revertOneDone、revertNotInGit、revertNoRepository、deleted、deletedHint、lineAdded、lineRemoved 与确认框四类标签，并启用既有却无人引用的 `revertConfirm` (covers: session-changes-dock/删除的行显示已删除, session-changes-dock/未勾选确认框时不能执行, design/D7, design/D8)
- [x] 行内三个动作改为纯图标加可访问名称：接受用勾形、撤销用回退箭头、查看变更用新增的眼睛图标 (covers: session-changes-dock/接受与撤销以图标呈现, session-changes-dock/查看变更以眼睛图标呈现, design/D7)
- [x] 行内显示 Host 报告的真实增删行数（此前 `refreshRecorded` 只取 path 建 Set 把数字丢弃），`origin` 为 `deleted` 的行改显示「已删除」 (covers: session-changes-dock/行上显示真实增删行数, session-changes-dock/删除的行显示已删除, design/D6, design/D7)
- [x] `withRecordedDeletions()` 把只有修订记录知道的删除路径并入列表（折叠只认写调用，被 shell 删除的文件在其内不存在），按 `firstSeq` 排序且不产生重复行 (covers: session-changes-dock/删除的行显示已删除, design/D1, design/D7)
- [x] `SessionChangesDock.module.css` 新增 .lines/.lineAdded/.lineRemoved/.deleted/.iconAction 与撤销/接受的 hover 色，并删除被取代的 .accept 与 .viewDiff 规则 (covers: session-changes-dock/接受与撤销以图标呈现, session-changes-dock/行上显示真实增删行数, design/D7)
- [x] `RevisionRemote` 的 `revertAll()` 改为 `revert(sessionId, path?)`，批量与单个共用同一宿主动词 (covers: session-changes-dock/行内撤销单个文件, design/D5)
- [x] `revert` 改用注入的会话 id，不再读全局活跃会话（消除「点击时会话已切换则撤错会话」的窗口） (covers: session-changes-dock/撤销作用于本条目绑定的会话, design/D5)
- [x] 行内增加撤销控件，仅在该路径有宿主修订记录时出现 (covers: session-changes-dock/行内撤销单个文件, session-changes-dock/没有修订记录的行不提供撤销, design/D5)
- [x] 进行中状态从全局布尔改为按路径的集合：同一路径不重复发起，一个路径进行中不影响其它行 (covers: session-changes-dock/行内撤销单个文件, session-changes-dock/撤销进行中不重复发起, design/D5)
- [x] 单个撤销的失败与成功结果按路径单独报告，不复用批量摘要的计数文案 (covers: session-changes-dock/行内撤销单个文件, design/D5)
- [x] 接入 `RiskConfirmation`：未勾选确认框时执行按钮禁用且不发出请求，取消不发请求，勾选后执行 (covers: session-changes-dock/未勾选确认框时不能执行, session-changes-dock/勾选后执行, session-changes-dock/取消不做任何事, design/D8)
- [x] 确认内容代入将影响的文件数（按宿主记录集合计数，非可见行数） (covers: session-changes-dock/未勾选确认框时不能执行, design/D8)
- [x] Client 单测：图标呈现与可访问名称、行数渲染、已删除行、删除路径并入与去重、单文件撤销只命名该路径、进行中不重复发起、确认框三道门禁、single 失败按原因给文案 (covers: session-changes-dock/接受与撤销以图标呈现, session-changes-dock/查看变更以眼睛图标呈现, session-changes-dock/行上显示真实增删行数, session-changes-dock/删除的行显示已删除, session-changes-dock/行内撤销单个文件, session-changes-dock/没有修订记录的行不提供撤销, session-changes-dock/撤销进行中不重复发起, session-changes-dock/未勾选确认框时不能执行, session-changes-dock/勾选后执行, session-changes-dock/取消不做任何事, session-changes-dock/撤销作用于本条目绑定的会话)
- [x] 确认快照归属：本改动不新增会话事件、不改变会话日志输出，故不落入 snapshots/ 的会话回放范围；其用户可见输出由本包测试与 `test:gui` 覆盖 (covers: design/D7)
- [x] 同步三个包的 README 与 JSDoc 契约，把「会话前未提交改动无法保住」写入 Known Limitations，并补记仓库相对路径与符号链拼写的约定 (covers: session-file-revisions/文件未加入 git 时报告不可恢复, session-file-revisions/工作区是仓库的子目录, design/D3)
- [x] 新增 Agent Note 记录删除识别改走 git 状态查询、基线时机、放弃 pre-execute 内容捕获的依据，以及自检发现的两处子目录路径缺陷，并完成 supersession 检查 (covers: design/D1, design/D3)
- [x] 运行受影响的聚焦检查并如实汇报：三包 273 项单测、`test:gui`（4759 过 / 1 既有失败）、`verify-client-ui-i18n`、`verify-package-invariants`、文档与工作区门禁 (covers: design/D7, design/D8)
