# 实现清单

- [x] 新增 `src/client/accept-store.ts`：用 `defineStore` 声明接受集 store（`Record<path, seq>` 初值、会话后缀持久化键、`accept`/`acceptMany` 写入集） (covers: session-changed-files/接受状态随会话存活并跨页面刷新保持, design/D2)
- [x] `src/client/index.ts` 的 dock 注册声明 `store: acceptStore`，并导出 store 工厂与 `AcceptedChanges` 类型 (covers: session-changed-files/接受状态随会话存活并跨页面刷新保持, design/D2)
- [x] `src/client/SessionChangesDock.tsx` 的适配器改从 `props.useStore` 读接受记录、经 `props.actions` 写入，删除组件本地 `useState` 接受集 (covers: session-changed-files/接受状态随会话存活并跨页面刷新保持, design/D2, design/D6)
- [x] 新增 `isPendingChange` 单一谓词供 `pendingChanges` 与行内接受控件共用，`pendingChanges` 语义不变 (covers: session-changed-files/接受按 (路径, 最后一次变更) 生效, design/D1, design/D3)
- [x] 面板新增「当前变更 / 全部变更」视图切换；「全部变更」列出本会话全部文件并把已接受行标记为「已接受」且不再提供接受按钮，同路径在任一视图至多一条 (covers: session-changed-files/提供当前变更与全部变更两个视图, design/D1)
- [x] 面板去掉无待处理时的整块 `return null`，改为仅在从未有变更时不渲染，使全部接受后仍可进入全部变更视图 (covers: session-changed-files/无待处理变更时仍可访问全部变更视图, design/D4)
- [x] `src/client/locales.ts` 增加两个视图与摘要文案键；`SessionChangesDock.module.css` 增加视图切换与已接受标记样式 (covers: session-changed-files/提供当前变更与全部变更两个视图, design/D1)
- [x] 测试：改写 `shows every change again after the dock remounts` 为重挂载保持；新增跨刷新保持、会话间独立、全部视图含已接受文件、全部接受后仍可进入全部视图、从未变更不渲染、单一谓词用例 (covers: session-changed-files/接受状态随会话存活并跨页面刷新保持, session-changed-files/提供当前变更与全部变更两个视图, session-changed-files/无待处理变更时仍可访问全部变更视图, session-changed-files/接受按 (路径, 最后一次变更) 生效, design/D6)
- [x] 真实装配测试 tests/accept-store-wiring.client.spec.tsx：在生产 SlotRegistry + ui-session scope adapter + renderer 上挂载插件 apply，验证注册声明的 store 被框架解析并交给组件、同会话复用实例、跨会话独立、切走再切回保留、会话移除后随作用域丢弃记录与存储键、持久化键名，以及用框架解析出的实例驱动真实组件 (covers: session-changed-files/接受状态随会话存活并跨页面刷新保持, design/D2)
- [x] 验证：该包 62 项单测通过、typecheck 干净、oxlint 0 警告 0 错误、verify-client-packages 与 verify-client-ui-i18n 通过、src/** 行覆盖 100% (covers: design/D1, design/D2, design/D4)
- [x] 收尾：更新该包 README（接受语义、两视图、已知限制改写）与 Agent Note；校验既有笔记与前身 change 的取代关系 (covers: design/D1, design/D2, design/D5)
- [x] 新增 `src/client/accept-store.ts`：用 `defineStore` 声明接受集 store（`Record<path, seq>` 初值、会话后缀持久化键、`accept`/`acceptMany` 写入集） (covers: session-changed-files/接受状态随会话存活并跨页面刷新保持, design/D2)
- [x] `src/client/index.ts` 的 dock 注册声明 `store: acceptStore`，并导出 store 工厂与 `AcceptedChanges` 类型 (covers: session-changed-files/接受状态随会话存活并跨页面刷新保持, design/D2)
- [x] `src/client/SessionChangesDock.tsx` 的适配器改从 `props.useStore` 读接受记录、经 `props.actions` 写入，删除组件本地 `useState` 接受集 (covers: session-changed-files/接受状态随会话存活并跨页面刷新保持, design/D2, design/D6)
- [x] 面板新增「当前变更 / 全部变更」视图切换；「全部变更」列出本会话全部文件并把已接受行标记为「已接受」且不再提供接受按钮，同路径在任一视图至多一条 (covers: session-changed-files/提供当前变更与全部变更两个视图, design/D1)
- [x] 面板去掉无待处理时的整块 `return null`，改为仅在从未有变更时不渲染，使全部接受后仍可进入全部变更视图 (covers: session-changed-files/无待处理变更时仍可访问全部变更视图, design/D4)
- [x] `src/client/locales.ts` 增加两个视图与摘要文案键；`SessionChangesDock.module.css` 增加视图切换与已接受标记样式 (covers: session-changed-files/提供当前变更与全部变更两个视图, design/D1)
- [x] 测试：改写 `shows every change again after the dock remounts` 为重挂载保持；新增跨刷新保持、会话间独立、全部视图含已接受文件、被接受路径再次变更时两视图各一行、再次接受后当前清空而全部仍一行、全部接受后仍可进入全部视图、从未变更不渲染、单一谓词用例 (covers: session-changed-files/接受状态随会话存活并跨页面刷新保持, session-changed-files/提供当前变更与全部变更两个视图, session-changed-files/无待处理变更时仍可访问全部变更视图, session-changed-files/接受按 (路径, 最后一次变更) 生效, design/D6)
- [x] 真实装配测试 tests/accept-store-wiring.client.spec.tsx：在生产 SlotRegistry + ui-session scope adapter + renderer 上挂载插件 apply，验证注册声明的 store 被框架解析并交给组件、同会话复用实例、跨会话独立、切走再切回保留、会话移除后随作用域丢弃记录与存储键、持久化键名，以及用框架解析出的实例驱动真实组件 (covers: session-changed-files/接受状态随会话存活并跨页面刷新保持, design/D2)
- [x] 验证：该包 62 项单测通过、typecheck 干净、oxlint 0 警告 0 错误、verify-client-packages 与 verify-client-ui-i18n 通过、src/** 行覆盖 100% (covers: design/D1, design/D2, design/D4)
- [x] 收尾：更新该包 README（接受语义、两视图、已知限制改写）与 Agent Note；校验既有笔记与前身 change 的取代关系 (covers: design/D1, design/D2, design/D5)
